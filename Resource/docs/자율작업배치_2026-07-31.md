---
type: ops_plan
project: projectTP
scope: 오너 취침 중 HD-2D 배경 목업 자율 진행
status: 완료 (2026-07-31 야간) — 오프라인 목업 트랙 종료. 결과: [[features/HD2D배경/raw/목업_결과_요약_2026-07-31|결과 요약]]
created: 2026-07-31
approved_by: 오너 ("꼭 지시해줘야 하는 사항이 아니면 독단으로 처리 / 작업 과정별 커밋으로 롤백 포인트")
---

# 자율 작업 배치 — 2026-07-31 야간

> 대상: 2D 타일셋(Winlu) → HD-2D 전투 배경 목업. 근거: [[에셋_후보_카탈로그]] · [[features/HD2D배경/raw/룩_지침_2D타일셋|룩 지침]] · [[전투VFX_방향]]
> 선례 패턴: [[자율작업배치_2026-07-17]](파트4 봉인 배치)

## 안전 속성 (이 배치가 위험하지 않은 이유)

- **언리얼 무접촉** — 에디터가 꺼져 있고, 전 작업이 Pillow 오프라인 이미지 합성 + 문서다. **uasset 0바이트 수정**
- **게임 로직 무접촉** — BP·DT·CSV 라이브 미러 전부 안 건드림
- 롤백 = `git revert`로 충분(문서·신규 이미지뿐, 기존 파일 파괴 없음)
- 커밋은 **정확한 경로만**(`git add -A`·`.` 금지). stray `D:\unreal\docs` 무접촉

## 저장소 분리 (2026-07-26 규약 준수 — [[저장소_구조_규약]])

| 산출물 | 위치 | 저장소 |
|---|---|---|
| 목업 이미지·가공 소재 | `_RawAssets/_mockups/`, `_RawAssets/tilesets/_parts/` | 🔒 **ProjectTP-Resource-Assets**(유료 파생물) |
| 문서·기록 | `Resource/docs/` | 🌐 ProjectTP |

⚠ 목업은 유료 타일셋 픽셀을 담으므로 **공개 저장소 금지**.

## 스테이지 (각 단계 = 커밋+태그 = 롤백 포인트)

| # | 내용 | 태그(공개/비공개) | 상태 |
|---|---|---|---|
| **N0** | 계획 수립 + hd2d 룩 지침 | `save/N0_야간계획` | ✅ |
| **N1** | 목업 v1 + 밀도 비교 + 소재 추출 | `save/N1_목업v1`(비) · `save/N1_스크립트`(공) | ✅ |
| **N2** | 구도 재설계 v2 + 룩 적용 | `save/N2_목업v2`(비) | ✅ *판정: 개선 부분적* |
| **N3** | qa 유효범위 판정 + 카탈로그 정정 | `save/N3_qa판정`(공) | ✅ |
| **N4** | ★캘리브레이션 실증(FAIL) + 엔진 실간격 배치 | `save/N4_캘리브레이션`(비) · `save/N4_문서`(공) | ✅ |
| **N5** | 문서 정합화 + 복귀 브리핑 | `save/N5_마감`(공) | ✅ |

> **계획 대비 변경**: N4를 "오프라인 가공(슬라이싱·추출)"에서 **"캘리브레이션 실증"**으로 교체했다.
> 사유 — N3에서 qa가 *"목업으로 색조·밀도 확정 불가"*를 판정했고, 그게 사실이면 **가공을 더 해봐야 판단 근거가 안 되기** 때문. 먼저 도구의 유효성부터 판가름하는 게 순서였고, 결과는 FAIL이라 **트랙 종료**가 옳은 판단이 됐다. (가공은 임포트 직전에 하면 되므로 손실 없음.)

## ★N3의 핵심 질문 (무감각 게이트 방지)

Pillow 목업이 **예쁘게 나왔다고 언리얼에서도 그렇다는 보장이 없다.** 목업은 2D 합성이고 실제는 3D 라이팅·머티리얼이다.
→ qa에게 **"이 목업으로 내릴 수 있는 결정 / 내릴 수 없는 결정"의 경계**를 판정시킨다. 경계를 모르면 목업만 보고 배율·색조를 확정했다가 언리얼에서 뒤집힌다.

## 오너 게이트 (여기서 정지 — 독단 금지)

| # | 항목 | 이유 |
|---|---|---|
| **G1** | **미적 판정 — "이 배경 방향이 좋은가"** | 프로젝트 규칙상 미적 최종판단은 오너 권한(CLAUDE.md) |
| **G2** | **픽셀 밀도 배율 확정** | Director가 1차 권고안을 내되 확정은 오너 |
| **G3** | **언리얼 임포트 착수** | 에디터 꺼짐 + A1 마감 후 아트 단계 순서 원칙 |
| **G4** | **push** | CLAUDE.md "push는 오너 확인" — 야간엔 **커밋만**, push는 복귀 후 |

## 중단(halt) 트리거

① 목업이 3회 시도해도 깨짐 → 중단·기록 ② 유료 에셋이 공개 저장소 스테이징에 잡힘 → 즉시 중단 ③ 기존 파일을 덮어쓰게 되는 상황 → 보류·기록

## 복귀 브리핑 항목

1. 목업 이미지(v1/v2 비교) 2. Director 배율 권고안+근거 3. qa 판정(목업의 유효 범위) 4. 가공 완료 소재 목록 5. 태그 목록 6. 오너 결정 대기 4건
