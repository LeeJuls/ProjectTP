---
type: test
project: projectTP
feature: 스킬연출구조
updated: 2026-08-11
status: TC 확정 — D4 게이트 대기
---

# D3 — TC 확정 + D1 적대 검증

> 상위: [[plan]] · 검증대상: [[D1_4슬롯구조_확정]]
> 참조: [[D4.5a_VFX재고_실측]] · [[F7b_재개계획_초안]] · [[SPD원장_오라클_v1]] · [[balance1_포지션킷_v1]] · [[F7_스킬아키텍처_확정]] · [[네이밍_폴더_규약]] · [[데이터_서버_규약]]
> 조회 범위: 파일·CSV만. **MCP 0회 · 수정 0건 · 커밋 0건.** D1 문서 무편집.
#projectTP/스킬연출구조

---

## 0. Director 3줄

1. **D4 게이트를 막는 Critical 4건.** X1 린트 L09가 라이브 `job_stats` **6/6행을 FAIL**시키고 그 해소책이 F7b 5스킬 회귀 자산을 건드린다 / X2 `Layer`가 라이브 struct에 없는데 D1이 "존치"로 분류해 **오너 struct 1회 편집 원칙이 깨진다** / X3 CSV 컬럼 ↔ struct 필드 집합 분리 미정의(무음 드롭) / X4 액티브 25원자의 문자열 키 **54종**이 어느 단계에도 배정돼 있지 않다(L17 FAIL).
2. **D1 반증 4건**: "라이브 5스킬 이동·시간 무변"(파이어볼 반증) / "투사체 신규 컬럼 0"(struct 필드 교체 1건) / "신규 문자열 키 전량 27종"(실측 81종) / "§5-4 병합 후 린트 전량 PASS"(구조상 불가). **행 수 주장(프로필 4행)은 30/30 커버리지로 실증 통과.**
3. **N6은 D6까지 이월 가능**(값 결정, 스키마 무영향). **N3은 Director가 닫음** — 다만 그 실측이 X2·X3을 새로 드러냈다.

---

## 1. TC 체계

### 1-1. 시드 17건의 출처 고지 (가짜 근거 방지)

`TC-P01`~`TC-G07` 시드 17건은 **어느 파일에도 기록돼 있지 않다**(`docs/` 전체 grep 결과: `plan.md:385` 1줄뿐, 목록 없음). 본 문서는 시드를 **plan §검증 22행 + D1 §8 E1~E20**에서 재구성했다. ID 매핑이 원안과 다르면 Director가 정정하되, **커버리지(무엇을 검증하는가)는 두 문서의 합집합이므로 누락은 없다.**

- `TC-P01~P05` = 패시브 축 (plan §검증 원장무영향·메뉴무오염·로그분리·무음폐기0·재귀가드)
- `TC-S01~S05` = 연출/스테이징 축 (극단8표본·회귀표현·레고검증·거리가드·N6회귀)
- `TC-G01~G07` = 데이터/게이트 축 (CSV린트·패시브상한·스칼라잔존·fail-loud·파트3회귀·멀티히트·타이밍회귀)
- `TC-N01~N14` = **본 문서 신규** (Director 판정 A~E + D1 확정서 + N3 실측이 만든 것)

### 1-2. 표기 규약

```
[단계][ID] 조건 → 기대결과 | 상태(대기/PASS/FAIL/이월)
```

| 필드 | 값 |
|---|---|
| **게이트 귀속** | `D4`(설계 게이트) / `D4.5`(행 확정) / `D5`(값 배정) / `D6`(구현) / `이월` |
| **실행 주체** | `V`=verifier 기계 / `O`=오너 육안 / `V+O`=기계 선행 후 육안 |
| **실행 가능 시점** | `NOW`=지금 CSV/문서만으로 실행 가능 / `PIE`=에디터 필요 / `LINT`=검증 스크립트 완성 후 |
| **판정** | PASS/FAIL이 **기계로 갈리는 형태**만 허용. "잘 보인다" 금지 |

### 1-3. ★린트 3계층 분리 (TC 설계상 필수 — D1 §5-4 step7 반증의 귀결)

D1 §5-4 step 7은 병합 직후 "§10 린트 **전량 PASS**"를 검증란에 적었다. **구조상 불가능하다** — 병합 시점에 액티브 25행은 `StagingId=0`·`Category=""`·`HitCount=0`이고 이는 L03이 FAIL시킨다(값은 D5가 채운다). 린트를 3계층으로 갈라야 verifier가 게이트마다 무엇을 돌릴지 알 수 있다.

| 계층 | 규칙 | 적용 게이트 | 미충족 시 |
|---|---|---|---|
| **T1 구조** | L01 · L21 · L24 · 컬럼셋 일치 · 토큰 수 | **병합 직후(D4.5)** | 병합 실패 — 되돌린다 |
| **T2 참조무결성** | L07a/b · L08 · L09 · L10 · L11 · L13 · L15 · L16 · L17 · L18 · L19 · L22 | **D5 완료 시** | 값 배정 미완 |
| **T3 완전성·밸런스** | L02 · L03 · L04 · L05a/b · L06 · L12 · L14 · L20 · L23 | **D6 임포트 직전** | 임포트 금지 |

> T2에 L09·L17을 넣은 것은 **지금 이미 FAIL이기 때문**이다(§4 X1·X4). "언제 PASS로 돌아오는가"에 담당 단계가 붙어야 한다.

---

## 2. 시드 17건 — 실행 가능 형태로 확정

### 2-1. 패시브 축 (TC-P01~P05)

---

**[D6][TC-P01] `PassiveId=0` 유닛의 PN-1~5 불변식** — plan §검증 "원장 무영향" / D1 §1-3 · E1
- **선행조건**: `job_stats.csv` 6행에 `PassiveId` 컬럼 추가, **전 행 `0`**(D1 B14). `DT_JobStats` 재임포트. F7b ⑦ GREEN. 8기 시나리오 = SPD원장 §0 고정입력(A3→B3→A4→B4→A1→B1→A2→B2, 전 유닛턴 기본공격 31000000만).
- **실행 절차**:
  1. Method A 자동 시나리오로 20 유닛턴 완주, 로그 전문 캡처.
  2. 로그에서 `BattleLog|` 행만 추출 → `SPD원장_오라클_v1` §7 파싱 CSV와 **행 단위 diff**.
  3. 같은 로그에서 `PassiveLog|` grep.
  4. `RunEffectBucket` 진입 카운터(임시 int 멤버 또는 `PassiveLog| enter` 1줄)로 호출 수 집계.
- **판정 기준**(5개 전부 AND):
  - PN-1 `RunEffectBucket` 패시브 경로 호출 수 **== 0**
  - PN-2 `PassiveLog|` 행수 **== 0**
  - PN-3 `BattleLog|` **행 단위 diff == 0** (요약값 일치는 PASS 근거 아님 — H2 규칙)
  - PN-4 `ActiveStatuses` 배열 길이 변동 == 0 ∧ `bBlockActive`/`BlockValue` 변동 == 0 (전 유닛턴 스냅샷)
  - PN-5 총 재생 시간이 MA-1 기준선 대비 **± 0 프레임**
- **게이트**: D6 / **주체**: V / **시점**: PIE
- **상태**: 대기

---

**[D6][TC-P02] 메뉴 무오염 3중 차단** — plan §검증 "메뉴 무오염" / D1 §2-4 · E9·E10
- **선행조건**: 병합 `skills.csv` 임포트 완료(패시브 10행 + 프루브 2행 포함). C6 거절 가드 구현됨.
- **실행 절차**:
  1. `job_stats.SkillIds` 6행 전부 토큰 수 카운트.
  2. PIE 진입 → 8기 각각 `WBP_SkillMenu` 자식 위젯 수 집계.
  3. 콘솔/디버그 함수로 `NotifySkillSelected(35000000)` **강제 호출**.
  4. 이어서 `NotifySkillSelected(31000000)`(정상 스킬) 호출.
- **판정 기준**:
  - `SkillIds` 토큰 수 == 3 (6/6행)
  - 메뉴 Row 수 == 3 (8/8유닛), 패시브 Id가 어느 Row에도 스탬프 **0회**
  - 3번 호출 후: RG-1 `PendingSkillId` 미변경 ∧ RG-2 `PendingTargetToken` 미변경 ∧ RG-3 `SelectedTargets[]` 미변경 ∧ RG-4 상태 == `AWAIT_COMMAND` ∧ RG-5 `BattleLog|` +0줄 ∧ RG-6 `PassiveLog|` **정확히 +1줄**(`reason=PASSIVE_NOT_SELECTABLE`) ∧ RG-7 `bSelected` 변동 0
  - 4번 호출이 **정상 수락**(소프트락 0) — `PendingSkillId == 31000000`
- **게이트**: D6 / **주체**: V / **시점**: PIE
- **상태**: 대기

---

**[D6][TC-P03] 훅 2종 거동 + 순회 결정론** — D1 §7-2
- **선행조건**: 임시 유닛 1기에 `PassiveId != 0`(상시형: `Timing=ON_BATTLE_START`, `EffectType=STATUS`, `DefaultDuration=-1`) 부여. **★이 시나리오는 SPD 원장 봉인 밖의 별도 세이브**에서 돌린다(원장 §0 전제 훼손 금지).
- **실행 절차**: 8기 전원에 서로 다른 패시브 부여 → `StartBattle` → 3턴 진행 → `PassiveLog|` 전문 캡처. 동일 시드로 **3회 반복**.
- **판정 기준**:
  - `phase=BATTLE_START` 로그가 **슬롯 인덱스 오름차순**(A1,A2,A3,A4,B1,B2,B3,B4)으로 정확히 8줄, 3회 반복 **문자열 동일**
  - `phase=BATTLE_START` 로그 8줄이 **전부 첫 `EnterTurnStart` 로그보다 앞**
  - `phase=TURN_START` 로그가 턴당 정확히 1줄, `unit=` 값이 그 턴의 행동 유닛과 일치
- **게이트**: D6 / **주체**: V / **시점**: PIE
- **상태**: 대기

---

**[D6][TC-P04] 무음 폐기 0 + Timing 화이트리스트** — plan §검증 "무음 폐기 0" / D1 E19 · L04·L06
- **선행조건**: `RunEffectBucket` 추출 완료(C7, ⑦ GREEN 이후 별도 커밋). 린트 스크립트 L04·L06 구현.
- **실행 절차**:
  1. `skill_effects.csv`에 **액티브 스킬 소유 + `Timing=ON_TURN_START`** 행 1개를 임시 삽입 → 린트 실행.
  2. 린트를 우회해 강제 임포트 → 해당 액티브를 캐스트 → 3-패스(PRE/ON/POST) 필터 도달 로그.
  3. `Timing=ON_KILL`(예약어, 화이트리스트 밖) 행 삽입 → 린트 실행.
- **판정 기준**:
  - 1번: 린트 종료코드 != 0 ∧ 출력에 `L06` 포함
  - 2번: 3-패스 필터 매칭 **0회** ∧ **경고 로그 1줄 이상**(에러 0·로그 0의 무음 폐기가 **재현되면 FAIL**)
  - 3번: 린트 종료코드 != 0 ∧ 출력에 `L04` 포함
- **게이트**: D6 / **주체**: V / **시점**: LINT+PIE
- **상태**: 대기

---

**[D6][TC-P05] 재귀 가드 depth ≤ 1** — plan §검증 "재귀 가드" / D1 §7-4 · E8
- **선행조건**: 자기 자신을 재트리거하는 패시브 1개 구성(`ON_TURN_START` → STATUS 부여 → 그 STATUS가 다시 훅을 부르는 구성). `PassiveReentryDepth` 멤버 존재.
- **실행 절차**: 해당 유닛 턴 1회 진행. 이어서 **다음 턴**도 진행.
- **판정 기준**:
  - `PassiveLog| reject reason=REENTRY_DEPTH` **정확히 1줄** ∧ 프레임 행오프/무한루프 0(턴이 정상 종료)
  - **다음 턴에 패시브가 정상 발동**(= `EnterTurnStart` 진입 시 depth 0 재설정이 실제로 동작 — 영구 차단 회귀 방어)
- **게이트**: D6 / **주체**: V / **시점**: PIE
- **상태**: 대기

---

### 2-2. 연출·스테이징 축 (TC-S01~S05)

---

**[D4][TC-S01] 극단 8표본 표현 실증 + 30원자 프로필 커버리지** — plan §검증 "극단 8표본"(D4 진입 조건) / D1 §3-6
- **선행조건**: 없음(문서·CSV만). `data/drafts/skills_30원자_초안.csv` 30행 + D1 §3-2 4행 + §3-6 8표본 표.
- **실행 절차**: 스크립트로 (a) D1 §3-2 "담당 원자" 목록을 파싱해 30원자 Id에 프로필을 배정 (b) 배정 집계 (c) §3-6 8표본 행의 `MotionRow`·`Target`·`Layer`를 30원자 초안 실값과 대조.
- **판정 기준**:
  - 30원자 중 프로필 **미배정 0개** ∧ **중복 배정 0개** (기대: 62000100=12, 62000200=3, 62000300=8, 62000400=7, 합 30)
  - 8표본 8행 전부 `MotionRow`·`Target`·`Layer`가 초안 CSV와 **완전 일치**
  - 프로필 총수 ≤ 8 (D1 §3-3 과설계 상한)
- **검증 결과 (본 문서에서 선실행)**: **PASS.** 12+3+8+7 = 30/30, 중복 0, 미배정 0. 8표본 8행 전부 실값 일치(31000000/5, 31012000/8, 32011000/11, 32001000/10, 31011000/7, 33001000/15, 34001000/11, 35000000/-1). 프로필 4 ≤ 8.
- **게이트**: D4 / **주체**: V / **시점**: NOW
- **상태**: **PASS**

---

**[D6][TC-S02] `AdvanceRatio` = 실제 Lerp 알파 (0.9 → 90% 이동)** — plan §`AdvanceRatio` 소비 계약 / D1 §4-2·4-3 · E15
- **선행조건**: `stagings.csv` 임포트, `ResolveApproachAnchor` 구현, C5 배선 완료. **임시 프로필 1행**(`AdvanceRatio=0.9`, 나머지는 62000100과 동일)을 추가하고 기본공격 31000000이 그것을 가리키게 한다.
- **실행 절차**:
  1. 캐스터 시작 위치 `S`, `ResolveApproachAnchor()` 반환 `A`를 로그 1줄로 출력.
  2. 접근 이동 완료 시점의 실제 위치 `P`를 로그 1줄로 출력.
  3. `AdvanceRatio`를 `0.0` / `0.5` / `0.9` / `1.0` 4회 반복.
  4. `AdvanceRatio=1.2` 및 공란으로 임포트 시도.
- **판정 기준**:
  - 각 α에 대해 `|P − Lerp(S,A,α)| < 1.0 uu` (4/4)
  - α=1.0일 때 `P == 현행 AttackPoint_<team> 위치`(회귀 0의 실증)
  - α=0.0일 때 접근·**복귀 양쪽 모두 스킵**(WalkBack 재생 0 — 비대칭 금지)
  - α=1.2 → 임포트 **FAIL** ∧ 공란 → 임포트 **FAIL**
  - 런타임에 범위 밖 값 주입 시 `Clamp` + **로그 1줄**(무음 클램프면 FAIL)
- **★무음 실패 0의 정의**: α=0.9에서 이동 거리가 α=1.0과 **같으면 즉시 FAIL**(계약이 소비되지 않는다는 증거).
- **게이트**: D6 / **주체**: V / **시점**: PIE
- **상태**: 대기

---

**[D6][TC-S03] 거리가드 AND 결합 — 없던 경로 0** — plan §검증 "거리가드 신규 경로" / D1 §3-5
- **선행조건**: C5 배선 완료, `CameraCutMinDistance` 데이터 멤버(초기값 1.0).
- **실행 절차**: 4조합 각 1회 캐스트 — (IsCameraCut, dist) = (T, ≥1.0) / (T, <1.0) / (F, ≥1.0) / (F, <1.0). 카메라 컷 발동 여부를 로그 1줄로 기록.
- **판정 기준**: 컷 발동 == **(T, ≥1.0) 1조합에서만**. 특히 **(T, <1.0)에서 발동하면 FAIL**(= "근거리인데 컷"이라는 없던 경로가 열림).
- **게이트**: D6 / **주체**: V / **시점**: PIE
- **상태**: 대기

---

**[D6][TC-S04] N6 회귀 — `AttackPointOverride` Clear 지배** — plan §검증 "N6 회귀" / D1 §4-2
- **선행조건**: `StagingId` 조회 재배선(C5) 완료.
- **실행 절차**: (1) `AttackPointOverride`를 세팅하는 스킬 캐스트 → (2) 같은 유닛의 **다음 턴**에 파이어볼 캐스트 → 이동 종점 로그. (3) `RowNotFound` 경로 강제(존재하지 않는 `StagingId` 주입) 후 동일 반복.
- **판정 기준**: 두 경로 모두에서 다음 턴 종점이 `AttackPointOverride` 잔존값이 **아님**. Override 필드가 캐스트 종료 시 **무효 상태**로 관측될 것.
- **★이 TC는 오라클 diff 0을 통과하는 무음 버그를 잡는 유일한 관문이다** — 실패 시 "다음 턴 파이어볼이 아군에게 걸어간다"가 로그상 정상으로 보인다.
- **게이트**: D6 / **주체**: V / **시점**: PIE
- **상태**: 대기

---

**[D4][TC-S05] 레고 검증 — 신규 원자 1개 편집 지점 수** — plan §검증 "레고 검증" / D1 §3-3
- **선행조건**: D1 §5-3 최종 스키마 확정.
- **실행 절차**: 가상의 신규 근접 원자 1개(`31050000`)를 추가한다고 가정하고 편집이 필요한 파일·그래프를 열거한다. D6 이후에는 **실제로 1행 추가 후 BP 노드 수 스냅샷 diff**.
- **판정 기준**: 편집 지점 == `skills.csv` 1행 + `strings.csv` 2행. **BP 그래프 노드 수 diff == 0**이면 PASS, 1노드라도 바뀌면 FAIL.
- **게이트**: D4(문서 판정) → D6(노드 diff 실증) / **주체**: V / **시점**: NOW→PIE
- **상태**: 대기

---

### 2-3. 데이터·게이트 축 (TC-G01~G07)

---

**[D4.5→D6][TC-G01] CSV 린트 24규칙 실행 가능성** — plan §검증 "CSV 린트" / D1 §10
- **선행조건**: `validate_ids.py` 계열에 L01~L24 구현. 병합 `skills.csv` 존재.
- **실행 절차**: 계층별로 3회 실행(T1/T2/T3, §1-3). 각 규칙에 대해 **위반 샘플 1행을 일부러 넣고** 종료코드를 확인(규칙이 실제로 물리는지 = 가짜 GREEN 방지).
- **판정 기준**:
  - 24규칙 전부 위반 샘플에서 **FAIL을 반환**(하나라도 통과시키면 그 규칙 FAIL)
  - 정상 데이터에서 T1 == 0건 위반
  - **L19는 `FrameCount <= GridX`로 정정되었는가**(§4 Y3) — 정정 전 규칙은 `FrameCount=20, GridX=13, GridY=9`를 통과시킨다. 이 케이스를 넣어 FAIL이 나야 PASS
- **게이트**: D4.5(T1) → D5(T2) → D6(T3) / **주체**: V / **시점**: LINT
- **상태**: 대기

---

**[D5][TC-G02] 패시브 상한 기계 검증** — plan §검증 "패시브 상한" / D1 L23 · §12 B11
- **선행조건**: D5가 `status_effects.csv` 패시브 상태 행을 채움.
- **실행 절차**: 패시브 10행이 참조하는 상태의 채널별 누적을 스크립트로 합산.
- **판정 기준**: `Atk` 배율 합 ≤ +0.15(FAIL 임계) ∧ `> +0.07`이면 WARN 출력 ∧ `OUT_MULT` ≤ +0.10 ∧ `Def` 가산 ≤ +3.
- **★선결 조건 (§4 Y5)**: `Def +3`은 **가산**인데 `status_effects.Channel` 어휘는 `SKIP_TURN/DOT/OUT_MULT/IN_MULT` 4종뿐이고 **가산 채널이 없다**. 이 TC는 채널 어휘 확정 전엔 실행 불가 → 확정 전이면 **이월**로 표기하고 D5 착수를 막는다.
- **게이트**: D5 / **주체**: V / **시점**: LINT
- **상태**: **대기(선결 미충족)**

---

**[D6][TC-G03] 텍스처 스왑 시 스칼라 4종 동시 세트** — plan §검증 "스칼라 잔존" / D1 §3-7
- **선행조건**: `vfx.csv` 2행 이상(서로 다른 `GridX`), MID 재설정 노드 구현.
- **실행 절차**: FX A(예: GridX=13) 재생 → 직후 FX B(예: GridX=8) 재생 → B 재생 중 `get_scalar_parameter`로 `RowIndex`·`FrameCount`·`GridX`·`GridY` 4종 조회.
- **판정 기준**: 4종 전부 **B의 값**. 하나라도 A의 값이 잔존하면 FAIL. (A→B→A 순으로 3회 왕복해 캐시 잔존 없음까지 확인)
- **게이트**: D6 / **주체**: V / **시점**: PIE
- **상태**: 대기

---

**[D6][TC-G04] fail-loud — 다른 FX 폴백 0** — plan §검증 "fail-loud" / D1 E17 · §3-7
- **선행조건**: `vfx.csv`에 존재하지 않는 Id를 스킬 `FxImpactId`에 주입.
- **실행 절차**: 해당 스킬 캐스트 1회. 화면 캡처 + 로그 캡처.
- **판정 기준**: 로그 **정확히 1줄**(미발견 사실 명시) ∧ `EffectQuad` **미재생**(임팩트 슬롯 MID 텍스처 파라미터 변동 0) ∧ **다른 FX가 대신 나오면 FAIL**. 캐스트는 정상 완료(크래시·소프트락 0).
- **게이트**: D6 / **주체**: V / **시점**: PIE
- **상태**: 대기

---

**[D6][TC-G05] 파트3 회귀 — `IsAttackFamily` 제거 후 잔상** — plan §검증 "파트3 회귀" / D1 §3-8
- **선행조건**: `motions.csv`에서 `IsAttackFamily` 제거 + `FxMotionId` 추가(C2·C4). **TC-N07의 이관 매핑이 선행**.
- **실행 절차**: 치유(MotionRow 11) 1회 · 기본공격(MotionRow 5) 1회 · 파이어볼(MotionRow 10) 1회 캐스트.
- **판정 기준**:
  - 기계: 캐스트 슬롯 MID의 텍스처 파라미터 세트 횟수 — row11 **0회**, row5 **1회 이상**, row10 **1회 이상**
  - 육안(O): 치유에 참격 잔상 **없음**, 기본공격·파이어볼에 잔상 **있음**
- **★방어선이 2겹→1겹이 된다** — 기계 판정을 반드시 먼저 통과시키고 육안은 확인용.
- **게이트**: D6 / **주체**: V+O / **시점**: PIE
- **상태**: 대기

---

**[D6][TC-G06] 멀티히트 — 판정 1회 / 연출 N회** — plan §검증 "멀티히트" / D1 §3-4 · E11
- **선행조건**: 31011000 연타에 `HitCount=3` 배정, `HitIntervalSec` 데이터 멤버.
- **실행 절차**: (1) 연타 1회 캐스트 → 로그·데미지 확인. (2) 타겟 HP를 1로 세팅 후 연타 캐스트(첫 타에 사망) → 잔여 임팩트 관측.
- **판정 기준**:
  - `BattleLog|` **정확히 1줄** ∧ `ApplySkillEffects` 호출 **1회** ∧ 총 데미지 == 단일타 데미지(3배 아님)
  - 임팩트 FX 재생 **3회** ∧ `EffectQuad` 절단(중간 프레임에서 다음 재생이 덮어씀) **0회**
  - 2번: 잔여 2회 임팩트는 FX·SFX만 ∧ `IsTargetFlinch` **스킵**(Hurt 모션 재생 0 — 시체 움찔 0) ∧ 판정 재실행 0
  - 쿨다운 세팅 **1회**
- **게이트**: D6 / **주체**: V / **시점**: PIE
- **상태**: 대기

---

**[D6][TC-G07] 타이밍 회귀 — MA-1 재생 원장 diff 0** — plan §검증 "타이밍 회귀" / D1 C12
- **선행조건**: ③④ 구현 완료. MA-1 기준선 원장 확보.
- **실행 절차**: Method A 자동 시나리오 재실행 → 재생 원장 전 행 diff.
- **판정 기준**: **전 행 diff 0**.
- **★유효 범위 고지**: F7b가 MA-1을 *"도구 검증은 **기본공격 패턴 한정**"*으로 규정했다. 기본공격은 62000100(`AdvanceRatio=1.0`)이라 diff 0이 나오는 것이 정상이며, **이 TC는 파이어볼·멀티히트·투사의 턴 길이 변화를 잡지 못한다.** 그 축은 TC-N08·TC-N09가 담당한다 — MA-1 PASS를 "턴 길이 무변"의 근거로 쓰면 **가짜 GREEN**이다.
- **게이트**: D6 / **주체**: V / **시점**: PIE
- **상태**: 대기

---

## 3. 신규 TC — Director 판정 A~E · D1 확정서 · N3 실측이 만든 것

---

**[D4→D6][TC-N01] `Layer=PROBE` 3중 배제 (판정 A)**
- **선행조건**: 병합 `skills.csv`에 프루브 2행(`31002000`·`31003000`) `Layer=PROBE`·`DevStatus=PROBE`. A2 생성기 코드(있으면).
- **실행 절차**:
  1. **풀**: 생성기의 4개 풀 필터를 각각 실행해 결과 Id 집합을 덤프.
  2. **메뉴**: `job_stats`/`characters`의 `SkillIds` 전량에서 PROBE Id grep(L10).
  3. **유일성 키**: 생성된 조합 키 `(PositionId, 평타, 공용, 직업, PassiveId)` 전량에서 PROBE Id grep.
  4. **런타임**: `NotifySkillSelected(31002000)` 강제 호출(디버그 off 상태).
- **판정 기준**: 1~3에서 PROBE Id 등장 **0회**(3/3) ∧ 4에서 `PassiveLog| reject reason=PROBE_NOT_SELECTABLE` 1줄 + 상태 무변(RG-1~7).
- **★새 구멍 (§4 Y7)**: 4번의 조건 `!bDebugProbesEnabled`에 쓰이는 **`bDebugProbesEnabled`가 D1 §11 BP 신규 멤버 목록에 없다**(목록은 `PassiveReentryDepth` 1개뿐). 또한 이 가드가 기본 on이면 **F7b ⑧ 프루브 E2E 게이트(T02·C03)가 실행 불가**해진다 — ⑧은 D6과 시간대가 겹친다. → **선결: (a) `bDebugProbesEnabled` 멤버 명세 (b) D6 병합 임포트 시점을 F7b ⑧ GREEN 이후로 못 박기.** 미결이면 이 TC는 실행 불가.
- **게이트**: D4(선결 판정) → D6(실행) / **주체**: V / **시점**: LINT+PIE
- **상태**: **대기(선결 미충족)**

---

**[D4.5][TC-N02] `ColorRow` 어휘 린트 (판정 D)** — ★D1 L01~L24에 `ColorRow` 규칙이 **하나도 없다**
- **선행조건**: `vfx.csv` 존재. 신규 린트 **L25** 구현.
- **실행 절차**: 아래 4케이스를 `vfx.csv`에 넣고 린트 실행.
- **판정 기준**:

| 케이스 | 기대 |
|---|---|
| `ColorRow = 0` ~ `5` | **PASS** (6케이스 전부) |
| `ColorRow = 6`, `7`, `8` | **FAIL** — 알파 예약(판정 D). 메시지에 `L25` 포함 |
| `ColorRow = 9` (GridY=9 초과) | **FAIL** — `ColorRow < GridY` 위반 |
| `ColorRow = 3`, `GridY = 1`(라이브 T_FX_Smear 계열) | **FAIL** — 같은 규칙에 걸려야 함 |

- **본 문서 선실행 근거**(`vfx_inventory_raw.csv` 180행 독립 재집계): row0~5 = 180/180 계열 일관 ✅ / row6 = 161/180(예외 19, hue 30.8~32.9°) / row7 = 174/180(예외 6, 35.8~37.1°) / row8 = **180/180이 223.6~279.2° 단일 연속 대역**. → **판정 D는 row6·row7에 대해 근거가 확실하다(진짜 이봉 분포). row8의 "12건 뒤집힘"은 4계열 버킷 경계(250°) 아티팩트이며 실제 색조 점프는 없다** — 예약 자체는 안전측이라 판정에 이의 없음, 다만 근거는 정정해 기록한다.
- **잔여 리스크(TC 아님, D5 인계 메모)**: row0의 파일 간 색조 폭이 **66.1°**(347°~55°), row3은 **63.9°**다. "row0=화염"으로 규약화하면 파일에 따라 빨강~노랑이 섞인다. 알파 팔레트는 6행이지만 **색 계열은 5종**(row0·row4가 둘 다 적-주황)이다.
- **게이트**: D4.5 / **주체**: V / **시점**: LINT
- **상태**: 대기

---

**[D4.5][TC-N03] `vfx.csv` ↔ 재고 CSV 기계 대조 가능성 (판정 E의 귀결)**
- **문제**: 판정 E로 "신규 합성 0, 남은 건 임포트뿐"이 확정됐다. 그런데 **`GridX`가 파일마다 5~23으로 다르다**(실측). D1 §6-2 `vfx.csv` 스키마에는 **원본 파일을 가리키는 컬럼이 없다**(`TexPath`는 임포트 후 `/Game/...` 경로). → `vfx.csv`의 `GridX` 오타 1개 = UV 폭 오류 = 무음 화면 깨짐이고, **기계로 검증할 조인 키가 존재하지 않는다.**
- **선행조건**: `vfx.csv`에 `SrcFile`(원본 상대경로) 컬럼 추가 — 또는 `#id_txt` ↔ 파일명 유도 규칙 1개 확정.
- **실행 절차**: `vfx.csv`의 각 행을 `SrcFile`로 `vfx_inventory_raw.csv`에 조인 → `GridX`·`GridY`·`FrameCount` 3값 대조.
- **판정 기준**: 조인 실패 행 **0** ∧ 3값 불일치 행 **0** ∧ `FrameCount == GridX`(이 팩 실측 180/180).
- **게이트**: D4.5 / **주체**: V / **시점**: LINT
- **상태**: **대기(선결: 조인 키 컬럼 미정의)**

---

**[D4][TC-N04] CSV 컬럼 집합 ↔ struct 필드 집합 일치 (N3 실측의 귀결)** ★Critical X3
- **문제**: D1 §5-3 최종 스키마 22컬럼 중 `Layer`·`PositionTag`·`DevStatus`는 **런타임 소비처가 D1 어디에도 없다**(offline 린트 전용처럼 보인다). 그런데 §2-4 거절 가드는 `row.Layer == "PASSIVE"`를 **DT 행에서 읽는다** → `Layer`는 struct 필수. `PositionTag`·`DevStatus`는 미정. UE DT 임포터는 struct에 없는 컬럼을 **경고 후 드롭**하므로, 정하지 않으면 무음 손실이 된다.
- **선행조건**: Director가 **(a) struct 필드 목록 (b) CSV-only 컬럼 목록**을 분리 확정.
- **실행 절차**: 임포트 후 `ObjectTools.list_properties(/Game/Data/F_SkillsRow)` 결과와 `skills.csv` 헤더를 스크립트 대조.
- **판정 기준**: `set(struct 필드) == set(CSV 헤더) − set(CSV-only 선언 목록)` ∧ 임포트 로그에 `not a member` / `extra column` 경고 **0건**.
- **게이트**: **D4(목록 확정)** → D6(대조) / **주체**: V / **시점**: NOW→PIE
- **상태**: **대기(선결 미충족 — D4 게이트 항목)**

---

**[D4][TC-N05] 린트 L09가 라이브 `job_stats` 6행에서 통과하는가** ★Critical X1
- **실행 절차**: `job_stats.SkillIds`의 3토큰을 30원자 초안의 `Layer`로 매핑해 `[BASIC, COMMON, JOB]`과 비교.
- **본 문서 선실행 결과 (CONFIRMED FAIL)**:

| 행 | 토큰 | Layer 시퀀스 | L09 |
|---|---|---|---|
| char_warrior_g1/g2/g3 | 31000000;31001000;33001000 | `[BASIC, COMMON, COMMON]` | **FAIL** |
| char_mage_g1/g2/g3 | 31000000;32001000;34001000 | `[BASIC, JOB, JOB]` | **FAIL** |

- **판정 기준**: 6/6 PASS여야 D6 임포트가 가능하다. **현재 0/6.**
- **★해소책이 회귀 자산을 건드린다**: 전사의 3번째 슬롯 33001000(막기)은 `Layer=COMMON`, 마법사의 34001000(치유)은 `Layer=JOB(SUPPORT)`이고 32001000(파이어볼)은 `JOB(DEALER)` — **한 유닛이 서로 다른 포지션의 JOB 스킬 2개를 든 상태**다. L09를 만족시키려면 라이브 `SkillIds`를 바꿔야 하는데, 그러면 **F7b ⑦ 게이트의 "5스킬 회귀(30/34 · 42+STUN · 61+ATK_DOWN · 막기15 · 치유−33)"에서 막기·치유가 라이브 유닛 메뉴에서 사라진다.**
- **Director 결정 항목(택1)**: (A) L09를 `characters.csv`에만 적용하고 `job_stats`는 **명시적 grandfather 예외**로 문서화 / (B) 라이브 `SkillIds` 개편 — 단 F7b ⑦·⑧ 회귀 세트 재정의 동반 / (C) L09를 "3토큰 · 각 토큰 존재"까지로 축소하고 **순서 강제는 `characters.csv`만**.
- **게이트**: **D4** / **주체**: V(실측 완료) + Director(판정) / **시점**: NOW
- **상태**: **FAIL — D4 결정 대기**

---

**[D4][TC-N06] 문자열 키 완비 (L17)** ★Critical X4
- **본 문서 선실행 결과 (CONFIRMED FAIL)**: 병합 대상 42행이 참조하는 `NameKey`/`DescKey` 중 **`strings.csv`에 없는 키 = 54종**(액티브 25원자 ×2 = 50, 프루브 2행 ×2 = 4). 여기에 D1 §12 B7의 패시브 20종을 더하면 **미등록 74종**, D1 §9-4의 UI 7종을 더하면 신규 총 **81종**이다.
- **D1 §9-4는 "신규 문자열 키 전량 = 27종"이라고 선언했다 → 반증.** 27종은 UI 7 + 패시브 20이며, **액티브 25원자의 50종 + 프루브 4종이 어느 단계에도 배정돼 있지 않다**(§12 B1~B14에 없음).
- **판정 기준**: `for k in skills.NameKey ∪ skills.DescKey: k ∈ strings.Key` — 미존재 **0건**이면 PASS. 현재 **54건**.
- **게이트**: **D4(담당 단계 배정)** → D5(채움) → D6(L17 검증) / **주체**: V / **시점**: NOW
- **상태**: **FAIL — D4 배정 대기**

---

**[D4.5][TC-N07] `IsAttackFamily` → `FxMotionId` 이관 매핑 (무음 잔상 소실 방어)**
- **문제**: D1 §6-5는 `motions.csv`에 `FxMotionId` 추가 + `IsAttackFamily` 제거만 규정하고 **이관 값 매핑을 지정하지 않았다.** 전 행 0으로 초기화하면 **현재 잔상이 나오는 6개 모션의 잔상이 전부 사라진다**(무음 회귀).
- **실측 대상**: `IsAttackFamily == TRUE` 행 = RowIndex **5, 6, 7, 8, 9, 10** (Atk1/Atk2/Atk3/AirAtk1/AirAtk2/Casting1) — 6행. `FALSE` = 나머지 11행(Casting2는 파트3에서 의도적으로 FALSE 전환됨).
- **판정 기준**: 이관 후 `motions.csv`에서 `FxMotionId > 0`인 행 집합 == `{5,6,7,8,9,10}` ∧ `FxMotionId == 0`인 행 집합 == 나머지 11행. **불일치 1행이라도 FAIL.**
- **게이트**: D4.5(매핑 확정) → D6(대조) / **주체**: V / **시점**: LINT
- **상태**: 대기

---

**[D6][TC-N08] 파이어볼 라이브 거동 변경 — 의도 확인 + 회귀 범위 확정** ★D1 §4-2 반증
- **문제**: D1 §4-2가 *"라이브 5스킬 이동 거리·시간 **무변**"*이라고 단정했다. **파이어볼(32001000)에서 거짓이다.**
  - 현재: `MotionRow=10`(Casting1, `IsAttackFamily=TRUE`) → 런지 전진 + 잔상. plan도 *"파이어볼조차 지금은 걸어가서 때린다"*고 실측했다.
  - D1 후: `StagingId=62000300`(`AdvanceRatio=0.0`, `IsCameraCut=FALSE`) → **제자리 시전 + 컷 없음**.
  - 즉 이동 거리·시간·카메라가 **셋 다 바뀐다.** 정령타(`MotionRow=11`, `IsAttackFamily=FALSE`)는 원래 제자리라 무변 — **"즉발과 투사의 캐스터 동작이 완전 동일"은 신규 스키마 안에서만 참이고 라이브 대비로는 거짓이다.**
- **실행 절차**: 배선 전/후로 파이어볼 1캐스트의 (a) 캐스터 이동 거리 (b) 캐스트 시작→ResolveHit 경과 시간 (c) 카메라 컷 발동 여부를 각각 로그 1줄로 기록해 비교.
- **판정 기준**: (a)가 배선 후 **0 uu**여야 PASS(설계 의도). (b)의 델타를 **수치로 기록**한다 — 0이 아닌 것이 정상이며, 이 값이 TC-G07(MA-1)의 유효 범위 밖임을 문서에 남긴다. (c)는 **PLAUSIBLE 미확인**: 현행 `IfThenElse_6 = OR(Target!="ENEMY1", dist<1.0)`의 True/False 핀 중 어느 쪽이 컷인지 미조회 → `BP_BattleSpawnPoint`의 `IfThenElse_6` 출력 핀 연결 대상을 조회하면 확정된다.
- **게이트**: D6 / **주체**: V + O(육안 최종) / **시점**: PIE
- **상태**: 대기

---

**[D6][TC-N09] 턴 길이 = f(HitCount, FxProjectileId) 실측** ★D1 §3-4 자기모순
- **문제**: D1 §3-4는 `HitIntervalSec`를 CSV 컬럼으로 두지 않는 이유를 *"스킬마다 다르면 턴 길이가 흔들려 SPD 원장에 노이즈"*라고 했다. 그런데 같은 문서가
  - `HitCount`(**CSV 컬럼**)에 `HitIntervalSec`를 곱해 턴을 늘리고(§3-4 상한식이 이를 명시),
  - §3-7에서 `투사지속 = min(vfx[FxProjectileId].FrameCount / FPS, budget)`으로 **`vfx.csv` 값이 턴 길이를 결정**하게 만든다.
  → **"턴 길이는 데이터가 흔들지 않는다"는 원칙이 두 곳에서 이미 깨져 있다.** 상수 하나를 지키려고 세운 논거가 자기 설계와 충돌한다.
- **실행 절차**: 기본공격(HitCount=1, Fx투사 0) 기준선 대비, (a) HitCount=3 (b) FxProjectileId>0(FrameCount 최대 파일) (c) 둘 다 — 3케이스의 캐스트→턴종료 경과 시간을 측정.
- **판정 기준**: 각 케이스의 델타를 **수치로 기록** ∧ 최대 델타 ≤ `SlotBudgetSec` ∧ 어떤 케이스도 **MA-1 재생 스크립트를 어긋나게 하지 않음**(자동 시나리오 완주). 델타가 `SlotBudgetSec` 초과면 FAIL → `MaxHitCount` 재산정.
- **게이트**: D6 / **주체**: V / **시점**: PIE
- **상태**: 대기

---

**[D5][TC-N10] `Def +3` 표현 가능성 — 가산 채널 부재** ★D1 §12 B11 실행 불가
- **문제**: plan·D1이 상시 패시브 상한을 **`Def ≤ +3`(가산)**으로 정했고 L23이 "Def 가산 ≤ +3"을 검증한다. 그런데 `status_effects.csv`의 `Channel` 실측 어휘는 **`SKIP_TURN` · `DOT` · `OUT_MULT` · `IN_MULT` 4종뿐**이고 **가산 채널이 없다**. `StatTarget=DEF, Channel=IN_MULT`는 배율이지 +3이 아니다.
  → D1 §7-3의 *"신규 코드 0 (검증된 `ApplyStatus` 재사용)"*은 **상시 Atk %에 대해서만 참**이고, **Def +3에는 거짓**이다(신규 Channel 값 = `switch` case 1개 = 판별기준 2의 "새 축" 영역).
- **판정 기준**: D5 산출 `status_effects.csv`에서 패시브 Def 상태 행이 (a) 기존 4채널 중 하나로 표현되고 (b) 그 값이 실제로 **+3 가산과 동등**함을 손계산으로 보이거나, (c) 신규 채널이 명시적으로 승인된 상태여야 PASS. 셋 다 아니면 FAIL.
- **게이트**: D5 / **주체**: V + balance / **시점**: NOW(문서 판정)
- **상태**: **FAIL — 어휘 확정 대기**

---

**[D4][TC-N11] `SkillIds` JOB 토큰 포지션 게이팅 린트 부재**
- **문제**: D1은 **패시브**에 대해서만 포지션 게이팅을 린트한다(L08). **`SkillIds`의 직업 토큰에는 대응 규칙이 없다.** A2 생성기는 §2-3에서 `Layer==JOB ∧ PositionTag ∈ {행 포지션, UNIVERSAL}`로 뽑지만, 손으로 만든 행이나 생성기 버그가 이를 위반해도 **린트가 통과시킨다**. 실제로 라이브 마법사 행은 `DEALER` + `SUPPORT` JOB 스킬을 동시에 들고 있다(TC-N05 표).
- **판정 기준**: 신규 린트 **L26** — `SkillIds[2]`의 `Layer==JOB` ∧ `PositionTag ∈ {행 PositionId, UNIVERSAL}`. 위반 샘플에서 FAIL 반환.
- **★평가 불가 문제**: `job_stats.csv`에는 **포지션 컬럼 자체가 없다**(`JobId`/`GradeId`만). L08도 같은 이유로 `job_stats`에서 **평가 불가**다 — 알파는 전 행 `PassiveId=0`이라 공허하게 참이지만, **하나라도 0이 아니게 되는 순간 L08은 조용히 침묵한다.** → **D1 §2-2의 "양 테이블 동일한 2컬럼 계약"은 부정확하다**: 계약은 `SkillIds` + `PassiveId` **+ 포지션 축**이며, 셋째가 `job_stats`에 없다.
- **게이트**: **D4(계약 정정)** → D5(린트) / **주체**: V / **시점**: NOW
- **상태**: **대기 — 계약 정정 필요**

---

**[D6][TC-N12] `ResolveApproachAnchor`의 ALLY1 경로**
- **문제**: D1 §4-2의 의사코드는 4분기 중 **2분기에서 `PrimaryTarget`을 전혀 쓰지 않는다**. `Target=ALLY1`이고 `AdvanceRatio > 0`이면 앵커가 `AttackPoint_<Caster.Team>`(적 방향 고정점)으로 해석되어 **"치유하러 적진으로 걸어간다"**가 된다. 알파는 서포트 프로필이 전부 `0.0`이라 발현되지 않지만 **L12는 어떤 프로필에도 `[0,1]`을 허용**한다.
- **실행 절차**: 임시 프로필(`AdvanceRatio=0.5`)을 치유(34001000)에 배정 → 1회 캐스트 → 이동 방향 벡터를 로그.
- **판정 기준**: 이동 벡터가 **아군(PrimaryTarget) 방향**이면 PASS. **적 AttackPoint 방향이면 FAIL** → 앵커 해석에 `Target` 분기 추가 또는 서포트 프로필 `AdvanceRatio` 상수 고정(0)을 린트로 강제.
- **게이트**: D6 / **주체**: V / **시점**: PIE
- **상태**: 대기

---

**[D4][TC-N13] F7b 단계 라벨 정합 — `⑩a` vs `⑩b`**
- **문제**: D1 §5-2가 구컬럼 5종 제거를 **"F7b ⑩b"** 라고 2회 적었고 §5-3도 *"⑩a~⑩b 사이 병존"*으로 썼다. F7b v2 실측은 **⑩a = `Effect*`/`PowerRate` 구컬럼 제거(비가역·스톱 체크리스트), ⑩b = 스캐폴드 처분**이다. 같은 D1의 §11 C1은 **"⑩a에 병합"** 으로 올바르게 적었다 — **문서 내부 충돌.** ⑩a는 유일 롤백 경로를 자르는 수술이라 라벨 오독의 대가가 크다. (plan L317도 같은 오기를 갖고 있다.)
- **판정 기준**: D1 §5-2·§5-3의 단계 라벨이 `⑩a`로 정정되거나, Director가 정정 지시를 남겼는가. 문서 grep으로 기계 판정.
- **게이트**: D4 / **주체**: V / **시점**: NOW
- **상태**: **FAIL — 정정 필요(D1 담당: system-ui)**

---

**[D4][TC-N14] 오너 수동 struct 세션 수 — 1회로 병합 가능한가** ★Critical X2
- **문제**: N3 실측으로 struct 편집이 **오너 수동 전용**임이 확정됐고, plan §구현순서 4가 *"struct 편집은 ⑩a에서 1회로 병합"*을 명령한다. 그런데 D1의 편집 지점표(C1~C12)로는 **1회가 불가능하다.**

| 항목 | D1 표기 | N3 실측 대조 | 판정 |
|---|---|---|---|
| `Category`·`HitCount`·`PrimaryTargetGroup` | §5-2 "존치" | struct에 **실재**(`category`·`hitCount`·`primaryTargetGroup`) | ✅ struct 편집 0 — **CSV 컬럼만 추가**. C1 작업량 축소 확정 |
| **`Layer`·`PositionTag`** | §5-2 **"존치"**(30원자 초안 출처) | struct에 **없다** | ❌ **오분류.** `Layer`는 §2-4 런타임 거절 가드가 DT에서 읽는 필드 → **struct 필수인데 C1의 "신규 6컬럼"에서 빠졌다** |
| `StagingId`·`Fx*3`·`Sfx*2` | 신규 6 | struct에 없음 | ✅ 신규 |
| `DevStatus` | §5-3 스키마에 포함 | struct에 없음 | ⚠ struct 필요 여부 미정(TC-N04) |
| 구 FX/SFX 5(string) | 폐기 | `castFX`·`projectileFX`·`impactFX`·`castSFX`·`impactSFX` **실재, 전부 string** | ✅ 제거 대상 확정 |
| `PowerRate`·`Effect*4` | ⑩a 제거 | 실재 | ✅ — 단 **⑩a 스톱 체크리스트 GREEN이 선행**이라 **시점을 강제**한다 |
| **`status_effects` `AuraFxId`** | §6-5 델타 | `F_StatusEffectsRow` 편집 필요 | ❌ **C1~C12 어디에도 없다** — 누락된 오너 세션 1개 |

- **귀결**: D1대로 진행하면 오너가 `F_SkillsRow`를 **최소 2회**(D6 배선용 신규 필드 → 이후 ⑩a 구컬럼 제거) 열게 되고, `Layer` 누락이 발견되면 **3회**가 된다. 함정㉓(모달 = MCP 브릿지 마비) 노출이 그만큼 늘어난다.
- **판정 기준(D4 게이트 항목)**: 아래 4개가 **한 문서에 확정**되면 PASS.
  1. `F_SkillsRow` **최종 필드 목록**(추가 목록에 `Layer` 포함 여부, `PositionTag`·`DevStatus`의 struct/CSV-only 귀속)
  2. `Kind`·`Target`·`Category`의 타입이 **FString인지 enum인지** — enum이면 `PASSIVE`·`NONE` 어휘 추가가 **또 하나의 수동 편집**이다. **미확인 — `ObjectTools.list_properties`의 타입 필드 또는 `F_SkillsRow` 상세 조회로 즉시 확정 가능**
  3. 편집 시점 = **⑩a 1회**로 못 박고, 그 결과 **D6의 struct 의존 작업(C1·C4·C5)이 "⑦ 이후"가 아니라 "⑩a 이후"**임을 plan D6 행과 D1 §11 전제문에 반영
  4. 오너 수동 세션 전량 목록: `F_SkillsRow` · `F_JobStatsRow` · `F_MotionsRow` · **`F_StatusEffectsRow`** + 신규 3종(`F_StagingsRow`·`F_VfxRow`·`F_SfxRow`) = **7건**(D1은 3건만 셈)
- **게이트**: **D4** / **주체**: Director + V(대조) / **시점**: NOW
- **상태**: **FAIL — D4 결정 대기**

---

## 4. D1 적대 검증 — 심각도순

> 형식: `[심각도] 위치 — 문제 — 재현/근거 — 확신도`

### Critical (D4 게이트를 막는다)

**[Critical] X1 — D1 §10 L09 × `data/job_stats.csv`** — 린트 L09가 라이브 6/6행을 FAIL시킨다.
재현: `SkillIds` 3토큰을 30원자 초안 `Layer`로 매핑 → 전사 `[BASIC,COMMON,COMMON]`, 마법사 `[BASIC,JOB,JOB]`, 기대 `[BASIC,COMMON,JOB]`. 해소책(라이브 `SkillIds` 개편)이 **F7b ⑦ 5스킬 회귀에서 막기·치유를 라이브 메뉴 밖으로 밀어낸다.** → **CONFIRMED**(스크립트 실측). TC-N05.

**[Critical] X2 — D1 §5-2 "`Layer`·`PositionTag` 존치" × 라이브 `F_SkillsRow`** — `Layer`는 struct에 없는데 "존치"로 분류돼 C1의 신규 필드 목록에서 빠졌다. `Layer`는 §2-4 거절 가드가 **DT에서 읽는 런타임 필드**다. → 오너 struct 편집이 1회로 안 끝난다(plan §구현순서 4 위반). → **CONFIRMED**(Director MCP 실측 23필드 대조). TC-N14.

**[Critical] X3 — D1 §5-3 스키마 × DT 임포터** — CSV 컬럼 집합과 struct 필드 집합의 분리가 정의돼 있지 않다. `PositionTag`·`DevStatus`는 런타임 소비처가 문서 어디에도 없고, struct에 없으면 임포터가 **경고 후 드롭**한다. 어느 쪽인지 정하지 않으면 무음 손실 또는 불필요한 필드가 영구 고착. → **CONFIRMED**(§5-3에 귀속 표기 부재). TC-N04.

**[Critical] X4 — D1 §9-4 "신규 문자열 키 전량 27종" × `data/strings.csv`** — 병합 42행이 참조하는 키 중 **54종이 미등록**이고 어느 단계에도 배정돼 있지 않다(§12 B1~B14에 없음). L17이 D6 임포트를 막는다. 실제 신규 총량은 **81종**(UI 7 + 액티브 50 + 프루브 4 + 패시브 20). → **CONFIRMED**(스크립트 실측 54건 열거). TC-N06.

### High

**[High] Y1 — D1 §4-2 "라이브 5스킬 이동 거리·시간 무변"** — 파이어볼에서 거짓. `MotionRow=10`은 `IsAttackFamily=TRUE`라 현재 전진하는데 `62000300`(`AdvanceRatio=0.0`)은 제자리다. 카메라 컷도 TRUE→FALSE로 바뀔 가능성이 높다. → **CONFIRMED**(이동), **PLAUSIBLE**(카메라 — `IfThenElse_6` 출력 핀 대상 미조회). TC-N08.

**[High] Y2 — D1 §3-1 "투사체 = 신규 컬럼 0(기존 컬럼 부활)"** — `projectileFX`는 **string**이고 D1 §5-2가 그것을 **폐기**한다. `FxProjectileId`(int)는 **신규 struct 필드**다. 즉 "부활"이 아니라 **타입 변경 + 개명 + 신규 추가**다. 같은 표의 `HitCount`는 진짜 부활(struct 필드 실재, CSV 컬럼만 추가)이라 **두 축이 같은 라벨을 쓰는데 비용이 다르다.** → **CONFIRMED**. ★단 **"프로필 4행으로 충분"이라는 행 수 주장 자체는 유효**(TC-S01 30/30 PASS) — 반증된 것은 비용 주장이다.

**[High] Y3 — D1 §10 L19** — `FrameCount <= GridX × GridY`는 너무 느슨하다. `FrameCount=20, GridX=13, GridY=9`가 통과하는데, 그 값은 **다음 색상 행으로 UV가 넘어간다**(한 행 = 한 애니메이션). 올바른 규칙은 `FrameCount <= GridX`(실측 180/180이 `FrameCount == GridX`). 또한 **`ColorRow`에 대한 린트가 L01~L24에 하나도 없다** — 판정 D를 강제할 규칙이 존재하지 않는다. → **CONFIRMED**. TC-N02·TC-G01.

**[High] Y4 — D1 §5-4 step 7 "§10 린트 전량 PASS"** — 병합 시점에 액티브 25행은 `StagingId=0`·`Category=""`·`HitCount=0`이라 L03이 반드시 FAIL한다. 같은 문서 step 5가 *"D5가 채운다"*고 인정한다. 검증란이 자기 절차와 모순. → **CONFIRMED**. §1-3 린트 3계층으로 해소 제안.

**[High] Y5 — D1 §12 B11 × `data/status_effects.csv`** — `Def ≤ +3`(가산)을 표현할 채널이 없다(`SKIP_TURN`/`DOT`/`OUT_MULT`/`IN_MULT` 4종 전부 배율·특수). §7-3의 "신규 코드 0"은 Atk % 계열에만 참. L23의 "Def 가산 ≤ +3" 검증 대상이 데이터에 존재할 수 없다. → **CONFIRMED**. TC-N10.

**[High] Y6 — D1 §6-5 × §11 C1~C12** — `motions.csv`의 `FxMotionId` **이관 매핑값이 미지정**이다. 0 초기화 시 RowIndex 5·6·7·8·9·10의 잔상이 전부 소실(무음 회귀). plan이 *"오너 육안 방어선이 2겹→1겹"*이라 경고한 바로 그 지점. → **CONFIRMED**. TC-N07.

**[High] Y7 — D1 §2-4 × F7b ⑧ (판정 A가 만든 새 구멍)** — 거절 가드가 참조하는 **`bDebugProbesEnabled`가 §11 BP 신규 멤버 목록에 없다**(목록 = `PassiveReentryDepth` 1개). 더 중요한 건 순서: F7b 실행 순서는 `⑦ → ⑧ → ⑨ → ⑩a`이고 ⑧이 **프루브 2행 E2E 게이트**인데, D6의 병합 임포트가 그 2행을 `Layer=PROBE`로 강등하고 선택을 차단하면 **⑧ 게이트가 실행 불가**해질 수 있다. → **CONFIRMED**(멤버 누락) / **PLAUSIBLE**(⑧의 프루브 호출 경로 미조회 — `F7b_인터프리터_진행상황_인계.md`의 ⑧ 실행 절차를 조회하면 확정). TC-N01.

### Medium

**[Medium] Y8 — D1 §2-2 "양 테이블 동일한 2컬럼 계약"** — 계약의 술어가 셋째 축(포지션)에 의존하는데 `job_stats.csv`에 포지션 컬럼이 없다. L08은 `job_stats`에서 **평가 불가**이고, 알파는 전 행 0이라 공허하게 참이다. 그리고 `SkillIds`의 JOB 토큰에는 **게이팅 린트가 아예 없다**(패시브만 L08로 강제 — 비대칭). → **CONFIRMED**. TC-N11.

**[Medium] Y9 — D1 §3-4 자기모순** — `HitIntervalSec`를 데이터화하지 않는 논거(*"턴 길이가 흔들려 SPD 원장에 노이즈"*)가 같은 문서의 `HitCount`(CSV 컬럼) × `FxProjectileId`(vfx 데이터 유래 지속) 설계와 충돌한다. 턴 길이는 **이미 데이터의 함수**다. → **CONFIRMED**. TC-N09.

**[Medium] Y10 — D1 §5-2·§5-3 단계 라벨 오기** — `⑩b`로 표기했으나 F7b v2는 `⑩a`가 구컬럼 제거(비가역·스톱). §11 C1은 `⑩a`로 올바름 → 문서 내부 충돌. plan L317도 동일 오기. → **CONFIRMED**. TC-N13.

**[Medium] Y11 — D1 §11 C1~C12 세션 누락** — `F_StatusEffectsRow`(`AuraFxId` 추가, §6-5 델타)의 편집 항목이 없다. 오너 수동 세션 실계는 **7건**(D1은 C1~C3에서 3건). → **CONFIRMED**. TC-N14.

**[Medium] Y12 — D1 §4-2 `ResolveApproachAnchor`** — 4분기 중 2분기가 `PrimaryTarget`을 무시한다. `ALLY1` + `AdvanceRatio>0` 조합이 적 방향으로 이동한다. L12가 그 조합을 막지 않는다. 알파 미발현. → **CONFIRMED**(논리) / 발현은 D5 값 배정에 의존. TC-N12.

### Low

**[Low] Y13 — 판정 D의 근거 정밀도** — `row8`의 "예외 12건"은 4계열 버킷 경계(250°)의 분류 아티팩트다. row8 실측은 **180/180이 223.6~279.2° 단일 연속 대역**(circular span 55.6°)으로, 색조 점프가 없다. row6(19건, 30.8~32.9° vs 269~301°)·row7(6건, 35.8~37.1° vs 262~333°)은 **진짜 이봉 분포**로 판정 D의 근거가 확실하다. 예약은 안전측이므로 **판정에 이의 없음**, 근거만 정정 기록. → **CONFIRMED**(독립 재집계).

**[Low] Y14 — 알파 팔레트 폭** — row0~5 6행 중 **row0과 row4가 둘 다 적-주황**이라 색 계열은 **5종**이다. 또 row0은 파일 간 색조 폭이 **66.1°**(347°~55°, 빨강~노랑), row3은 **63.9°**다. "row0 = 화염"식 팀 규약은 파일에 따라 노란 화염을 낳는다. D5 색 배정 시 인지 필요. → **CONFIRMED**.

### 반증 실패 — 검토했으나 통과시킨 축 (빈손 방지)

| 축 | 검토 방법 | 결과 |
|---|---|---|
| **③ 프로필 4행 커버리지** | §3-2 "담당 원자" ↔ 30원자 초안 Id 전수 배정 집계 | **PASS** — 12+3+8+7 = **30/30**, 미배정 0, 중복 0. 프로필 4 ≤ 상한 8 |
| **③ 투사 7원자의 캐스터 동작 동일성** | 7원자 실값 대조 | **PASS** — 마력탄·원소구·제압사격·화염징표·파이어볼·연속마격·봉인 **전부 `Kind=MAG`·`MotionRow=10`·`Target=ENEMY1`**. 스테이징 5컬럼이 동일하므로 **1행으로 충분, 행 부족 없음**. (즉발 정령타는 `MotionRow=11`로 다르지만 `MotionRow`는 `skills.csv` 소유 축이라 프로필 분화 사유가 아니다 → **D1 논증 성립**) |
| **⑤ Id 조인 성립성** | `skills_v2_draft` ∩ `30원자 초안` | **PASS** — LIVE 5개(31000000·31001000·32001000·33001000·34001000) **완전 일치**. 조인 미커버 25행은 D5 소관으로 D1이 명시. 프루브 2행은 조인 대상 아님(step 3에서 별도 추가) — **절차 정합** |
| **⑤ 42행 산술 · Id 유일성** | 30 + 프루브 2 + 패시브 10 | **PASS** — 42, Id 충돌 0(31002000/31003000이 30원자 Id군과 미충돌, 35000000~35009000 신규 대역) |
| **① `PassiveId` 어휘** | `0` vs 공란 vs `-1` 논거 | **PASS** — `-1`이 `MotionRow` 중립 sentinel로 선점됐고 `0`이 `StagingId=0`·`FxCastId=0`과 어휘 일치. 공란 금지 논거(DT int 공란→0 무음 캐스팅)는 정확 |
| **② `position_stats` 슬롯 미보유 논거** | `data/drafts/position_stats_초안.csv` 실측 | **PASS** — 실제로 **3행**(tank/dealer/support)이고 스탯 9컬럼뿐. 포지션이 슬롯을 소유하면 1000캐릭터가 3종류로 붕괴한다는 논거는 **데이터로 확인됨** |
| **판정 A 3중 차단의 논리 완결성** | 풀 필터 · L10 · 유일성 키 · 런타임 가드 | **PASS**(논리) — PROBE는 4개 풀 어디에도 안 들어가므로 유일성 키에 등장 불가. 구멍은 차단 자체가 아니라 `bDebugProbesEnabled` 미정의(Y7) |
| **판정 C(STUN)의 정합성** | §7-2 훅 표 · E7 · §7-3 | **PASS** — `ON_BATTLE_START`는 `StartBattle` 말미(STUN 발생 이전)라 STUN의 영향을 받을 수 없고, 영구 상태는 `dur==-1`이라 `TickStatusesAtTurnEnd`가 만료시키지 않는다. **두 훅의 거동이 갈리는 지점이 구조적으로 성립.** 단 실증은 TC-N03 필요 |
| **판정 E(합성 0)** | `vfx_inventory_raw.csv` 180행 독립 재집계 | **PASS** — GridY 전량 9 · `FrameCount != GridX` 0건 · 무채색 행 `5` 180/180 · 빈 행 0건. D4.5a 수치 전량 재현 |
| **`AdvanceRatio` (a)안 채택 논거** | (b)안 기각 3사유 | **PASS** — float 타입과 `{0,1}` 계약의 영구 불일치, 근접 정지 거리의 데이터 자리 상실, 도약 착지점 표현 불가. 3사유 모두 성립 |
| **PN-1~5 불변식의 검증 가능성** | 5항목 각각의 관측 수단 | **PASS** — 5개 모두 로그/카운터/스냅샷으로 기계 판정 가능(TC-P01로 확정) |
| **RG-1~7 거절 불변식** | 7항목 | **PASS** — 7개 모두 기계 판정 가능. 특히 RG-2가 F7a ⑤형 소프트락을 직접 겨냥 |
| **판정 B(오디오 도메인 없음)** | [[네이밍_폴더_규약]] §1-C `SFX_` 예약 + §2 "애매하면 생략" | **PASS** — 도메인 추가 없이 `SFX_Impact_Fire` 성립. 신규 TC 불필요 |

---

## 5. 이월 판정

### N3 — **닫힘**(Director MCP 실측, 2026-08-11)

`F_SkillsRow` 라이브 23필드 확정. gameplay("8필드 존재")와 D1("CSV 15컬럼")은 **둘 다 맞았다** — struct와 CSV라는 다른 대상을 봤을 뿐 불일치가 아니었다.
**단, 이 실측이 Critical 2건을 새로 드러냈다**: X2(`Layer`가 struct에 없는데 "존치"로 오분류) · X3(CSV/struct 집합 분리 미정의). **N3는 닫혔지만 그 자리에 더 무거운 미결이 생겼다** — TC-N04·TC-N14.

D6 작업량 갱신:
- **축소**: `Category`·`HitCount`·`PrimaryTargetGroup` = **struct 편집 0, CSV 컬럼 추가만**. C1의 해당 3필드 작업이 "추가"→"값 채우기"로 확정.
- **확대**: 오너 수동 struct 세션이 **3건(D1 표기) → 7건(실계)**. 그중 `F_SkillsRow` 1회 병합 가능 여부가 미확정(X2).

### N6 — **D6까지 이월 가능** (D4 게이트를 막지 않는다)

| 항목 | 판정 |
|---|---|
| **결론** | **이월 승인 권고.** D4 게이트 통과를 막지 않는다 |
| **사유** | N6은 **값(Id 대분류 숫자) 결정**이지 **스키마 결정이 아니다.** D1의 어느 구조도 `35`에 의존하지 않는다 — 패시브 식별은 `Layer==PASSIVE`가 하고(L01·L02·L07b·L22 전부 `Layer` 기반), Id는 PK로서 유일하기만 하면 된다. `35xxxxxx`가 `36xxxxxx`로 바뀌어도 **린트 24규칙 중 0개가 영향받고, struct 필드 0개가 바뀐다** |
| **영향 범위** | `skills.csv` 패시브 10행의 `Id` 셀 10개 + D1 §9-4 표 + `strings.csv` 키(키는 `Skill.<Name>` 형식이라 Id 무관) |
| **검증 예정 단계** | **D5 착수 직전**(balance가 패시브 10행을 실제로 쓰기 시작하는 시점). 그 전까지는 `35xxxxxx`를 **잠정값**으로 표기하고 문서에 `[N6 미확정]` 태그 유지 |
| **닫는 방법** | [[데이터_서버_규약]] §4 "대분류 자릿수 배분은 오너 확정" → **오너 한 줄 결재**. Director가 대행 판정해도 무방(기술적 제약 없음) |
| **★단, 함께 물어야 할 것** | `Kind` 어휘에 `PASSIVE`를 추가하는 것이 **enum 편집인지 문자열인지**(TC-N14 판정 기준 2). enum이면 **오너 수동 세션이 1건 더 늘고, 그건 D4에서 알아야 한다.** N6 자체는 이월이지만 이 부수 질문은 **D4에서 답해야 한다** |

---

## 6. TC 인덱스 (전량 · 상태)

| ID | 요약 | 게이트 | 주체 | 상태 |
|---|---|---|---|---|
| TC-P01 | `PassiveId=0` PN-1~5 불변식 | D6 | V | 대기 |
| TC-P02 | 메뉴 무오염 3중 차단 + RG-1~7 | D6 | V | 대기 |
| TC-P03 | 훅 2종 거동 + 순회 결정론 | D6 | V | 대기 |
| TC-P04 | 무음 폐기 0 + Timing 화이트리스트 | D6 | V | 대기 |
| TC-P05 | 재귀 가드 depth ≤ 1 + 다음 턴 복구 | D6 | V | 대기 |
| TC-S01 | 극단 8표본 + 30원자 커버리지 | D4 | V | **PASS** |
| TC-S02 | `AdvanceRatio` 0.9 → 90% 이동 | D6 | V | 대기 |
| TC-S03 | 거리가드 AND — 없던 경로 0 | D6 | V | 대기 |
| TC-S04 | N6 회귀 — Override Clear 지배 | D6 | V | 대기 |
| TC-S05 | 레고 검증 — 편집 지점 수 | D4→D6 | V | 대기 |
| TC-G01 | 린트 24규칙 실행 가능성(위반 샘플) | D4.5~D6 | V | 대기 |
| TC-G02 | 패시브 상한 기계 검증 | D5 | V | 대기(선결) |
| TC-G03 | 스칼라 4종 동시 세트 | D6 | V | 대기 |
| TC-G04 | fail-loud — 다른 FX 폴백 0 | D6 | V | 대기 |
| TC-G05 | 파트3 회귀 — 치유 잔상 0 | D6 | V+O | 대기 |
| TC-G06 | 멀티히트 판정 1 / 연출 N | D6 | V | 대기 |
| TC-G07 | MA-1 재생 원장 diff 0 (범위 한정) | D6 | V | 대기 |
| TC-N01 | `Layer=PROBE` 3중 배제 | D4→D6 | V | 대기(선결) |
| TC-N02 | `ColorRow` 린트 L25 (0~5만 통과) | D4.5 | V | 대기 |
| TC-N03 | `vfx.csv` ↔ 재고 CSV 조인 대조 | D4.5 | V | 대기(선결) |
| TC-N04 | CSV 컬럼 ↔ struct 필드 집합 일치 | **D4** | V | 대기(선결) |
| TC-N05 | L09 라이브 6행 | **D4** | V | **FAIL** |
| TC-N06 | L17 문자열 키 54종 미등록 | **D4** | V | **FAIL** |
| TC-N07 | `FxMotionId` 이관 매핑 6행 | D4.5→D6 | V | 대기 |
| TC-N08 | 파이어볼 거동 변경 실증 | D6 | V+O | 대기 |
| TC-N09 | 턴 길이 = f(HitCount, 투사) | D6 | V | 대기 |
| TC-N10 | `Def +3` 가산 채널 부재 | D5 | V | **FAIL** |
| TC-N11 | JOB 토큰 게이팅 린트 부재(L26) | **D4** | V | 대기 |
| TC-N12 | `ResolveApproachAnchor` ALLY1 경로 | D6 | V | 대기 |
| TC-N13 | F7b `⑩a`/`⑩b` 라벨 오기 | **D4** | V | **FAIL** |
| TC-N14 | 오너 struct 세션 1회 병합 가능성 | **D4** | Director+V | **FAIL** |

**집계**: 31건 = 시드 17 + 신규 14. PASS 1 · FAIL 5 · 대기 25.
**D4 게이트 항목**: TC-S01(PASS) · TC-S05 · TC-N04 · TC-N05 · TC-N06 · TC-N11 · TC-N13 · TC-N14 — **8건 중 1건 PASS, 4건 FAIL, 3건 결정 대기.**

---

## 7. 미확인 (추정 금지 — 조회 방법 명시)

| # | 항목 | 조회 방법 |
|---|---|---|
| M1 | `Kind`·`Target`·`Category`의 타입(FString vs enum) | `ObjectTools.list_properties(/Game/Data/F_SkillsRow)` 타입 필드. enum이면 어휘 추가 = 오너 수동 세션 +1 |
| M2 | `IfThenElse_6`의 True/False 핀 중 어느 쪽이 카메라 컷인가 | `BP_BattleSpawnPoint` 해당 노드 출력 핀 연결 대상. TC-N08 (c) 판정에 필요 |
| M3 | F7b ⑧이 프루브 2행을 **어떤 경로로 호출**하는가(메뉴 클릭인지 강제 호출인지) | `F7b_인터프리터_진행상황_인계.md` ⑧ 절차 + ⑧ TC 골격(T02·C03). `Layer=PROBE` 차단이 ⑧을 막는지 확정 |
| M4 | 유닛의 `SkillIds` 캐시 여부 / `job_stats` 행 조회 키 (D1 N1·U-1·U-2) | `BP_BattleUnit` 멤버 + `WBP_SkillMenu:SetActiveUnit` 그래프. **D1의 "미확인" 표기는 정직하다** — MCP 없이는 알 수 없다 |
| M5 | `ResolveHit` 재진입 가드 유무 (D1 N2) | `ResolveHit` 그래프. **정직한 미확인** |
| M6 | UE DT 임포터가 struct 미보유 컬럼을 경고로 처리하는지 오류로 처리하는지 | 실제 임포트 1회 + Output Log. TC-N04 판정 기준의 엄격도를 결정 |

> **D1의 N1~N7 정직성 판정**: N1·N2(BP 그래프)·N4(스프라이트 실측)·N5(런타임 실측)는 **파일 조회로 알 수 없는 것이 맞다 — 정직한 미확인.** N7(행 Id 계열)은 D4.5 소관으로 정당. **N3만 "확인 가능한데 안 한 것"이었고**(struct 조회 1회로 닫혔다), Director가 닫았다. **N6은 오너 결재 사항이 맞다.** → 전체적으로 미확인 표기는 정직하며, 유일한 태만은 N3 1건이었다.

---

## 관련

[[plan]] · [[D1_4슬롯구조_확정]] · [[D4.5a_VFX재고_실측]] · [[F7b_재개계획_초안]] · [[SPD원장_오라클_v1]] · [[balance1_포지션킷_v1]] · [[F7_스킬아키텍처_확정]] · [[네이밍_폴더_규약]] · [[데이터_서버_규약]] · [[UI_화면_규약]] · [[개발_워크플로우]]
