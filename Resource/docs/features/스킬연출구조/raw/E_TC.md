---
type: test
project: projectTP
feature: 스킬연출구조
updated: 2026-08-11
status: WIP
status_note: TC 확정 — E-S0 진행 중
---

# E단계 — 단계별 TC + plan 적대 검증

> 상위: [[E_스파이크_plan]] · 판정: [[E0_에이전트피드백_Director판정]] · 계승: [[D3_TC_확정]]
> 작성: qa-critic (적대 검토). **MCP 0회 · 파일 수정 0건 · 커밋 0건** — 문서·CSV·애셋 파일시스템 조회로만 작성.
> 계승 관계: D3 TC 31건(`TC-P01`~`TC-N14`) + [[BP정리_통합명세_2026-08-11]] BPC-01~32를 **중복 생성하지 않고 선실행·확장**한다. 계승 항목은 원 ID를 병기했다.
#projectTP/스킬연출구조

---

## 0. Director 3줄

1. **plan을 막는 Critical 4건.** ① **밀도 해법이 적용 대상과 정확히 어긋난다** — "액션 캠 컷에서만 크게"인데 신규 VFX 3장을 쓰는 스킬은 **7행 중 전부 `IsCameraCut=FALSE`**이고 컷이 켜진 스킬은 전부 legacy VFX다(적용 스킬 **0개**). ② **관통 판정값 `62000300`이 "빈 struct 기본값"과 구별되지 않는다** — R7 완화책 자체가 R7에 걸린다. ③ **S4b의 DT_Skills 갱신 경로 미지정** — `data/skills.csv`(15컬럼/5행) 재임포트 시 프루브 2행 + 8필드 소실, F7b ⑧ 게이트 파괴. ④ **S2' 텍스처 임포트 설정 판정 기준 부재** — Filter가 legacy와 다르면 밀도·블룸 판정 전량 무효.
2. **TC 51건 설계**(S0~S6). NOW 선실행 **3건 PASS**(CSV 무결성 · `SrcFile` 조인 3/3 · `FrameCount==GridX`).
3. **plan의 미확인 5건은 정직하나 목록이 불완전하다** — 최소 4건이 빠져 있다. 그리고 **struct 자동화 전례**(TAPython `PythonStructLib`가 2026-07-16에 `F_SkillsRow` 15→23필드를 스크립트로 확장)가 검토되지 않아 S4a 오너 게이트가 필요 이상으로 강하게 잡혀 있을 수 있다 → Director가 **S4a-0(조사) 신설**로 답했다(§7).

### ★건수 정정 (자기 신고)

qa-critic 원 답변은 **"TC 47건"**이라 적었다. 본 문서로 옮기며 표를 전수 재집계한 결과 **실제 51건**이다(S0:3 · S1:5 · S2:9 · S2':5 · S3:8 · S4a:4 · S4b:5 · S5:8 · S6:4 = **51**). 47은 S5까지의 누계를 총계로 착각한 집계 오류다.

> **파일명 개명 완료(Director 판정 2026-08-11)**: `E_TC_47건.md` → **`E_TC.md`**. 외부 참조 0건을 확인하고 옮겼다. ★**파일명에 건수를 넣지 않는다** — TC가 늘 때마다 파일명이 스테일이 되고, 그 스테일은 개명해야만 고쳐지므로 잘 안 고쳐진다. **본문·인덱스의 정본 수치는 51건.**

---

## 1. 표기 규약 (D3 §1-2 계승)

```
[단계][ID] 조건 → 기대결과 | 상태(대기/PASS/FAIL/이월)
```

| 필드 | 값 |
|---|---|
| **실행 주체** | `V` = verifier 기계 / `O` = 오너 육안 / `V+O` = 기계 선행 후 육안 |
| **판정** | PASS/FAIL이 **기계로 갈리는 형태**만 허용. "잘 보인다" 금지 |
| **상태** | 초기값 전부 `대기`. 선실행분만 `PASS` |
| **★** | plan이 근거로 삼은 축 또는 본 검토가 새로 연 축 |

---

## 2. 단계별 TC (S0~S6) — 51건

### 2-0. S0 — 노드 가용성 프로브

| ID | 선행조건 | 절차 | 판정(기계) | 주체 | 실패 시 의심 | 상태 |
|---|---|---|---|---|---|---|
| **E-S0-01** | ★**Director 판정 4로 15종 확정**(§7). 원 12종 = `SetScalarParameterValue`/`SetTextureParameterValue`/`CreateDynamicMaterialInstance`/`GetDataTableRowNames`/`GetDataTableRow(F_VfxRow)`/`GetDataTableRow(F_StagingsRow)`/`LoadAsset(Texture2D by path)`/`SetViewTargetWithBlend`/`SetGlobalTimeDilation`/`RetriggerableDelay`/`SpawnActor(class)`/`InputKey` **+ gameplay 3종** = `AlignSpriteToCamYaw` 호출 · `PlayHurtReaction` 호출 · 외부 raw 변수 Get | 폐기용 BP에 15종 생성 → 컴파일 → `find_nodes` 카운트 | 생성 성공 **15/15** ∧ 컴파일 에러 **0** ∧ 프로브 애셋 삭제 후 `find_assets` 결과 **0건** | V | MCP `BlueprintTools`의 노드 생성 커버리지 부족. 특히 `LoadAsset`(soft path 런타임 로드)과 `SetViewTargetWithBlend` | 대기 |
| **E-S0-02** | E-S0-01 | 프로브 BP에서 `LoadAsset("/Game/VFX/T_FX_CastAtk")` 호출(대상 미임포트 상태) | 반환 **None** ∧ 크래시 0 ∧ 로그에 소프트로드 실패 흔적 존재 | V | **fail-loud 전체가 이 노드 하나에 걸려 있다.** 여기서 예외가 나면 S2의 `TEXMISS` 설계 전면 재검토 | 대기 |
| **E-S0-03** ★ | E-S0-01 | `BP_BattleSpawnPoint` 인스턴스에 대해 (a) **raw 변수 Get** (b) `AlignSpriteToCamYaw` 호출 (c) `PlayHurtReaction` 호출 노드를 **생성만** | 3종 전부 노드 생성 가능 ∧ 컴파일 0. **(b) 불가 → R3(빌보드 스냅) 완화책 무효 / (c) 불가 → S3 FLINCH 축 검증 불가** | V | plan이 "유닛은 외부 Get만"이라 했으나 R3·FLINCH는 **함수/이벤트 호출**을 요구한다. 이 모순이 여기서 갈린다 | 대기 |

> ★**gameplay S0 실측 반영 ①** — `BP_BattleSpawnPoint`에 **`Sprite`라는 단일 컴포넌트가 없다.** 실제는 **`SpriteRight` + `SpriteLeft` 2개를 `FaceLeft` bool로 스위칭**하는 구조다. 따라서
> - **E-S0-03 (a)**의 Get 대상은 `Sprite`가 아니라 **`SpriteRight`·`SpriteLeft`·`FaceLeft` 3종**이다.
> - **E-S0-03 (b)** `AlignSpriteToCamYaw`가 **두 컴포넌트를 모두 정렬하는지, 활성 1개만 정렬하는지**를 함께 확인해야 한다 — 컷 진입 시 비활성 스프라이트가 미정렬로 남으면 R3(종이 효과)가 방향 전환 순간에 재발한다.
> - 변위 관측 대상은 §2-3 E-S3-03에서 **액터 루트**로 확정한다.

---

### 2-1. S1 — 레벨 복제 + 3액터 삭제

| ID | 선행조건 | 절차 | 판정(기계) | 주체 | 실패 시 의심 | 상태 |
|---|---|---|---|---|---|---|
| **E-S1-01** | 복제 전 `Content/` 전체 SHA256 manifest 캡처 | 복제 → 3액터 삭제 → 저장 → manifest 재캡처 | `Stages/map_battle_octopath.umap` **SHA256·크기·mtime 전부 불변**(기준: `3848264 B`, `Jul 17 13:05`) | V | 에디터 Save All이 원본까지 저장. **OFPA 아님을 확인했으므로**(`__ExternalActors__`에 `test`만 존재) `.umap` 단일 파일 체크가 유효 | 대기 |
| **E-S1-02** ★ | E-S1-01 manifest | manifest diff 전량 | **변경 파일 집합 == `{map_battle_fxlab.umap}` 정확히 1개.** 특히 `Materials/M_Sprite_Flipbook_Lit` · `VFX/MI_FX_Smear` · `VFX/MI_FX_Hit` · `Blueprints/BP_BattleSpawnPoint` · `Data/DT_Motions` **불변** | V | ★**plan의 "라이브 무손상" 정의가 `.umap`+전투수치뿐이라 공유 애셋 변경을 못 잡는다.** `EmissiveBoost` 노브(3.0→1.0)가 정확히 이 표면 — MI 자산을 고치면 라이브도 같이 바뀌고 dmg는 안 변한다 | 대기 |
| **E-S1-03** ★ | fxlab 레벨 | PIE 30초, 입력 없음 | ★**gameplay 실측 반영으로 판정 강화**(아래 주석): 에러 0 **∧** 유닛 8기의 **초기화 결과값이 라이브와 동일**(스탯 로드 여부 / `Hp`·`Atk`·`Def` 실값 8기 전수 대조). **"에러 0" 단독은 가짜 GREEN** | V | `IsValid(ManagerRef)` 가드의 Not Valid 분기가 미배선이라 **에러가 안 뜨고 초기화가 조용히 스킵된다** | 대기 |
| **E-S1-04** | 〃 | `GetAllActorsOfClass` 카운트 | `BP_BattleManager` **0** ∧ `BP_AttackButton` **0** ∧ `BP_CamToggleButton` **0** ∧ `BP_BattleSpawnPoint` **8** ∧ `BP_AttackPoint` **2 이상**(라이브와 동수) | V | 삭제 과다/과소. `BP_AttackPoint`는 `AdvanceRatio` 앵커 계산에 필요하므로 **삭제 금지 대상**인데 plan 삭제 목록에 명시가 없다 | 대기 |
| **E-S1-05** | 라이브 레벨 | `map_battle_octopath` PIE → 1턴 기본공격 | dmg **30** ∧ `BattleLog\|` 행수 = 기준선 ∧ 컴파일 에러 0 (BPC-31 부분 선실행) | V | 복제 과정에서 BP 클래스 디폴트가 변경됨 | 대기 |

> ★**gameplay S0 실측 반영 ②** — **`IsValid(ManagerRef)` 가드의 "Not Valid" 분기가 미배선**임이 확인됐다. 이로써 본 검토의 **E-M2가 PLAUSIBLE → CONFIRMED로 승격**되고, 동시에 **원래 예상과 반대 방향**임이 드러났다:
> - 원 예상: 매니저 삭제 → `Accessed None` 에러 다발 → "런타임에러 0"이 **자동 FAIL**
> - 실측 귀결: Not Valid 분기가 비어 있어 **에러가 뜨지 않고 초기화만 조용히 스킵된다** → "런타임에러 0"이 **자동 PASS = 가짜 GREEN**
> → 완화책이 "화이트리스트"에서 **"초기화 결과값 직접 관측"**으로 바뀐다. E-S1-03 판정을 위와 같이 교체했다.

---

### 2-2. S2 — `BP_FxLabQuad` + `PlayVfx`

| ID | 선행조건 | 절차 | 판정(기계) | 주체 | 실패 시 의심 | 상태 |
|---|---|---|---|---|---|---|
| **E-S2-01** | `DT_Vfx` 5행 | `63000200`(hit) 1회 재생, `SetVisibility(true)→(false)` 타임스탬프 로그 2줄 | Δt = **0.55 ± 0.02**(= 6/10 − 0.05). `63000100`(smear)은 **0.45 ± 0.02** | V | 표시시간이 여전히 리터럴. 계산식 미배선 | 대기 |
| **E-S2-02** ★관통 | E-S2-01 | `set_rows`로 `63000200.fps` **10 → 40** → 재생 → 원복 → 재생 | Δt **0.55 → 0.10 → 0.55**. ★**plan의 "10→20 → Δt 0.25" 판정은 채택 불가** — 판별폭 0.05가 허용오차 ±0.05와 같아 구별이 안 되고, `63000100`이면 0.20이라 행 지정 없이는 기준이 흔들린다 | V | 값 캐시(BeginPlay 캐시 = 실험대 실격) 또는 리터럴 잔존 | 대기 |
| **E-S2-03** ★핫리로드 | E-S2-02 | ★**PIE를 유지한 채** `set_rows` 실행 → 다음 재생 관측 | PIE 재시작 없이 다음 재생부터 새 Δt 반영. **미반영이면 실험대가 아니라 스냅샷** | V | plan 미확인 항목 중 실험대 상설화를 좌우하는 유일한 건. **S5가 아니라 S2에서 닫아야 한다** — 여기서 실패하면 이후 전 단계의 반복 비용이 몇 배가 된다. MCP 브릿지가 PIE 중 동작하는지도 함께 확정 | 대기 |
| **E-S2-04** ★fail-loud | 텍스처 3장 미임포트 | `63000300` **단일 슬롯** 1회 재생 | `TEXMISS` **정확히 1줄**(vfxId·경로 포함) ∧ 쿼드 **미표시**(폴백 텍스처로 대체 0) ∧ 크래시 0. ★**로그 0건이면 FAIL** | V | plan의 "정확히 1줄"은 슬롯 미지정 시 오판을 부른다 — E-S2-05 참조 | 대기 |
| **E-S2-05** ★ | E-S2-04 | 파이어볼 배선(`FxCast=63000300` + `FxProj=63000500`) 1캐스트 | `TEXMISS` **정확히 2줄**(슬롯당 1줄). **1줄이면 한 슬롯이 조용히 스킵된 것 = FAIL** | V | ★**plan은 "정확히 1줄"만 적어 이 케이스에서 자동 FAIL 또는 오판한다.** 3슬롯 전량을 쓰는 스킬은 `32001000` 1행뿐이고 미임포트 텍스처는 3장 전부이므로 이 상황은 확정적으로 발생한다 | 대기 |
| **E-S2-06** ★0 vs 없는 Id | 〃 | (a) `vfxId=0` (b) `vfxId=63009999`(미존재) 각 1회 | (a) → **폴백 경로 로그**(`FXFALLBACK`, 모션 기본값으로 대체) ∧ `TEXMISS` 0줄 / (b) → **`FXNOROW` 1줄** ∧ 재생 0 ∧ 폴백 0. **두 로그 토큰이 달라야 한다** | V | ★**plan에 이 구별이 정의돼 있지 않다.** 기본공격 `FxCastId=0`이 즉시 이 경로를 탄다(Memo: "FxCast=0 → motions row5 폴백 63000100"). 0을 fail-loud로 처리하면 TEXMISS 폭주, 미존재 Id를 0처럼 폴백하면 무음 실패 | 대기 |
| **E-S2-07** | 〃 (**TC-G03 · I7 선실행**) | `63000100`(GridX=5,GridY=1) → `63000300`(13,9) → `63000100` 3회 왕복 | 매 재생 후 MID 스칼라 **4종 동시 일치**(`RowIndex`/`FrameCount`/`GridX`/`GridY`) ∧ 이전 값 잔존 **0** | V | 스칼라 부분 세트 = 무음 UV 깨짐. TC-G03을 D6보다 앞당겨 닫는다 | 대기 |
| **E-S2-08** 경계값 | 〃 | `fps=0` / `frameCount=0` / `gridX=0` / `colorRow=9` 4케이스 주입 | 4/4 모두 **재생 스킵 + 경고 1줄** ∧ 크래시·0나눗셈·무한 Delay **0**. 특히 `frameCount=0`은 Δt가 **음수(−0.05)**가 되므로 클램프 필요 | V | 린트가 아직 없다(L25 미구현). 실험대는 노브를 사람이 돌리므로 **잘못된 값이 반드시 들어온다** | 대기 |
| **E-S2-09** 순서 | 〃 | `PlayVfx` 그래프의 DT 조회 노드와 `SetVisibility`/스칼라 세트의 exec 위상 정적 조회 | DT 조회가 소비 노드보다 **exec 상류**. ★**하류면 첫 재생은 기본값·이후는 직전 값**(파트3 N1 / I1과 동형) | V | plan은 이 배치를 규정하지 않는다. 같은 함정이 라이브에서 이미 한 번 발생했다 | 대기 |

---

### 2-2'. S2' — VFX 텍스처 3장 임포트 (S2와 병렬)

| ID | 선행조건 | 절차 | 판정(기계) | 주체 | 실패 시 의심 | 상태 |
|---|---|---|---|---|---|---|
| **E-S2T-01** | 원본 3장 실재(선실행 확인 완료) | `vfx_draft.SrcFile` ↔ `vfx_inventory_raw.csv` 조인 (**TC-N03 선실행**) | 조인 실패 **0** ∧ `GridX`/`GridY`/`FrameCount` 불일치 **0**. **legacy 2행(SrcFile 공란)은 조인 대상 제외**를 명문화 | V | — | **PASS** |
| **E-S2T-02** ★ | 임포트 완료 | 임포트 결과 3장의 텍스처 설정을 `T_FX_Smear`/`T_FX_Hit`와 대조 | `CompressionSettings` · `sRGB` · **`Filter`** · `MipGenSettings` · `AddressX/Y` **5종 전부 legacy와 동일**. ★**`Filter != Nearest`이면 FAIL** | V | ★**plan에 이 판정이 없다.** 도트 시트를 4.15배(240uu → 415uu)로 확대하는 것이 S6의 1순위 실험인데, 필터가 Linear이거나 밉이 생성되면 확대 시 뭉개진다 → **뭉개짐을 "밀도 문제"로, 압축 링잉을 "블룸 과다"로 오독**한다 | 대기 |
| **E-S2T-03** | 〃 | 임포트 경로 확인 | 애셋 경로가 `DT_Vfx.texPath` 문자열과 **문자 단위 일치**(`/Game/VFX/T_FX_CastAtk` · `T_FX_CastSupport` · `T_FX_ProjectileArcane`) | V | `texPath`는 **String**(D5.5 절차서 확인)이라 런타임 경로 로드 → **DT 재임포트 불필요**가 맞다. 이름·폴더가 1글자만 달라도 `TEXMISS`가 영구히 지속된다 | 대기 |
| **E-S2T-04** ★결정적 | E-S2T-02, S2 완료 | `63000300`을 `colorRow=2`로 1회 재생 | 색이 **시안(197.7°)**. **보라(273.8°) = V축 반전** / **금색(52.0°) = 파라미터 미적용** | **O** | ★**plan의 이 단일 테스트는 실측 근거상 타당함을 확인했다** — 9행 그리드에서 index 2의 반전상은 index 6이고 실측 hue가 `52.0 / 281.9 / 197.7 / 118.9 / 24.6 / -1 / 273.8 / 301.9 / 263.6`이라 세 결과가 명확히 갈린다. 기계 선행조건: 재생 중 `RowIndex` 스칼라 readback **== 2** | 대기 |
| **E-S2T-05** | E-S2T-04 | `TEXMISS` 로그 재확인 | 임포트 후 `TEXMISS` **0줄** ∧ **BP 편집 0**(그래프 노드 수 diff 0) | V | ★plan이 "데이터 구동의 최종 증명"이라 부른 지점. 노드 diff가 1이라도 있으면 증명이 성립하지 않는다 | 대기 |

> ★**gameplay S0 실측 반영 ③** — `AssetTools.load_asset("/Game/VFX/T_FX_Smear")`(**접미사 없는 경로**)가 정상 리졸브됨이 확인됐다. → **E-S2T-03의 전제가 실증으로 닫혔다**: `texPath`가 `/Game/VFX/T_FX_CastAtk` 형식(`.T_FX_CastAtk` 오브젝트 접미사 없음)으로 저장돼 있어도 런타임 로드가 성립한다. 남은 위험은 **경로 자체의 철자·폴더 불일치 1종**으로 좁혀졌다.

#### E-S2T-01 선실행 결과 (본 문서에서 실행 · **PASS**)

| SrcFile | vfx_draft (GridX/GridY/FrameCount) | vfx_inventory_raw 실측 | 판정 |
|---|---|---|---|
| `Free/Part 1/03.png` | 13 / 9 / 13 | 832×576, CellPx 64, **13 / 9 / 13** | 일치 |
| `Free/Part 13/612.png` | 15 / 9 / 15 | 960×576, CellPx 64, **15 / 9 / 15** | 일치 |
| `Free/Part 14/652.png` | 16 / 9 / 16 | 1024×576, CellPx 64, **16 / 9 / 16** | 일치 |

조인 실패 0 · 불일치 0 · `FrameCount == GridX` **3/3**. → **TC-N03의 선결 조건(조인 키 컬럼 부재)은 `SrcFile` 컬럼 추가로 이미 해소됐다.**

---

### 2-3. S3 — Director + stagings 해석 + 자동 배터리

| ID | 선행조건 | 절차 | 판정(기계) | 주체 | 실패 시 의심 | 상태 |
|---|---|---|---|---|---|---|
| **E-S3-01** ★관통 | `DT_Stagings` 4행 | `62000200`(stg_melee_leap) 배터리 재생 → 5값 로그 에코 | 5값 == `advanceRatio 1.0 / approachMotionRow 4 / arcHeight -1.0 / isCameraCut TRUE / isTargetFlinch TRUE`. ★**Director 판정 2로 `62000300` → `62000200` 교체 승인** | V | `62000300`은 struct 기본값과 1비트 차이라 무효(§4 E-C2) | 대기 |
| **E-S3-02** ★3중 구별 | E-S3-01 | 같은 관측량에 대해 (a) 정상행 (b) `DefaultStagingId=62000100` (c) 미존재 Id 3케이스 | 세 케이스의 관측값이 **서로 다른 3값** ∧ (c)에서 `STGNOROW` 1줄 | V | 연쇄 가짜 GREEN(R7). 두 값만 비교하면 세 번째 경로가 통과한다 | 대기 |
| **E-S3-03** ★소비 실증 | 〃 (**TC-S02 선실행**) | `advanceRatio` 0.0 / 0.5 / 1.0 3회, 캐스터 시작 `S`·앵커 `A`·종점 `P` 로그 | `\|P − Lerp(S,A,α)\| < 1.0 uu` (3/3). ★**α=0.5의 이동거리가 α=1.0과 같으면 즉시 FAIL**(로그 에코는 "읽었다"의 증명일 뿐 "소비했다"가 아니다). ★**관측 대상 = 액터 루트 `GetActorLocation`** | V | plan의 "이동비 0.50" 판정은 형태가 옳다 — 관측량이 로그가 아니라 실제 변위다 | 대기 |
| **E-S3-04** 경계값 | 〃 | `advanceRatio` = `-0.5` / `1.5` 주입 | `[0,1]` **클램프 + 경고 1줄**. 무음 클램프면 FAIL ∧ 외삽으로 타겟 뒤로 넘어가면 FAIL | V | L12 린트 미구현 상태에서 노브가 사람 손에 있다 | 대기 |
| **E-S3-05** ★ | ~~E-S0-03(b) 통과~~ → ★**재기술(2026-08-12, [[자율진행_TC]] §8-3 · AU-A4-08)**: **대역 액터(`BP_FxLabDummy`)가 자기 컴포넌트로 빌보드 정렬을 구현했을 것.** 원 선행조건 `E-S0-03(b)`(= `BP_BattleSpawnPoint` 인스턴스에서 `AlignSpriteToCamYaw` 외부 호출)는 **S0 실측에서 FAIL**했고, Director가 대역 액터 전환으로 답했다 — 이 표는 그 전환을 반영하지 않은 채 남아 있었다 | `isCameraCut=TRUE` 행 재생 | 뷰타겟이 실제로 전환(`GetViewTarget` 로그 2줄이 다름) ∧ 복귀 ∧ **컷 진입·복귀 2시점에 빌보드 스냅 호출 각 1회** ∧ ★**`SpriteRight`·`SpriteLeft` 양쪽이 정렬**(비활성 측 미정렬 잔존 0) | V | ★**카메라 전환은 S3의 신규 구현 항목인데 plan 단계 설명에 없다** — S1이 매니저·토글버튼을 삭제하므로 기존 카메라 코드가 통째로 사라진다. R3의 실행 수단이 **대역 액터의 자체 구현 여부**에 걸려 있다(구 `E-S0-03(b)` 아님) | 대기 |
| **E-S3-06** | ~~E-S0-03(c) 통과~~ → ★**재기술(2026-08-12, [[자율진행_TC]] §8-3 · AU-A4-08)**: **대역 액터(`BP_FxLabDummy`) 자체의 flinch 구현이 있을 것.** 원 선행조건 `E-S0-03(c)`(= `BP_BattleSpawnPoint` 인스턴스에서 `PlayHurtReaction` 외부 호출)는 **S0 실측에서 FAIL**했고, Director가 대역 액터 전환으로 답했다 | `isTargetFlinch` TRUE/FALSE 각 1회 | TRUE에서 타겟 Hurt 모션 재생 **1회**, FALSE에서 **0회** | V | 호출 수단이 없으면 이 축은 **S3에서 검증 불가 → D6 이월**로 명시 기록. ★AT4 착수 전 이 재기술 산출물(대역 액터가 실제로 정렬/flinch를 구현했는가) 확인이 **AT4 착수 게이트**(AU-A4-08) | 대기 |
| **E-S3-07** ★커버리지 | 〃 | 배터리 4행 전량 실행 | 4/4 행이 **스킬을 경유하지 않고 직접 stagingId 지정으로** 도달. ★`62000200`은 라이브 7행 어디서도 참조되지 않으므로(실측: `62000100×4`, `62000300×1`, `62000400×2`) 스킬 경유 배터리는 구조적으로 3/4밖에 못 돈다 | V | plan "4행 배터리"가 스킬 경유를 전제하면 도약 프로필이 영원히 미검증 | 대기 |
| **E-S3-08** | 〃 | `62000200` 재생(`approachArcHeight = -1.0`) | 캐스터 Z좌표 **지면 아래로 내려가지 않음** ∧ 음수 arcHeight에 대해 경고 1줄 | V | `-1.0`은 "미확정 sentinel(L14 FAIL 의도)"이지 실값이 아니다. 그대로 소비하면 땅을 파고드는 포물선이 나온다 | 대기 |

---

### 2-4. S4a — 오너 수동 struct (추가 6필드)

| ID | 선행조건 | 절차 | 판정(기계) | 주체 | 실패 시 의심 | 상태 |
|---|---|---|---|---|---|---|
| **E-S4a-01** | 세션 전 `list_properties(F_SkillsRow)` 23필드 스냅샷 + `get_rows(DT_Skills)` 7행 전문 덤프 | 세션 후 재조회 | 필드 수 **23 → 29** ∧ **기존 23필드의 이름·타입·순서 diff 0** ∧ 신규 6개 전부 **Integer, 기본값 0** | V | 철자 오류(대소문자 무관하나 철자 틀리면 **무음 매칭 실패 후 기본값**) | 대기 |
| **E-S4a-02** ★ | 〃 | 〃 | `DT_Skills` **행 수 == 7** ∧ 기존 23필드 **값 diff 0**(7행 × 23필드 = **161셀 전수**) | V | struct 편집이 DT를 재직렬화한다. 요약값 일치는 근거 아님 — **셀 단위 diff** | 대기 |
| **E-S4a-03** | 〃 | `BP_BattleManager` · `WBP_SkillMenu` 컴파일 | 컴파일 에러 **0** ∧ `BreakFSkillsRow` 노드의 기존 출력 핀 연결 **전부 유지** | V | struct 변경 시 Break 노드 핀 재생성으로 연결이 끊긴다 | 대기 |
| **E-S4a-04** ★문서 | — | `D5.5_오너_struct세션_절차서.md` §2차 세션 표 확인 | ★절차서가 "추가 6 / 제거 10"을 여전히 한 세션으로 적고 있다 → S4a가 추가 6을 먼저 하면 ⑩a 세션에서 오너가 **중복 추가를 시도**한다. 절차서에 **"추가 6은 E단계 S4a에서 완료 — ⑩a는 제거 10만"**이 반영돼야 PASS | V | 세션 계획이 **2회 → 3회**로 늘어난 사실이 plan에도 절차서에도 없다(E-H3) | 대기 |

> **잔여**: Director 판정 5로 **S4a-0(TAPython 자동화 조사)가 신설**됐다. 본 문서는 지시대로 새 TC를 지어내지 않으므로 **S4a-0 전용 TC는 미작성**이다. 조사 결과가 나온 뒤 (a) 자동화 성립 → E-S4a-01~03의 주체가 `O`→`V`로 바뀌고 E-S4a-04는 **불요**, (b) 수동 확정 → E-S4a-04가 **필수 게이트**로 승격. **어느 쪽인지 정해지기 전까지 E-S4a-04를 해제하지 말 것.**

---

### 2-5. S4b — `skills` 7행 값 기입 + 상류 1홉

| ID | 선행조건 | 절차 | 판정(기계) | 주체 | 실패 시 의심 | 상태 |
|---|---|---|---|---|---|---|
| **E-S4b-01** ★Critical | S4a 통과 | ★**Director 판정 3 승인** — 갱신 수단을 **MCP `set_rows`로 고정**하고 **`data/skills.csv` 재임포트 금지**를 명문화 | 갱신 후 `DT_Skills` **행 수 == 7**(프루브 `31002000`·`31003000` 생존) ∧ **필드 수 == 29** ∧ 기존 23필드 값 diff 0 | V | `data/skills.csv`는 **15컬럼·5행**이고 라이브 DT는 **23필드·7행**이다. 재임포트하면 프루브 2행 + 8필드가 한 번에 소실되고 **F7b ⑧이 실행 불가**가 된다. CSV를 쓴다면 `data/skills_v2_draft.csv`(23컬럼/7행)여야 한다 | 대기 |
| **E-S4b-02** | E-S4b-01 | 7행 6필드 값을 `skills_merged_42.csv` 동일 Id 행과 대조 | **42셀 전수 일치**(예: `32001000` = `62000300 / 63000300 / 63000500 / 63000200 / 0 / 0`) | V | 손입력 오타 | 대기 |
| **E-S4b-03** ★관통 | 〃 | `32001000` 1캐스트 → `RESOLVE` 로그 | 로그의 stagingId·fx 3종이 DT 값 **그대로** ∧ 이어서 E-S3-01의 5값이 `62000300` 행 값과 일치(= 상류 1홉이 실제로 이어짐) | V | 중간 캐시·하드코딩 | 대기 |
| **E-S4b-04** ★레고 | 〃 (**TC-S05 선실행**) | `32001000.StagingId`를 `62000300 → 62000200`으로 **1셀** 변경 | 연출이 바뀐다(이동비 0.0 → 1.0, 컷 F → T) ∧ **BP 그래프 노드 수 diff == 0** | V | 1노드라도 바뀌면 레고 원칙 미달 | 대기 |
| **E-S4b-05** ★라이브 회귀 | 〃 | `map_battle_octopath` PIE 5스킬 회귀 (**BPC-31 선실행**) | 기본 **30** · 베기 **42+STUN** · 파볼 **61+ATK_DOWN** · 막기 피격 **15** · 치유 **−33** ∧ `BattleLog\|` 행수 불변 ∧ **E-S1-02 manifest 재실행**(공유 애셋 불변) | V | 수치만 보면 룩 변경을 못 잡는다 — manifest 병행이 필수 | 대기 |

---

### 2-6. S5 — 노브·키보드·루프·슬로우·패리티

| ID | 선행조건 | 절차 | 판정(기계) | 주체 | 실패 시 의심 | 상태 |
|---|---|---|---|---|---|---|
| **E-S5-01** ★패리티 | 3슬롯 배선 완료 | ★**`32001000` 한정**으로 `bSingleQuadMode` on/off 각 1캐스트 | off(3슬롯) → `PREEMPT` **0줄** ∧ 동시 표시 쿼드 수 **2 이상** / on(1쿼드) → `PREEMPT` **≥1줄** ∧ 동시 표시 **1** | V | ★**plan의 "토글에 따라 ≥1/0"은 스킬을 지정하지 않으면 무의미하다** — 7행 중 3슬롯을 동시에 쓰는 것은 `32001000` **1행뿐**이고 나머지 6행은 FX 슬롯 1개라 양쪽 다 0이 나온다 | 대기 |
| **E-S5-02** | E-S5-01 | on 모드에서 캐스트→투사 전환 관측 | 캐스트 FX가 **중간 프레임에서 절단**되는 사실이 `PREEMPT` 로그에 **프레임 인덱스**로 기록 | V | I8(라이브 슬롯 경합)의 정량 근거를 남기는 유일한 지점 | 대기 |
| **E-S5-03** | 〃 | Time Dilation 1.0 → 0.25 | Δt **4배 ± 5%**(0.55 → 2.20) ∧ 프레임 진행도 4배 감속(정지 아님) | V | 머티리얼 `Time` 기반 플립북이면 dilation을 따르고, BP 스칼라 세팅 기반이면 Tick 감속을 따른다 — **어느 쪽인지 이 TC가 닫는다**(plan 미확인 항목) | 대기 |
| **E-S5-04** ★소프트락 | E-S5-03 | Dilation **0** 시도 | 하한 **0.02 클램프** 적용 ∧ 재생이 유한 시간에 종료 ∧ 소프트락 0 | V | `AWorldSettings::MinGlobalTimeDilation`이 0.0001이라 진짜 0은 안 되지만 **실질 정지**(0.55초 → 5500초)다. plan의 "하한 0.02 권장"은 옳은 완화이며 TC로 강제해야 한다 | 대기 |
| **E-S5-05** ★ | 〃 | `ColorRow` 노브를 `4`로 올린 상태에서 **legacy 행**(`63000100`, GridY=**1**) 재생 | UV 범위 밖 접근 **차단** + 경고 1줄. 깨진 화면이 그대로 나오면 FAIL | V | ★plan 노브 목록에 `ColorRow`가 있으나 **`ColorRow < GridY` 가드가 없다.** legacy 2행은 GridY=1이라 0 외의 모든 값에서 즉시 깨진다 | 대기 |
| **E-S5-06** ★스케일 노브 | 〃 | 쿼드 스케일 2.4 → 4.15 적용 후 legacy·신규 동시 표시 | 등가 밀도 6.48을 **두 계열이 동시에 만족하지 못함**을 수치로 기록: legacy 5.00 → **1.30배**, 신규 3.75 → **1.73배**. → **스케일 노브는 전역 1값이 아니라 vfxId별 오버라이드**여야 함을 S6 산출물에 반영 | V | 전역 1값이면 한쪽이 반드시 어긋난 채로 오너가 룩을 판정하게 된다 | 대기 |
| **E-S5-07** 상태누수 | 〃 | 루프 모드 20사이클 | 20사이클 후 MID 스칼라·슬롯 점유·타이머 핸들 잔존 **0** ∧ 메모리 증가 선형 아님 | V | 턴 큐가 없어 상태 리셋 지점이 없다 — 루프가 유일한 누수 검출 수단 | 대기 |
| **E-S5-08** 동시성 | 〃 | 같은 슬롯에 2회 연속 즉시 재생 | `RetriggerableDelay` 리트리거로 첫 재생이 잘리는지 로그로 판별 ∧ Δt 측정이 오염되지 않음(측정 전용 타임스탬프 별도) | V | E-S2-01/02의 Δt 판정 전체가 이 오염에 노출된다 | 대기 |

> **잔여**: Director 판정 1로 **S5에 `IsCameraCut` 런타임 오버라이드 노브**가 추가된다. 본 문서는 새 TC를 지어내지 않으므로 **해당 노브 전용 TC는 미작성**이다. 다만 **E-S6-03이 이미 그 노브의 존재를 전제**하고 있으므로(§2-7), 노브가 없으면 E-S6-03은 실행 불가다. 노브 신설 시 TC 1건 추가 필요.

---

### 2-7. S6 — 오너 육안

| ID | 선행조건 | 절차 | 판정 | 주체 | 실패 시 의심 | 상태 |
|---|---|---|---|---|---|---|
| **E-S6-01** | S5 전량 | 스케일 축(2.4 / 3.2 / 4.15) 순회 | 축별 채택값 1개 확정 + **기각된 값과 사유 기록** | O | — | 대기 |
| **E-S6-02** | 〃 | `EmissiveBoost` 3.0 / 2.0 / 1.0 | ★**MI 자산이 아니라 MID(런타임)로만 조작했음을 E-S1-02 manifest로 사후 확인** | O+V | 자산 직접 편집 시 라이브 legacy FX 룩이 같이 바뀐다 | 대기 |
| **E-S6-03** ★ | 〃 + **S5 `IsCameraCut` 오버라이드 노브** | 8기 배치 상태에서 4.15 스케일 이펙트 표시 | 진형 150uu에서 좌우 이웃 몇 기를 덮는지 **기수로 기록**(예상 2~3기) ∧ **컷 on/off 양쪽에서** 관측 | O | ★컷이 켜진 스킬에는 신규 VFX가 하나도 붙어 있지 않다(E-C1) — 이 TC는 반드시 `IsCameraCut`을 **노브로 강제 오버라이드**한 상태에서 돌려야 의미가 있다 | 대기 |
| **E-S6-04** | 〃 | 산출물 작성 | 축별 채택값 + 스키마 승격 요청 컬럼 + ★**"3슬롯 전제" · "카메라 컷 강제 전제" · "타이밍 축 미검증" 3개 유효범위 고지**가 문서에 포함 | O | 고지 없으면 D6에서 그대로 채택돼 라이브에서 무너진다 | 대기 |

---

## 3. TC 인덱스 (전량 51건)

| 단계 | ID 범위 | 건수 | 주체 | 상태 |
|---|---|---|---|---|
| S0 | `E-S0-01` ~ `-03` | 3 | V | 대기 3 |
| S1 | `E-S1-01` ~ `-05` | 5 | V | 대기 5 |
| S2 | `E-S2-01` ~ `-09` | 9 | V | 대기 9 |
| S2' | `E-S2T-01` ~ `-05` | 5 | V 4 / O 1 | **PASS 1** · 대기 4 |
| S3 | `E-S3-01` ~ `-08` | 8 | V | 대기 8 |
| S4a | `E-S4a-01` ~ `-04` | 4 | V | 대기 4 |
| S4b | `E-S4b-01` ~ `-05` | 5 | V | 대기 5 |
| S5 | `E-S5-01` ~ `-08` | 8 | V | 대기 8 |
| S6 | `E-S6-01` ~ `-04` | 4 | O 3 / O+V 1 | 대기 4 |
| **합계** | | **51** | **V 43 · O 7 · O+V 1** | **PASS 1 · 대기 50** |

**계승 관계**: `E-S2-07`⊃TC-G03·I7 / `E-S2T-01`⊃TC-N03 / `E-S3-03`⊃TC-S02 / `E-S4b-04`⊃TC-S05 / `E-S4b-05`⊃BPC-31 / `E-S1-05`⊃BPC-31(부분). D3 TC와 **중복 생성 0건** — 전부 선실행 또는 확장이다.

**미작성 2건(신설 대기)**: S4a-0(TAPython 조사) · S5 `IsCameraCut` 오버라이드 노브. 둘 다 Director 판정으로 신설된 항목이며 "새 TC를 지어내지 마라"는 지시에 따라 본 문서에서는 **미작성으로 명시**만 한다.

---

## 4. plan 적대 검증 — Critical 4건

### [Critical] E-C1 — plan §3대 불일치 ① × `skills_merged_42.csv` + `stagings_draft.csv`: 밀도 해법의 적용 대상이 0개다

plan은 밀도 문제(신규 VFX 0.58×)의 답을 **"액션 캠 컷에서만 크게"**로 확정했다. 데이터 대조 결과 **컷이 켜진 스킬과 신규 VFX를 쓰는 스킬의 교집합이 공집합**이다.

| 스테이징 | `IsCameraCut` | 이 프로필을 쓰는 라이브 7행 | 그 행들의 FX |
|---|---|---|---|
| `62000100` 근접 | **TRUE** | 31000000 · 31001000 · 31002000 · 31003000 | FxCast **0**(legacy smear 폴백) · FxImpact **63000200**(legacy hit) |
| `62000200` 도약 | **TRUE** | **0행** | — |
| `62000300` 원거리 | **FALSE** | 32001000 | 63000300 · 63000500 · 63000200 |
| `62000400` 지원 | **FALSE** | 33001000 · 34001000 | 63000400 |

→ **신규 VFX 3장(63000300/400/500)은 전부 `IsCameraCut=FALSE` 스킬에만 붙어 있고, 컷이 켜진 스킬은 전부 legacy(5.00 uu/텍셀, 0.77×)다.** 밀도가 가장 나쁜 대상에 완화책이 도달하지 않는다.
**재현**: `skills_merged_42.csv` 7행의 `StagingId` × `stagings_draft.csv` `IsCameraCut` 조인. → **CONFIRMED**
**Director 판정 1 (§7)**: **(c)+(a) 채택** — plan의 "답"을 *"근접은 문제없음 / 원거리·지원은 미해결"*로 격하하고, S5에 `IsCameraCut` 런타임 오버라이드 노브를 넣어 S6 실험으로 답을 찾는다.

### [Critical] E-C2 — plan §위험 R7 × `F_StagingsRow` 기본값: 관통 판정값 `62000300`이 R7에 걸린다

plan은 *"`DefaultStagingId=62000100`과 구별하려면 `62000300`으로 판정"*이라 했다. 그런데

| | advanceRatio | approachMotionRow | arcHeight | isCameraCut | isTargetFlinch |
|---|---|---|---|---|---|
| `62000300`(정상행) | 0.0 | -1 | 0.0 | FALSE | **TRUE** |
| **F_StagingsRow struct 기본값** | 0.0 | -1 | 0.0 | false | **false** |
| `62000200`(권고 판정값) | **1.0** | **4** | **-1.0** | TRUE | TRUE |

→ `62000300`은 **행 조회가 실패해 빈 struct를 받아도 `isTargetFlinch` 한 비트를 빼고 전부 같은 값**이 나온다. 게다가 flinch 축은 하드 스코프 + `PlayHurtReaction` 외부 호출 가능성 미확인이라 **관측 자체가 불확실**하다. plan의 R7 완화책이 R7이 경계한 바로 그 무음 실패를 통과시킨다.
근거: D5.5 절차서 §① 기본값 표 실측. → **CONFIRMED**
**Director 판정 2 (§7)**: **승인.** `62000200` + 3중 구별로 교체 → `E-S3-01`·`E-S3-02`에 반영 완료.

### [Critical] E-C3 — plan S4b × `data/skills.csv` 실측: 갱신 경로 미지정이 프루브 2행과 8필드를 지운다

plan S4b는 *"`skills` 7행 값 기입"*이라고만 적고 **갱신 수단을 지정하지 않는다.** 파일 실측:

| 대상 | 컬럼 | 행 | 비고 |
|---|---|---|---|
| 라이브 `F_SkillsRow` / `DT_Skills` | **23필드 / 7행** | 프루브 `31002000`·`31003000` 포함 | mtime Jul 16 14:45 |
| `data/skills.csv` | **15컬럼 / 5행** | 프루브 없음 | **라이브 소스가 아니다** |
| `data/skills_v2_draft.csv` | 23컬럼 / 7행 | 실제 임포트 소스 | |

→ `data/skills.csv`를 재임포트하면 **프루브 2행 소실 + `Category`/`HitCount`/`PrimaryTargetGroup`/FX·SFX 5문자열 8필드 소실**. F7b ⑧이 프루브 2행 E2E 게이트이므로 **⑧이 실행 불가**가 된다. [[BP정리_통합명세_2026-08-11]] §3 순서12가 P1으로 이미 금지했으나 **plan에 그 금지가 반복돼 있지 않다.** → **CONFIRMED**
**Director 판정 3 (§7)**: **승인.** MCP `set_rows` 고정 + `data/skills.csv` 재임포트 금지 명문화 → `E-S4b-01`에 반영 완료.

### [Critical] E-C4 — plan S2' × 텍스처 임포트 설정: 판정 기준 부재가 S6 룩 판정 전량을 오염시킨다

S2'의 완료 판정은 색 축(`RowIndex=2` 시안) 하나뿐이고 **`Filter`·`CompressionSettings`·`MipGenSettings`·`sRGB` 판정이 없다.** 도트 시트를 4.15배(240uu → 415uu)로 확대하는 것이 S6의 1순위 실험인데, Filter가 Linear이거나 밉이 생성되면 확대 시 뭉개진다 → **오너가 그 뭉개짐을 "밀도 문제"로, BC 압축 링잉을 "블룸 과다"로 오독**한다. legacy 2장과 설정이 다르면 3대 불일치의 legacy↔신규 비교 자체가 성립하지 않는다.
→ **CONFIRMED**(판정 기준 부재) / 영향 **CONFIRMED**(도트 확대의 필터 의존은 결정론적). 완화 = `E-S2T-02`.

---

## 5. High 9건 · Medium 7건

### High

| ID | 위치 | 문제 | 재현/근거 | 확신도 |
|---|---|---|---|---|
| **E-H1** | plan §FX슬롯 / R1 / S5 | **`bSingleQuadMode` 패리티의 표본이 1행뿐.** 3슬롯을 실제로 동시 점유하는 라이브 스킬은 `32001000` 하나이고(나머지 6행은 FX 슬롯 1개), 다른 행에서는 토글해도 양쪽 다 `PREEMPT=0`이라 "토글에 따라 ≥1/0"이 성립하지 않는다 | `skills_merged_42.csv` 7행 Fx 3컬럼 전수 | CONFIRMED |
| **E-H2** | plan S3 | **액션캠 전환이 신규 구현인데 단계 설명에 없다.** S1이 `BP_BattleManager`·`BP_CamToggleButton`을 삭제하므로 카메라 코드가 통째로 사라진다. 밀도 해법이 컷에 걸려 있으므로 부수 기능이 아니라 **S6의 전제** | plan S1 삭제 목록 × [[전투BP_현황도_2026-08-11]] §2(카메라 제어 = Manager 소유) | CONFIRMED |
| **E-H3** | plan §설계제약 3 × [[D5.5_오너_struct세션_절차서]] | **오너 struct 세션이 2회 → 3회로 늘어난다.** 절차서는 `F_SkillsRow` 추가6+제거10을 ⑩a 1세션으로 못박았는데 S4a가 추가6만 먼저 뗀다. BP정리 §4-3의 *"비용은 편집 수가 아니라 세션 수에 비례"*와 직접 충돌하고, **절차서 2차 세션 표가 갱신되지 않으면 오너가 중복 추가를 시도**한다 | 절차서 §2차 세션 ④ 표 | CONFIRMED |
| **E-H4** | plan S2 완료판정 | **`TEXMISS` "정확히 1줄"이 파이어볼에서 자동 오판.** `32001000`은 미임포트 텍스처를 2개(`63000300`+`63000500`) 참조하므로 1캐스트에 2줄이 정상 | 실데이터 | CONFIRMED |
| **E-H5** | plan S2 완료판정 | **FPS 관통 판정이 행 미지정 + 판별폭 부족.** "10→20 시 Δt 0.25"는 `63000200`(6프레임) 한정이고 `63000100`(5프레임)이면 0.20이다. 게다가 판별폭 0.05가 허용오차 ±0.05와 같아 **구별 불가** | `vfx_draft.csv` FrameCount 실측 + `FrameCount/FPS−0.05` 공식 | CONFIRMED |
| **E-H6** | plan §검증 "라이브 무손상" | **정의가 `.umap` 바이트 + 전투 수치뿐이라 공유 애셋 변경을 못 잡는다.** 노브 ②(`EmissiveBoost` 3.0→1.0)가 정확히 그 표면 — `MI_FX_Smear`/`MI_FX_Hit`는 라이브가 쓰는 자산이고, 이를 고쳐도 dmg·로그 행수는 안 변한다 | `Content/VFX/` 실측(MI 2개, 신규 텍스처 0개) | CONFIRMED |
| **E-H7** | plan §노브 목록 | **`ColorRow` 노브에 `ColorRow < GridY` 가드가 없다.** legacy 2행은 GridY=1이라 0 외의 모든 값에서 즉시 UV 범위 밖 | `vfx_draft.csv` GridY 열 | CONFIRMED |
| **E-H8** | plan 전반 | **`FxId=0`(폴백)과 미존재 Id(fail-loud)의 구별이 정의돼 있지 않다.** 기본공격 `FxCastId=0`이 즉시 이 경로를 탄다. 0을 fail-loud로 처리하면 로그 폭주, 미존재를 0처럼 폴백하면 **R8이 막으려던 무음 실패** | `skills_merged_42.csv` Memo + 7행 중 4행이 FxCast=0 | CONFIRMED |
| **E-H9** | plan §노브 우선순위 ① | **전역 스케일 1값으로는 legacy와 신규를 동시에 6.48로 못 맞춘다.** 등가 배율이 legacy **1.30×**, 신규 **1.73×**로 다르다. S6가 "축별 채택값 1개"를 내면 한쪽이 반드시 어긋난 채 확정된다 | 6.48/5.00 vs 6.48/3.75 | CONFIRMED |

### Medium

| ID | 문제 | 확신도 |
|---|---|---|
| **E-M1** | **S0의 "12종" 목록이 plan에 없다** → "12/12 생성"은 목록 없이 검증 불가한 판정 기준이다 | CONFIRMED → **Director 판정 4로 15종 확정·해소** |
| **E-M2** | S1 "PIE 런타임에러 0"이 무의미한 판정이 된다 | ★**PLAUSIBLE → CONFIRMED 승격**(gameplay S0 실측). 단 **방향이 반대**: 예상은 "Accessed None 다발로 자동 FAIL"이었으나, 실측상 `IsValid(ManagerRef)`의 Not Valid 분기가 **미배선**이라 **에러 없이 초기화만 조용히 스킵**된다 → **자동 PASS = 가짜 GREEN**. E-S1-03 판정을 "초기화 결과값 직접 관측"으로 교체함 |
| **E-M3** | **`62000200`이 라이브 7행 어디서도 참조되지 않는다** → 스킬 경유 배터리는 3/4행만 커버 | CONFIRMED |
| **E-M4** | `ApproachArcHeight=-1.0`은 "미확정 sentinel(L14 FAIL 의도)"인데 S3가 그대로 소비하면 **지면 아래 궤적** | CONFIRMED(값) / PLAUSIBLE(발현) |
| **E-M5** | **struct 자동화 전례가 검토되지 않았다.** TAPython + PythonScriptPlugin이 설치·활성화돼 있고 `f7b_struct_bootstrap.py`가 `PythonStructLib`로 **`F_SkillsRow`를 15→23필드로 이미 확장한 실적**이 있다. "MCP 토큰셋에 struct 툴이 없다"는 절차서 근거는 참이나 **Python 경로는 별개**다. ⚠단 스크립트의 **알려진 결함 2건**(멱등 가드가 애셋 1개 존재만으로 전체 스킵 / `_ensure_friendly_name`이 `names[-1]`을 "방금 추가한 필드"로 가정)을 **선수정**해야 한다 | CONFIRMED(전례 존재) / PLAUSIBLE(현 스키마 재적용) → **Director 판정 5로 S4a-0 신설** |
| **E-M6** | S2/S3의 **DT 조회 exec 위상**이 규정돼 있지 않다 → 파트3 N1 / I1과 동형 함정(첫 재생 기본값, 이후 직전 값)이 FxLab에서 재발 가능 | CONFIRMED(규정 부재) |
| **E-M7** | plan §미확인 5건에서 **빠진 미확인 4건**: (a) 카메라 컷 신규 구현 수단 (b) `AlignSpriteToCamYaw` 외부 호출 가능성 (c) `PlayHurtReaction` 외부 호출 가능성 (d) PIE 중 MCP `set_rows` 동작 여부. ★(b)는 gameplay 실측으로 **대상이 2컴포넌트(`SpriteRight`/`SpriteLeft`)임이 추가 확인**되어 난이도가 올라갔다 | CONFIRMED |

---

## 6. 오너 질문 6건에 대한 직답

| # | 질문 | 답 |
|---|---|---|
| 1 | "S2·S3는 S4a와 완전 독립"이 참인가 | **데이터 의존은 참**(`F_VfxRow`·`F_StagingsRow`·`DT_Vfx`·`DT_Stagings`가 이미 엔진에 존재, 2026-08-11 10:07 생성 확인). **그러나 S3에 숨은 구현 의존이 2개** — 카메라 전환 신규 구현(E-H2)과 flinch 호출 수단(E-S0-03c). 둘 다 struct와 무관하지만 S3 완료 판정 3축 중 2축을 좌우한다 |
| 2 | 3슬롯 튜닝이 통째로 폐기될 위험이 실제로 완화되나 | **부분적으로만.** 토글은 차이를 관측하게 해주지만 표본이 `32001000` **1행**이고(E-H1), **라이브에서 3슬롯을 어떻게 만들지**(컴포넌트 추가 = 라이브 클래스 편집 = 제약 위반)는 스파이크가 답하지 않는다. S6 산출물의 "3슬롯 전제" 명시는 필수이되 그것만으로는 폐기 위험이 남는다 |
| 3 | 밀도 4.15 vs 150uu의 답 "액션 캠 컷에서만"이 모든 스킬에 통하는가 | **통하는 스킬이 0개다.** 컷 TRUE 스킬 4행은 전부 legacy VFX, 신규 VFX 3장은 전부 컷 FALSE 스킬에 붙어 있다(E-C1). `62000300`/`62000400`은 어떻게 되나 → **plan에 답이 없었다.** → Director 판정 1로 "미해결"을 명시하고 S6 실험으로 찾는다 |
| 4 | 텍스처 미임포트를 "차단 아님"으로 본 것이 옳은가 | **fail-loud 설계로는 옳다**(`texPath`가 String이라 런타임 경로 로드 → 임포트 즉시 살아나는 것도 참, gameplay `load_asset` 실측으로 보강). **다만 "빈 화면을 통과로 오판"할 위험은 실재한다** — E-H4(슬롯당 로그 수)와 E-H8(0 vs 미존재 구별)이 정의되지 않은 상태에서는 오판이 가능하다. 두 TC를 넣으면 차단 아님이 유지된다 |
| 5 | 미확인 5건이 정직한가 | **정직하다. 다만 목록이 불완전하고 일부는 지금 닫힌다.** Plane 100uu는 MCP 1콜로 닫히고(plan 자신의 3.75/5.00/6.48 계산이 그 전제로 자기일관), Dilation 소프트락은 `MinGlobalTimeDilation=0.0001` 클램프 때문에 "진짜 락"이 아니라 "실질 정지"이며 plan의 0.02 하한은 옳은 완화다. **반대로 목록에 없는 미확인이 4건 더 있다**(E-M7) |
| 6 | 하드 스코프가 검증을 무의미하게 만드는 지점 | **"룩"은 유효, "타이밍"은 무효.** §8 참조 |

---

## 7. Director 판정 (2026-08-11) — qa 요구 5건에 대한 답

| # | qa 요구 | 판정 | 본 문서 반영 |
|---|---|---|---|
| **1** | E-C1 밀도 해법 | **(c)+(a) 채택** — plan의 "답"을 *"근접은 문제없음 / 원거리·지원은 미해결"*로 **격하**하고, **S5에 `IsCameraCut` 런타임 오버라이드 노브**를 넣어 S6 실험으로 답을 찾는다. **답을 미리 아는 게 아니라 찾는 게 실험대의 목적** | §4 E-C1 · §2-6 잔여 · `E-S6-03` 선행조건 |
| **2** | E-C2 관통 판정값 | **승인.** `62000200` + 3중 구별 | `E-S3-01` · `E-S3-02` |
| **3** | E-C3 S4b 갱신 수단 | **승인.** MCP `set_rows` 고정 + `data/skills.csv` 재임포트 금지 명문화 | `E-S4b-01` |
| **4** | E-M1 S0 12종 목록 | **15종으로 확정** — qa 12종 + gameplay 3종(`AlignSpriteToCamYaw` 호출 · `PlayHurtReaction` 호출 · 외부 raw 변수 Get). **이미 S0 실행 중** | `E-S0-01` |
| **5** | E-M5 struct 자동화 | **S4a-0(조사) 신설.** TAPython 경로가 살아 있으면 오너 세션 0. ⚠결함 2건 선수정 필요. 수동이면 **세션 2→3회 증가**를 D5.5 절차서에 반영 | §2-4 잔여 · `E-S4a-04` |

### ★gameplay S0 실측 반영 3건 (qa TC 작성 시점엔 없던 정보)

| # | 실측 | 영향받는 TC | 반영 내용 |
|---|---|---|---|
| **G1** | `BP_BattleSpawnPoint`에 **`Sprite` 단일 컴포넌트가 없다** — 실제는 `SpriteRight` + `SpriteLeft`를 **`FaceLeft` bool로 스위칭** | `E-S0-03` · `E-S3-03` · `E-S3-05` | E-S0-03(a) Get 대상을 **`SpriteRight`·`SpriteLeft`·`FaceLeft` 3종**으로 확정 / E-S3-03 변위 관측 대상을 **액터 루트 `GetActorLocation`**으로 확정(스프라이트 컴포넌트 아님) / E-S3-05에 **양 컴포넌트 정렬** 판정 추가 — 비활성 측 미정렬 잔존은 방향 전환 순간 R3(종이 효과) 재발 |
| **G2** | **`IsValid(ManagerRef)` 가드의 "Not Valid" 분기가 미배선** — 에러는 안 뜨고 초기화가 스킵된다 | `E-S1-03` · `E-M2` | E-M2 **PLAUSIBLE → CONFIRMED 승격**. 단 **방향이 반대로 확정**: "에러 다발로 자동 FAIL"이 아니라 **"에러 0으로 자동 PASS = 가짜 GREEN"**. 완화책이 화이트리스트 → **초기화 결과값(8기 `Hp`/`Atk`/`Def`) 직접 관측**으로 교체 |
| **G3** | `AssetTools.load_asset("/Game/VFX/T_FX_Smear")`(접미사 없음)가 **정상 리졸브** | `E-S2T-03` | 전제 실증 확인. `texPath`가 오브젝트 접미사 없이 저장돼 있어도 런타임 로드가 성립 → 남은 위험이 **경로 철자·폴더 불일치 1종**으로 좁혀짐 |

---

## 8. 이 스파이크가 놓칠 것 — D6에서 재검증 필수 (12건)

"구조가 돈다"가 확인돼도 **여전히 모르는 것**:

| # | 놓치는 것 | 왜 놓치나 | D6 재검증 항목 |
|---|---|---|---|
| **1** | ★**라이브 exec 위상 전체.** 턴 길이를 실제로 지배하는 `Delay 0.55/0.25/0.75/0.45`(H06~H09)와 `PlayAttack` 내부 ~0.58초 블록(H18)이 FxLab에 없다 | FxLab은 자체 리듬. R2의 "가짜 블로킹 딜레이 노브"는 근사치 1개일 뿐 4단 위상이 아니다 | TC-N09 / BPC-12 / BP정리 §5-1 Director 판정 (a)·(b) |
| **2** | ★**임팩트 FX 시각 ↔ 데미지 판정 시각의 분리.** "숫자가 먼저 뜨고 이펙트가 나중에 터진다"를 **원리적으로 관측 불가** | 데미지·HP 없음 | BP정리 §5-6 / TC-N08 |
| **3** | **멀티히트 연출 N회 / 판정 1회.** 라이브 7행은 `HitCount` 전량 1이라 스파이크 데이터에 멀티히트가 존재하지 않는다 | 데이터 부재 + 판정 없음 | TC-G06 / BPC-23 |
| **4** | **사망 계열 전량** — 시체 움찔 0, 동시 사망, 턴 진행 중 사망, 사망 유닛 대상 FX | 하드 스코프 | TC-G06(2) / BPC-08 |
| **5** | ★**라이브에서 3슬롯을 어떻게 만드는가(I8/H30).** FxLab은 신규 액터 3기, 라이브는 `EffectQuad` 1메쉬 + MID 2개. 패리티 토글은 **차이를 재기만 하고 해법을 내지 않는다** | 컴포넌트 추가 = 라이브 클래스 편집 = 제약 위반 | I8 담당 지정 + BPC-27 |
| **6** | ★**시간 밀도 ②(8fps 캐릭터 vs 20fps VFX).** 유닛을 "외부 Get만"으로 구동하면 **캐릭터 모션을 재생시킬 수 없어** 두 리듬을 한 화면에서 비교하지 못한다 | plan §구성 "유닛은 바깥에서 구동" ↔ 모션 재생은 Set/이벤트 호출 필요 | E-S0-03로 선판정, 불가 시 D6 이월 명시 |
| **7** | **SFX 전량.** `sfx_draft.csv` 3행 전부 `SoundPath=NONE`, 오디오 애셋 0개. 옵토패스풍 타격감의 절반이 사운드다 | 애셋 부재 | I6 + 오디오 애셋 조달 |
| **8** | **라이브 카메라 컷과 FxLab 신규 컷의 동일성.** 블렌드 시간·뷰타겟·FOV가 다르면 S6에서 채택한 컷 룩이 라이브로 이전되지 않는다 | 매니저 삭제로 원본 코드 미사용 | E-S3-05 + D6에서 파라미터 대조 |
| **9** | **42행 전량 커버리지.** 스파이크는 7행. `62000200`(도약 3원자)은 7행 어디서도 참조되지 않아 임시 배정 없이는 아예 안 돈다 | 데이터 범위 | TC-S01 커버리지 + D6 42행 임포트 |
| **10** | **성능.** 3쿼드 × 8기 동시 + 4.15배 확대 + 블룸. 스파이크는 3기 순차라 최악 부하 미측정 | 시나리오 범위 | D6 8기 동시 캐스트 프로파일 |
| **11** | ★**`IsTargetFlinch` 타이밍의 이전 가능성.** 스파이크는 flinch 트리거 시점이 임의, 라이브는 `ResolveHit` 시점. **룩(무엇이 보이는가)은 이전되지만 타이밍(언제 보이는가)은 이전되지 않는다** | 판정 경로 부재 | TC-G05 / BPC-32 |
| **12** | **상태 누수의 턴 경계 검증.** 턴 큐가 없어 "턴 사이 리셋"이라는 실제 경계가 없다. S5 루프 모드가 부분 대체할 뿐 | 하드 스코프 | TC-S04(Override Clear) / BPC-17 |

> **한 줄 요약**: 이 스파이크는 **정지화면 품질(룩)**을 유효하게 판정하고, **시간 결합(타이밍·판정·사망)**은 판정하지 못한다. S6 산출물에 이 유효범위를 명시하지 않으면 [[2D배경_기각_교훈_2026-08-10]]의 "오프라인 판정 → 엔진에서 기각" 패턴이 한 층 위에서 재발한다.

---

## 9. 검토했고 통과시킨 축 (빈손 방지)

| 축 | 검증 방법 | 결과 |
|---|---|---|
| **CSV 무결성**(인용·쉼표·ragged) | 7개 CSV 파서 전수(`vfx`/`stagings`/`sfx`/`motions`/`skills_merged_42` draft + `motions.csv` + `skills.csv`) | **PASS** — quotes **0**, ragged **0**. BP정리 §5-2(vfx 인용 18개)·§5-3(motions 13행 필드 초과)이 **이미 수정 완료**됨을 실측 확인 |
| **`vfx_draft` ↔ 재고 조인**(TC-N03) | `SrcFile` 컬럼 조인 | **PASS** — 3/3 조인, `GridX`/`GridY`/`FrameCount` 전량 일치, `FrameCount==GridX` 3/3. TC-N03 선결(조인 키 부재) 해소 |
| **S2' 색 단일 테스트의 결정성** | `vfx_inventory_raw.csv` RowHues 대조 | **PASS** — index2=197.7°(시안) / 반전상 index6=273.8°(보라) / index0=52.0°(금색). 세 결과가 명확히 갈린다. **plan의 이 테스트는 타당하다** |
| **`ColorRow=4` 정정**(plan §데이터 정정) | RowHues 실측 | **PASS** — row0=52.0°(금색), row4=24.6°(적주황). **plan의 정정이 옳다** |
| **밀도 산술**(3.75 / 4.15) | CellPx=64 실측 × 쿼드 스케일 | **PASS** — 2.4×100/64=3.75, 6.48×64/100=4.147. Plane 100uu 전제 하에 자기일관 |
| **`.umap` 단일 파일 체크의 유효성** | `__ExternalActors__` 조회 | **PASS** — OFPA 대상은 `test` 맵뿐. `map_battle_octopath`는 모놀리식(3848264 B)이라 **바이트 비교가 유효**하다 |
| **S1 레벨 복제로 룩 조건 동일성 확보** | 논거 검토 | **PASS** — "새로 만들면 틀린 조명 아래 판정한다"는 논거 성립. 야간 HD2D 목업 기각 교훈과 정합 |
| **`advanceRatio` 관통 판정의 관측량 형태** | plan S3 판정 검토 | **PASS** — "이동비 0.50"은 로그 에코가 아니라 **실제 변위 비율**이라 TC-S02의 무음 실패 정의를 만족한다 |
| **원본 PNG 3장 실재** | `_RawAssets/vfx/Free/Part {1,13,14}` | **PASS** — S2'은 **소스 부재로 차단되지 않는다** |
| **신규 struct·DT 3종 엔진 반입** | `Content/Data/Structs/` · `Content/Data/` 실측 | **PASS** — `F_StagingsRow`·`F_VfxRow`·`F_SfxRow` + `DT_Stagings`·`DT_Vfx`·`DT_Sfx` 전부 실재(2026-08-11 10:07). **S2·S3의 데이터 선행조건은 이미 충족** |

---

## 10. 미확인 (추측 금지 — 조회 방법 명시)

| # | 항목 | 무엇을 조회하면 닫히는가 |
|---|---|---|
| **E-U1** | `/Engine/BasicShapes/Plane` 로컬 바운드 100uu (밀도 계산 전제) | MCP `get_actor_bounds` 1콜. plan 자신의 계산이 이미 자기일관이라 위험은 낮다 |
| **E-U2** | 플립북 구동 주체가 **머티리얼 `Time`인지 BP 스칼라 세팅인지** (히트스톱 품질의 선결) | `MI_FX_Smear` 파라미터 목록 1콜 → 그 뒤 E-S5-03이 닫는다 |
| **E-U3** | PIE 중 MCP `set_rows` 동작 여부 (실험대 상설화의 전제) | **E-S2-03이 닫는다** — plan은 이를 S5쯤에 두었으나 S2에서 닫아야 반복 비용이 안 커진다 |
| **E-U4** | 카메라 컷 신규 구현 수단 (`SetViewTargetWithBlend` 파라미터 = 라이브와 동일한가) | E-S0-01(15종) + `BP_BattleManager` 카메라 코드 **읽기 전용** 조회 |
| **E-U5** | `AlignSpriteToCamYaw` 외부 호출 가능성 + **2컴포넌트 정렬 범위** | **E-S0-03(b)** — gameplay G1로 대상이 2개임이 확인돼 난이도 상승 |
| **E-U6** | `PlayHurtReaction` 외부 호출 가능성 (FLINCH 축의 전제) | **E-S0-03(c)** |
| **E-U7** | 밀도 4.15가 진형 150uu에서 견딜 만한가 | **오너 육안 전용** — E-S6-03. 정직한 미확인 |

---

## 11. 관련

[[E_스파이크_plan]] · [[E0_에이전트피드백_Director판정]] · [[D3_TC_확정]] · [[D1_4슬롯구조_확정]] · [[D5.5_오너_struct세션_절차서]] · [[D4.5a_VFX재고_실측]] · [[D4.5b_VFX행배정]] · [[BP정리_통합명세_2026-08-11]] · [[전투BP_현황도_2026-08-11]] · [[2D배경_기각_교훈_2026-08-10]] · [[개발_워크플로우]]
