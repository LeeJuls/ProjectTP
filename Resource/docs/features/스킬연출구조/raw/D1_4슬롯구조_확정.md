---
type: design
project: projectTP
feature: 스킬연출구조
updated: 2026-08-11
status: PASS
status_note: 3차 개정(D4.5 실측 반영) — D5 대기
---

# D1 — 스킬 4슬롯 구조 확정서

> ## ★Director 판정 (2026-08-11) — 확정, 재론 금지
>
> D1이 올린 미결 3건에 답한다.
>
> | # | 사안 | 판정 |
> |---|---|---|
> | **N8** | `Layer=PROBE` 신설 | **채택.** 프루브 2행이 A2 1000행 생성 풀에 새는 것을 "F7b ⑧ 후 폐기"로 막는 건 약하다 — 잊으면 샌다. 1토큰으로 구조적 차단. 린트에 `Layer=PROBE → A2 생성 풀 제외`를 명시할 것 |
> | **N9** | 오디오 도메인 | **추가 없음.** `SFX_` 접두사가 §1-C에 이미 예약돼 있고 §2가 "도메인 애매하면 생략" → `SFX_Impact_Fire`로 성립. plan의 `Battle` 도메인 예고는 철회 |
> | **§7-2** | STUN 시 `ON_TURN_START` 미발동 | **D1 확정을 승인.** 행동성 훅은 행동 카운터가 물어야 하고, 패시브에 카운터가 하나라도 생기는 건 순이득이다. plan 본문의 *"STUN에도 패시브는 돈다"*는 **상시형에 한해서만 참**으로 부분 정정됨 |
>
> **추가 판정 (D4.5-a 실측 반영)**: `ColorRow`는 알파에서 **`0~5`만 허용**한다(row6~8은 파일별 계열 불일치 89~96%). §10 린트에 반영 필요.
>
> **미해결 이월**: **N3**(라이브 `F_SkillsRow` 필드 목록 — MCP 필요) · **N6**(`Kind=PASSIVE → 35xxxxxx` = 오너 확정 사항). D3가 게이트 차단 여부를 판정한다.

> ## ★3차 개정 인덱스 (2026-08-11, D4.5 실측 반영) — 어디가 바뀌었나
>
> D4.5-b(art-pipeline MCP 실측)와 D4.5-c(본 설계자)가 **2차 개정의 설계 배정 2건을 반증**했다. 원문은 취소선으로 보존한다.
>
> | # | 무엇이 틀렸나 | 어떻게 고쳤나 | 고친 절 |
> |---|---|---|---|
> | **Z1** | §3-6 표가 **치유에 `FxImpactId > 0`** 배정 — 라이브는 `Target=="ENEMY1"` 단일 게이트라 **ALLY 대상은 절대 재생 안 됨** | 치유 행 **`0`으로 정정**. 근거는 "라이브가 그러니까"가 아니라 ①알파 스코프 밖 ②쓸 이펙트가 타격 스파크뿐 ③미뤄도 비용 동일 | **§3-6 3차 개정** |
> | **Z2** | 임팩트 VFX와 flinch가 **같은 축인지 미판정** — 다르면 프로필 컬럼 +1 위험 | **다른 축으로 판정**. 단 **신규 컬럼 0** — flinch=`stagings.IsTargetFlinch`(연출층) / 임팩트=`skills.FxImpactId`(이펙트층)로 **이미 분리돼 있었고 묶여 있던 건 런타임 코드뿐**. **4행 유지** | **§3-6b 신설** · §11 C14 |
> | **Z3** | 표시지속 공식 `min(FrameCount/FPS, SlotBudgetSec)`이 **라이브와 다르다** — 라이브는 `− 0.05` 세이프마진을 쓴다(리터럴 `0.45`/`0.55`가 소수점까지 일치) | **`− FxSafetyMarginSec`(0.05) 계승.** 누락 시 라이브 5스킬 전부 **0.05s 증가 → MA-1 무음 회귀** | **§3-7 3차 개정** · §6-2 · R-9 L27 · §10 L19 · §11 C10 |
> | **Z4** | `FxMotionId` legacy 플레이스홀더 미해소 | **`63000100` 확정**(D4.5-b `vfx_legacy_smear`). 6행 전부 동일 | R-4 · §11 C13 |
>
> **Q4·Q5 닫힘**(R-10). 상세 근거 = [[D4.5b_VFX행배정]] · [[D4.5c_연출SFX행확정]].

> 상위: [[features/스킬연출구조/plan|plan]] · [[features/스킬연출구조/청사진|청사진]]
> 선행 확정: [[F7_스킬아키텍처_확정]](§5 레고·§8 구현) · [[balance1_포지션킷_v1]](30원자·포지션) · [[SPD원장_오라클_v1]](§0 고정입력) · [[모션연결_규칙안]] · [[네이밍_폴더_규약]] §4 · [[데이터_서버_규약]] §4 · [[UI_화면_규약]] §E·§G
> 이 문서는 plan의 **Director 판정 1~5를 전제로만** 쓴다(재론 없음). 답하는 것은 plan D1 행의 미해결 ①~⑤와 추가 산출물 4종이다.
#projectTP/스킬연출구조

---

## ★2차 개정 (2026-08-11) — qa Critical 4 + Director 실측 X5

> **원문은 취소선(`~~…~~`)으로 보존한다.** 판단 과정이 남아야 다음 사람이 같은 함정을 피한다.
> **Director 판정 5건은 확정이다. 본 절은 그 판정을 문서에 반영한 결과이며 재론이 아니다.**

### R-0. 개정 인덱스 (무엇이 바뀌었는가)

| 판정 | 무엇이 틀렸나 | 어떻게 고쳤나 | 고친 절 |
|---|---|---|---|
| **X5** | §5가 `data/skills.csv`(15컬럼)를 "라이브 DT 소스"로 잡았다 → **틀린 기준선** | 라이브 DT의 실질 소스는 **`skills_v2_draft.csv`(23컬럼)**. 병합 기준선 교체 + **값 소실 방지 절차 R-5** 신설. 42행 단일 SSOT 방향은 유지 | §5 전면 · R-5 |
| **X2·X3** | `Layer`를 struct 필드로 전제(§2-4가 `row.Layer`를 런타임에서 읽음) | **`#` 접두어 규약** — `#Layer`·`#PositionTag`·`#DevStatus`는 **CSV 전용, struct 편집 0**. 거절 가드는 **`MotionRow < 0`**으로 판정 | R-2 · R-3 · §2-4 · §5-3 |
| **X1** | 린트 L09가 라이브 `job_stats` **6/6행 FAIL** | L09 **적용 대상 = `characters.csv` 한정**. `job_stats.csv`는 **회귀 기준선 픽스처 → 면제 + 무접촉**. 린트표에 **「적용 대상」 열 신설** | R-6 · §10 |
| **X4** | "신규 문자열 키 전량 27종" | 실제 **81종**. Critical 아님 → **§12 B15로 D5 편입**, `strings.csv`↔`ST_UI` 싱크 갭 해소를 **선행조건**으로 명기 | §9-4 · §12 |
| **Y1** | "라이브 5스킬 이동·시간 무변" | **파이어볼에서 거짓.** 회귀 표에 정직 기록 + **의도된 변경**으로 판정(오너 육안 게이트) | R-7 · §4-2 |
| **Y2** | "투사체 = 신규 컬럼 0" | `FxProjectileId`(int)는 **신규 struct 필드**. `HitCount`(진짜 부활)와 **비용 분리 표기**. 4행 구조는 유지 | §3-1 |
| **Y3** | L19 `FrameCount ≤ GridX×GridY` | **`≤ GridX`로 정정** + `ColorRow` 린트 **L25 신설** | §10 |
| **Y4** | "병합 후 린트 전량 PASS" | **린트 3계층(T1/T2/T3) 분리** | R-6 · §5-4 · §10 |
| **Y5** | `Def ≤ +3` 가산 채널 부재 | **Def 패시브를 곱연산(`IN_MULT`)으로 재정의** — 신규 코드 0 유지 | R-8 · §12 B11 |
| **Y6** | `FxMotionId` 이관 매핑 미지정 | **17행 전수 이관 매핑표** 작성. 6행 전부 **동일 legacy VFX 행**을 가리킨다(무회귀) | R-4 · §6-5 |
| **Y7** | `bDebugProbesEnabled` 미정의 · F7b ⑧ 게이트 충돌 | **PROBE 런타임 가드 자체를 폐기.** `Layer=PROBE`의 의미는 **"A2 생성 풀 제외"뿐** → ⑧ 영향 **0**, 신규 멤버 0 | R-3 · §2-4 |
| **Y8** | "양 테이블 동일 2컬럼 계약" | 계약은 **3축**(`SkillIds` + `PassiveId` + **포지션 참조**). `job_stats`는 3축 중 1축 결손 → **레거시 픽스처**로 재분류. JOB 게이팅 린트 **L26 신설** | §2-2 · §10 |
| **Y9** | §3-4 "턴 길이 데이터화 금지" 자기모순 | 논거 **철회 후 재수립** — 턴 길이는 이미 데이터의 함수임을 인정하고 **턴 예산 계약 + 린트 L27**로 대체 | R-9 · §3-4 |
| **Y10** | `⑩b` 라벨 오기 | §5-2·§5-3을 **`⑩a`로 정정**(§11 C1은 원래 맞았다) | §5-2 · §5-3 |
| **Y11** | 오너 struct 세션 3건으로 셈 | **재집계 = 6건 · 1세션**(`#AuraFxId` 규약으로 `F_StatusEffectsRow` 1건 소거) | R-1 |
| **Y12** | `ResolveApproachAnchor` ALLY1 경로 | 앵커 해석에 **`Target` 분기 추가** + 린트 L12b | §4-2 |

---

### R-1. ★오너 struct 수동 편집 — 1회분 확정 【D4 게이트 조건】

> struct 편집은 **MCP 불가·오너 수동**이고, **편집 모달이 열리면 MCP 브릿지 전체가 마비**된다(함정㉓). **두 번 열게 하지 않는다.**

#### R-1-A. `F_SkillsRow` 최종 편집 목록 (이 표가 오너에게 전달되는 유일한 지시서)

라이브 23필드(Director MCP 실측) → **편집 후 19필드**. 추가 6 · 제거 10 · 유지 13.

| # | 필드명 | 조작 | 타입 | 기본값 | 사유 |
|---|---|---|---|---|---|
| 1 | `stagingId` | **추가** | `int32` | `0` | 연출 프로필 FK(§3-2). `0`=미지정 → 린트 L03 FAIL / 런타임 도달 시 `DefaultStagingId` 폴백 + 로그(E14) |
| 2 | `fxCastId` | **추가** | `int32` | `0` | 캐스트 VFX FK. `0` → `motions.fxMotionId` 폴백(§3-7) |
| 3 | `fxProjectileId` | **추가** | `int32` | `0` | 투사 VFX FK. `0` = 투사 없음(즉발). **폴백 없음** |
| 4 | `fxImpactId` | **추가** | `int32` | `0` | 임팩트 VFX FK. **폴백 없음 — fail-loud**(E17) |
| 5 | `sfxCastId` | **추가** | `int32` | `0` | 캐스트 SFX FK. 알파 = 전 행 `0`(슬롯만) |
| 6 | `sfxImpactId` | **추가** | `int32` | `0` | 임팩트 SFX FK. 알파 = 전 행 `0` |
| 7 | `castFX` | **제거** | `FString` | — | **라이브 실측 `""`**(3/3행). 정수 FK(#2)로 교체 |
| 8 | `projectileFX` | **제거** | `FString` | — | 〃 → #3. ★Y2: 이것이 "부활"이 아니라 **타입 변경 + 개명 + 신규 추가**인 지점 |
| 9 | `impactFX` | **제거** | `FString` | — | 〃 → #4 |
| 10 | `castSFX` | **제거** | `FString` | — | 〃 → #5 |
| 11 | `impactSFX` | **제거** | `FString` | — | 〃 → #6 |
| 12 | `powerRate` | **제거** | `float` | — | F7b **⑩a** 구컬럼. `skill_effects.csv` 이관 완료 + ⑩a 스톱 체크리스트 GREEN이 **선행** |
| 13 | `effectType` | **제거** | (미확인) | — | 〃 |
| 14 | `effectValue` | **제거** | `float` | — | 〃 |
| 15 | `effectDurationTurns` | **제거** | `int32` | — | 〃 |
| 16 | `effectChance` | **제거** | `float` | — | 〃 |
| — | `motionRow` | **무편집 (의미만 승격)** | `int32` | — | ★`-1`이 패시브 sentinel **겸 런타임 거절 가드의 판정 근거**로 승격(R-3). **타입·이름 무변 → 편집 0** |
| — | `category` | **무편집** | (미확인) | — | Director 실측: struct **실재** ∧ DT에 값 존재(`"ATTACK"`/`"DEFEND"`/`"MAGIC"`). CSV 컬럼만 승계 |
| — | `hitCount` | **무편집** | `int32` | — | 〃 실측값 `1`. §3-4 계약으로 **의미만 부여**(진짜 부활 — 비용 0) |
| — | `primaryTargetGroup` | **무편집** | (미확인) | — | 〃 실측값 `"SINGLE_ENEMY"`/`"SELF"`/`"SINGLE_ALLY"` |
| — | `nameKey`·`descKey`·`kind`·`target`·`cost`·`cooldownTurns`·`memo` | **무편집** | — | — | 존치 |
| — | `id` · `idTxt` | **무편집 (사문 필드)** | `int32`/`FString` | `0`/`""` | ★`Id`는 RowName으로 소비되고 `#id_txt`는 `#` 규약으로 미기입 → **영구히 값이 안 들어온다.** 제거해도 이득 0·편집 비용만 발생 → **존치하되 런타임 참조 금지**(R-2 주의) |

> **CSV-only 컬럼(struct 편집 0)**: `#Layer` · `#PositionTag` · `#DevStatus` — R-2 규약. **X2·X3이 요구한 struct 편집 3건이 0건이 된다.**

#### R-1-B. `Kind` / `Target` / `Category` 타입 — **미확인, D4에서 확정**

- **판정 근거 부족**: Director 실측 JSON은 `"kind": "PHYS"`로 **문자열 직렬화**되지만, UE는 `UENUM`을 DT→JSON 내보낼 때도 **enumerator 이름을 문자열로** 쓴다. 따라서 **직렬화 형태만으로 enum/FString이 갈리지 않는다.** 추측하지 않는다.
- **확정 방법**: `ObjectTools.list_properties(/Game/Data/F_SkillsRow)`의 **타입 필드(`PropertyClass` / `StrProperty` vs `ByteProperty`·`EnumProperty`)**. D4에서 1회 조회로 닫힌다.
- **분기 결과**:

| 타입 | 어휘 추가 대상 | 오너 세션 증분 |
|---|---|---|
| **FString** | 없음 — CSV 값만 바꾸면 된다 | **+0** |
| **enum** | `Kind += PASSIVE` · `Target += NONE` · `Category += NONE` | **최대 +3**(enum 애셋 3종 각 1회) |

- ★**타입과 무관하게 지금 확정하는 것**: 패시브 행의 `Category` 중립값을 ~~공란 `""`~~ → **`NONE` 토큰**으로 바꾼다. 사유: (a) enum이면 공란 표현 자체가 불가 (b) §1-1 `PassiveId`가 공란을 금지한 논거(*"없다"와 "안 채웠다"의 구별*)와 동형 (c) `EffectType=NONE`·`Target=NONE`이라는 **라이브 선례**가 이미 있다. → §5-3 어휘표·L02 반영.

#### R-1-C. 오너 수동 세션 재집계 — ~~3건~~ → **6건 · 1세션 권고**

| # | 애셋 | 조작 | 필요 사유 |
|---|---|---|---|
| 1 | `F_SkillsRow` | 추가 6 / 제거 10 | R-1-A |
| 2 | `F_JobStatsRow` | `+passiveId : int32 = 0` | 런타임이 읽는다(패시브 실행 §7) → CSV-only 불가 |
| 3 | `F_MotionsRow` | `+fxMotionId : int32 = 0` · `+sfxMotionId : int32 = 0` · `−isAttackFamily` | 런타임 소비(§3-8·R-4) |
| 4 | `F_StagingsRow` | **신규** | §3-2 |
| 5 | `F_VfxRow` | **신규** | §6-2 |
| 6 | `F_SfxRow` | **신규** | §6-3 |
| ~~7~~ | ~~`F_StatusEffectsRow` (`+AuraFxId`)~~ | **삭제 — 불필요** | ★`AuraFxId`는 알파에서 **전 행 0 · 소비 노드 0의 순수 예약**이다. R-2 규약을 적용해 **`#AuraFxId`로 CSV에만 예약**하면 struct 편집이 사라진다. 베타에서 실제로 소비할 때 `#`를 떼고 그때 편집한다 |

- **총 6건. 단 "6세션"이 아니라 "1세션 안의 6애셋"으로 처리한다.** 함정㉓ 비용은 *세션 수*에 비례하지 실제 편집 수에 비례하지 않는다.
- **세션 진입 조건 (둘 다 GREEN)**: ① **D4.5 완료**(`stagings`/`vfx`/`sfx` 행·스키마가 실데이터로 확정된 뒤에만 신규 struct 3종을 만든다 — 먼저 만들면 컬럼 수정으로 2세션이 된다) ② **F7b ⑩a 스톱 체크리스트 GREEN**(#12~16 구컬럼 제거가 ⑩a 수술 자체다).
- **귀결(plan §구현순서 4 정합)**: D6의 struct 의존 작업 **C1·C2·C3·C4·C5는 "⑦ 이후"가 아니라 "⑩a 이후"**다. §11 전제문 정정 완료.
- **세션 중 MCP 금지**: 그 시간대에 MCP 의존 작업(verifier 실증, DataTable 조회)을 스케줄하지 않는다. Director 스케줄 항목.
- **조건부 +3**: R-1-B가 enum으로 밝혀지면 같은 세션에 enum 3종을 붙인다(세션 수 불변).
- **미확인**: `DT_Stagings`/`DT_Vfx`/`DT_Sfx` **DataTable 애셋 생성**이 MCP로 가능한지 여부. 가능하면 오너는 struct 6건만 하면 된다. → D4/D6에서 `AssetTools` 계열 toolset 조회로 확정.

---

### R-2. ★`#` 접두어 규약 — CSV 전용 컬럼 (X2·X3 해소)

**실증(Director MCP)**: CSV의 `#id_txt` 컬럼이 있는데 struct `idTxt`는 `""`다. → **`#` 접두어 컬럼은 임포터가 struct에 기입하지 않는다.** (부수 확인: 첫 컬럼 `Id`는 RowName으로 소비되어 struct `id`도 `0`이다.)

| 규약 | 내용 |
|---|---|
| **정의** | 컬럼명이 `#`로 시작하면 **오프라인 전용**이다. 린트·A2 생성기·사람이 읽고, **struct에도 런타임에도 존재하지 않는다** |
| **적용 대상** | `#id_txt`(기존 관례) · **`#Layer`** · **`#PositionTag`** · **`#DevStatus`** · **`#AuraFxId`**(status_effects, 베타까지 예약) |
| **무음 손실 아님** | 임포터가 값을 버리는 것이 **의도된 설계**다. 라이브가 이미 `#id_txt`로 이 관례를 쓰고 있다 |
| **금지** | `#` 컬럼을 런타임에서 읽는 설계 **금지**. 읽어야 하면 `#`를 떼고 struct 편집 세션에 올린다 |
| **자기모순 해소** | plan 판정1-C1이 *"`Layer`는 오프라인 생성·검증 전용"*이라고 못 박았는데 §2-4가 런타임에서 읽었다. **원문이 틀렸고, 이 규약이 원칙을 복원한다** |

> ⚠ **TC-N04 판정 기준 정정 요청(qa 인계)**: 원안은 *"임포트 로그에 `not a member` 경고 **0건**"*이었다. `#` 컬럼은 **정확히 그 경고를 발생시킬 수 있다.** 올바른 기준은 **`set(경고 컬럼) == set(선언된 CSV-only 컬럼)`(집합 동일)**이다. 0건을 요구하면 정상 설계가 FAIL한다.
> **미확인**: UE DT 임포터가 미매칭 컬럼에 대해 경고를 내는지·문구가 무엇인지 → **D6 첫 임포트 Output Log 1회로 확정**(추측 금지).

---

### R-3. ★거절 가드 재설계 — `Layer` 참조 폐기, `MotionRow < 0` 판정

#### R-3-A. 판정 근거의 정당성 (왜 `MotionRow`인가)

| 근거 | 내용 |
|---|---|
| 이미 struct에 있다 | 편집 0 |
| plan 판정1-C3의 중립값 | `MotionRow = -1`은 **이미 확정된 패시브 sentinel**(0 금지 — row0=Idle이라 idle 재생 지시가 된다) |
| 패시브의 정의적 특징 | 패시브는 재생할 모션이 없다. 우연한 상관이 아니라 **개념적 동치** |
| **양방향 불변식이 성립한다** | L02: `Layer==PASSIVE → MotionRow == -1` / L03: `Layer ∈ {BASIC,COMMON,JOB,PROBE} → MotionRow ∈ motions.RowIndex`(실측 `0..16`, 전부 ≥ 0) → **`MotionRow < 0` ⟺ `Layer == PASSIVE`** |
| fail-closed | 범위 밖 음수도 거절된다(`< 0`이지 `== -1`이 아니다) |

> ★**이 가드의 정확성은 린트 L02·L03에 의존한다.** 따라서 두 규칙의 `MotionRow` 절은 **T1(구조) 계층으로 승격**해 병합 직후 검증한다(§10). 값 배정(D5)까지 미루면 그 사이 임포트가 가드를 무력화한다.

#### R-3-B. PROBE 런타임 가드 — **폐기** (Y7 해소)

- `#Layer`가 CSV 전용이므로 **런타임은 `PROBE`를 알 수 없다.** 알 필요도 없다.
- ★**`Layer=PROBE`의 의미는 「A2 1000행 생성 풀에서 제외」 **하나뿐**이며, 런타임 선택 차단이 아니다.**
- **귀결**: (a) `bDebugProbesEnabled` **신규 멤버 불필요** — 목록은 `PassiveReentryDepth` 1개 그대로 (b) 프루브 2행은 **F7b ⑧ E2E 게이트에서 평소대로 선택·실행된다 → ⑧ 영향 0** (c) 3중 차단은 **2중**(A2 풀 필터 + L10)으로 축소되고, 그것으로 충분하다(프루브 2행은 어차피 `SkillIds`에 없으므로 메뉴에 뜨지 않는다).

---

### R-4. ★`IsAttackFamily` → `FxMotionId` 이관 매핑표 (Y6 해소 — 17행 전수)

**현행 실측(`data/motions.csv`)**: `IsAttackFamily == TRUE` = RowIndex **5,6,7,8,9,10**(Atk1/Atk2/Atk3/AirAtk1/AirAtk2/Casting1) 6행. 나머지 11행 FALSE.

★**결정적 사실**: 현행 불리언은 **6행 전부에 동일한 잔상 FX(라이브 `T_FX_Smear`)를 낸다.** 따라서 **무회귀 이관 = 6행이 전부 같은 VFX 행을 가리키는 것**이다. 방향별 차별화(Atk2=세로 잔상 등)는 **이관이 아니라 신규 기능**이므로 분리한다.

| RowIndex | `#id_txt` | 현행 `IsAttackFamily` | **`FxMotionId` 이관값** | `SfxMotionId` | 근거 |
|---|---|---|---|---|---|
| 0 | motion_idle1 | FALSE | **0** | 0 | 무변 |
| 1 | motion_idle2 | FALSE | **0** | 0 | 무변 |
| 2 | motion_run1 | FALSE | **0** | 0 | 무변 |
| 3 | motion_run2 | FALSE | **0** | 0 | 무변 |
| 4 | motion_jump | FALSE | **0** | 0 | ★도약 프로필이 이 행을 `ApproachMotionRow=4`로 **처음 소비**하지만 **잔상은 없다**(현행 FALSE 보존). 접근 방식과 잔상은 다른 축 |
| **5** | motion_atk1 | **TRUE** | **`<VFX_SMEAR_LEGACY>`** | 0 | 라이브 5스킬 중 기본공격·강타가 쓰는 행 |
| **6** | motion_atk2 | **TRUE** | **`<VFX_SMEAR_LEGACY>`** | 0 | 현행과 동일 FX |
| **7** | motion_atk3 | **TRUE** | **`<VFX_SMEAR_LEGACY>`** | 0 | 〃 |
| **8** | motion_airatk1 | **TRUE** | **`<VFX_SMEAR_LEGACY>`** | 0 | 〃 |
| **9** | motion_airatk2 | **TRUE** | **`<VFX_SMEAR_LEGACY>`** | 0 | 〃 |
| **10** | motion_casting1 | **TRUE** | **`<VFX_SMEAR_LEGACY>`** | 0 | ★파이어볼. **잔상은 유지하고 전진만 제거**한다 — ~~이 한 행이 §3-8의 2축 분해가 실제로 필요한 이유의 증거다~~ → **정정(2026-08-12, [[전진로직_실체_확정]])**: 전진(`Target` 3-way)과 잔상(`IsAttackFamily`)은 애초에 하나의 게이트가 아니라 **서로 다른 두 개의 독립된 legacy 게이트**였다 — 이 행은 "게이트 분해가 필요하다"의 증거가 아니라 **"두 게이트를 각각 독립 데이터 컬럼(`AdvanceRatio`/`FxMotionId`)으로 옮겨야 값을 따로 조정할 수 있다"**는 증거다. 결론(2컬럼 필요)은 불변 |
| 11 | motion_casting2 | FALSE | **0** | 0 | 파트3 결정②(b) 보존 — **치유 잔상 0** |
| 12 | motion_hurt | FALSE | **0** | 0 | 무변 |
| 13 | motion_dying | FALSE | **0** | 0 | 무변 |
| 14 | motion_dash | FALSE | **0** | 0 | 무변 |
| 15 | motion_block | FALSE | **0** | 0 | 무변 |
| 16 | motion_roll | FALSE | **0** | 0 | 무변 |

**TC-N07 판정 기준 (강화판 — qa 인계)**: `FxMotionId > 0` 행 집합 == `{5,6,7,8,9,10}` ∧ **그 6행의 값이 전부 동일** ∧ 나머지 11행 == `0`. 6행 중 하나라도 값이 다르면 **이관이 아니라 신규 기능이 섞인 것**이므로 FAIL.

> **★폐기(2026-08-12)**: 오너의 오버레이 폐지 결재로 이 TC의 검증 목적이 소멸했다 — 전문은 `D3_TC_확정.md` TC-N07 항목 참조. 대체 불변식: *"알파에서 `motions.FxMotionId` 전 행 `0`"*([[내장트레일_director판정]]).

#### R-4-B. ★`vfx.csv`에 **legacy 2행이 반드시 필요하다** (신규 발견)

`<VFX_SMEAR_LEGACY>`는 D4.5의 180장 PNG(GridY=9) 유래 행이 **아니다.** 라이브 `T_FX_Smear`/`T_FX_Hit`은 **GridY=1의 별도 제작물**이다(gameplay 실측). 신규 아틀라스 행을 가리키면 **첫 임포트에서 잔상 룩이 바뀐다 = 무음 회귀.**

| 행 | `TexPath` | `ColorRow` | `GridX` | `GridY` | `FrameCount` | `FPS` |
|---|---|---|---|---|---|---|
| `vfx_legacy_smear` | 라이브 `T_FX_Smear` | `0` | **5** | **1** | **5** | **[D4.5 역산]** |
| `vfx_legacy_hit` | 라이브 `T_FX_Hit` | `0` | **7** | **1** | **6** | **[D4.5 역산]** |

- `FPS` 역산식: **`FPS = FrameCount / 현행 재생시간`**. 현행 Smear는 Delay 리터럴 `0.45`(plan 실측)로 보이나 **그것이 재생 지속인지 후속 지연인지 미확인** → D4.5에서 `BP_BattleSpawnPoint` 해당 핀을 조회해 확정한다. **추측으로 채우지 않는다.**
- 게이트: legacy 2행의 `FrameCount/FPS`가 현행 재생시간과 일치해야 **MA-1 재생 원장 diff 0**(TC-G07)이 성립한다.
- **미확인**: 라이브에서 **임팩트 FX(`HitMID`)가 어느 스킬에 재생되는가**(막기·치유 포함 여부). §3-6 표는 이를 **설계 배정**으로 적었을 뿐 실측이 아니다. → D4.5에서 `ResolveHit`/`BP_BattleSpawnPoint`의 `HitMID` 소비 분기 조회로 확정. 불일치 시 §3-6의 `FxImpactId` 열을 정정한다.

---

### R-5. ★재임포트 값 소실 방지 절차 (X5의 핵심)

**문제**: 라이브 DT에는 `category`/`hitCount`/`primaryTargetGroup` **값이 들어 있는데** `data/skills.csv`(15컬럼)엔 그 컬럼이 **없다.** 그 파일로 재임포트하면 3필드가 **전부 기본값으로 리셋**된다. 지금은 소비 노드 0이라 무증상이지만, §3-4가 `HitCount`를 소비하기로 확정한 순간 터진다.

| # | 규칙 | 사유 |
|---|---|---|
| **P1** | **`data/skills.csv`(15컬럼)를 `DT_Skills`에 임포트하는 것을 금지한다.** 병합 완료 전까지 이 파일은 **읽기 전용 이력**이다 | 3필드 무음 리셋 |
| **P2** | 병합 산출물은 **`data/drafts/skills_merged_42.csv`**(작업 파일)로 만든다. `data/skills.csv` 덮어쓰기는 **F7b ⑩a GREEN + struct 세션 완료 이후**에 rename으로 승격 | ⑩a 절차가 `data/skills.csv`를 재임포트할 경우 **라이브 DT에 37행이 무단 유입**되는 사고를 차단(fail-closed). ※⑩a가 실제로 재임포트하는지는 **미확인 — F7b 인계 문서 ⑩a 절차 조회로 확정** |
| **P3** | **임포트 전 스냅샷**: `DataTableTools.get_rows(DT_Skills)` 전 행 JSON을 `raw/DT_Skills_preimport_<date>.json`으로 보관 | 유일한 롤백 경로 |
| **P4** | **임포트 전 대조**: 병합 CSV의 LIVE 5행 + 프루브 2행 × (23필드 − `#`컬럼 − `id`/`idTxt`)를 P3 스냅샷과 **필드 단위 대조**. 불일치 0이어야 임포트 허가 | 값 소실은 임포트 **후엔 못 잡는다** |
| **P5** | **임포트 후 재대조**: 같은 7행의 `category`·`hitCount`·`primaryTargetGroup`이 P3과 동일한지 재확인 | 임포터 동작 실증 |
| **P6** | DT 임포트는 **struct 세션(R-1-C) 이후 1회만**. 그 전엔 CSV 파일만 존재하고 라이브 DT는 무접촉 | 재임포트 0회 = 값 소실 0 |

> ★**gameplay 보고 정정**: *"스키마는 이미 있고 값 전부 공란"*은 **FX/SFX 5필드(`castFX`·`projectileFX`·`impactFX`·`castSFX`·`impactSFX`)에만 해당**한다. `category`·`hitCount`·`primaryTargetGroup` **3필드는 값이 채워져 있다**(Director 실측 3/3행). plan §실태조사 ①의 *"값 전부 공란"* 서술도 같은 범위로 읽어야 한다.

> **미확인 — D4에서 닫는다**: `DataTableTools.get_row_names(/Game/Data/DT_Skills)`. 반환에 **`31002000`·`31003000`(프루브 2행)이 포함되면 X5(소스 = `skills_v2_draft.csv`)가 확정**된다. 5행뿐이면 "누군가 3필드를 수동 입력했거나 v2_draft의 LIVE 5행만 임포트했다"는 다른 경로이므로 병합 대조 범위가 7행 → 5행으로 줄어든다.

---

### R-6. ★린트 재편 — 「적용 대상」 열 신설 + 픽스처 면제 + 3계층 (X1·Y4)

#### R-6-A. 픽스처 면제 레지스트리 (신설 — 이게 없어 X1이 났다)

| 파일 | 지위 | 면제 규칙 | 사유 | 승인 |
|---|---|---|---|---|
| **`data/job_stats.csv`** | **F7b ⑦ 5스킬 회귀의 기준선 픽스처 — 무접촉** | **L09** · **L26** · **L08** | L09를 만족시키려면 라이브 `SkillIds`를 고쳐야 하고, 그러면 **막기(33001000)·치유(34001000)가 유닛 메뉴 밖으로 밀려 F7b ⑦ 5스킬 회귀가 깨진다.** 회귀 보호가 스키마 순정성보다 우선한다. L08은 **포지션 컬럼 자체가 없어 평가 불가**(Y8) | **Director 2026-08-11** |
| 〃 | 면제되지 **않는** 규칙 | **L07a · L07b · L10 · L09f** | `PassiveId` 축은 신규라 픽스처 보호 대상이 아니다(전 행 `0` → 자동 PASS). L10·L09f는 무접촉으로도 통과한다 |

- **신설 L09f (픽스처판 L09)**: `SkillIds` = 정확히 3토큰 ∧ 각 토큰이 `skills42`에 존재. **순서 강제 없음.** → **현재 6/6 PASS**(전사 `31000000;31001000;33001000` · 마법사 `31000000;32001000;34001000` 6토큰 전부 42행에 존재). 무접촉으로 오타 방어만 유지한다.
- **면제는 파일이 픽스처를 벗어나면 소멸한다**: A2가 `characters.csv`를 만드는 순간 `job_stats.csv`는 회귀 픽스처 외 용도가 없어지고, 그때 L09/L26 전면 적용 대상이 되는 것은 `characters.csv`뿐이다.

#### R-6-B. 린트 3계층 (Y4 — §5-4 step7 "전량 PASS" 자기모순 해소)

| 계층 | 언제 돌리는가 | 무엇을 검증하는가 | 미충족 시 |
|---|---|---|---|
| **T1 — 구조** | **병합 직후(D4.5)** | 컬럼셋 일치 · Id 유일 · 어휘 토큰 · 토큰 수 · **`MotionRow` 부호 불변식(L02/L03의 해당 절)** | 병합 실패 — 되돌린다 |
| **T2 — 참조 무결성** | **D5 완료 시** | FK 존재 · 문자열 키 존재 · 포지션 게이팅 · 범위 | 값 배정 미완 |
| **T3 — 완전성·예산** | **D6 임포트 직전** | 값이 다 찼는가 · 밸런스 상한 · **턴 예산(L27)** | **임포트 금지** |

> ★**`MotionRow` 부호 불변식이 T1인 이유**: R-3-A 거절 가드의 정확성이 여기 걸려 있다. D5까지 미루면 그 사이 임포트가 가드를 무력화한다. **L02·L03을 통째로 T1에 넣는 것이 아니라 `MotionRow` 절만 T1로 분리**한다(`StagingId>0`·`Category` 채움은 D5 소관이므로 T3).

---

### R-7. ★회귀 정직 기록 — 라이브 5스킬 (Y1 해소)

~~"라이브 5스킬 이동 거리·시간 무변"~~ 은 **거짓이었다.** 실제:

| 스킬 | 현행 | D1 스키마 후 | 이동 | 잔상 | 카메라 | **판정** |
|---|---|---|---|---|---|---|
| 기본공격 31000000 | MotionRow 5(AF=T) 전진 | `62000100` `AdvanceRatio=1.0` | **무변** | **무변**(R-4 row5) | 무변 | 회귀 0 |
| 강타 31001000 | MotionRow 5(AF=T) 전진 | `62000100` | **무변** | **무변** | 무변 | 회귀 0 |
| **파이어볼 32001000** | **MotionRow 10(AF=T) → 전진 + 잔상** | `62000300` `AdvanceRatio=0.0` | ★**전진 → 제자리(변경)** | **유지**(R-4 row10) | ★**[미확인]** | **의도된 변경** |
| 막기 33001000 | SELF 분기 · run 1프레임 스침 | `62000400` `AdvanceRatio=0.0` | 제자리(스침 해소) | 없음(row15 FALSE) | 무변 | **의도된 개선** |
| 치유 34001000 | ALLY1 분기 제자리 | `62000400` | 무변 | 없음(row11 FALSE) | 무변 | 회귀 0 |

> ★**정정(2026-08-12, [[전진로직_실체_확정]])**: 위 표의 "MotionRow N(AF=T) 전진" 및 파이어볼 행의 "→ 전진 + 잔상" 표기는 **인과가 뒤바뀐 표현**이다. 전진 여부는 `IsAttackFamily`(AF)가 아니라 `BP_BattleManager`의 `Target` 문자열 3-way 분기(`ALLY1`/`SELF` 예외 외 전부 전진)가 결정한다. `AF=T`는 **잔상 표시 여부**만 결정한다. 기본공격·강타·파이어볼이 전진하는 이유는 셋 다 `Target=ENEMY1`이라서지 `AF=T`라서가 아니다. **표의 판정(회귀 0 / 의도된 변경)은 그대로 유효**하다.

**파이어볼 판정 = 회귀가 아니라 의도된 변경.** 근거: plan §연출 프로필이 *"파이어볼조차 지금은 **걸어가서 때린다**"*를 **결함으로 실측 기록**했고, ③ 연출층 분리의 목적 자체가 그것을 고치는 것이다. 단 다음 3건을 **게이트로 강제**한다.

| # | 강제 항목 |
|---|---|
| **G-1** | 이동 거리 배선 후 **0 uu** 실측(TC-N08 (a)) — 0이 아니면 배선 실패 |
| **G-2** | 캐스트→`ResolveHit` 경과 시간 **델타를 수치로 기록**. **0이 아닌 것이 정상**이며, 이 값은 **MA-1(TC-G07) 유효범위 밖**이다 — MA-1 PASS를 "턴 길이 무변"의 근거로 쓰면 **가짜 GREEN** |
| **G-3** | **오너 육안 최종 승인**(제자리 시전이 "멋진가") — 미적 판단은 오너 소관 |

**[미확인] 카메라 컷 방향**: 현행 `IfThenElse_6 = OR(Target != "ENEMY1", dist < 1.0)`의 **True/False 핀 중 어느 쪽이 컷인지 미조회**. → `BP_BattleSpawnPoint`의 `IfThenElse_6` 출력 핀 연결 대상을 조회하면 확정. **D4~D6 조회 항목**(추측 금지).

---

### R-8. ★`Def` 패시브 — **곱연산으로 재정의** (Y5 해소)

**문제**: `status_effects.csv` `Channel` 실측 어휘는 **`SKIP_TURN` · `DOT` · `OUT_MULT` · `IN_MULT` 4종뿐**이고 **가산 채널이 없다.** §12 B11의 `Def ≤ +3`(가산)은 **표현할 데이터가 존재하지 않는다.** §7-3의 *"신규 코드 0"*은 **Atk % 계열에만 참**이었다.

**확정: Def 패시브를 `IN_MULT` 곱연산으로 재정의한다.** 가산 채널을 신설하지 않는다.

| 항목 | 확정 |
|---|---|
| 표현 | `Family=SELF_BUFF` · `Channel=IN_MULT` · `StatTarget=DEF` · `DefaultValue=<음수 float>` · `DefaultDuration=-1`(영구) |
| **선례** | 라이브 `DMG_REDUCE`(막기)가 **정확히 같은 형태**(`SELF_BUFF/IN_MULT/DEF/-0.5/1`)다 → **신규 코드 0이 실제로 참이 된다** |
| 의미 | `DefaultValue = v` → 수신 피해 배율 `×(1+v)`. `v=-0.05` → `×0.95` |
| 린트 L23 정정 | ~~`Def` 가산 ≤ +3~~ → **`Layer==PASSIVE` 행이 참조하는 `IN_MULT` 값의 합 ≥ `PassiveInMultFloor`**(하한, D5가 채움) |
| ★balance 인계 제약 | **§7 검산표 총합 불변**을 유지하려면 "스탯 이전" 원칙을 곱연산에 맞게 재유도해야 한다: ~~`탱 Def 11 → 8, 패시브 +3`~~ → **`탱 Def 11 → X, 패시브 IN_MULT v`로 유효 피격량이 현행과 같아지는 `(X, v)`를 산출**. 이건 수치 작업이므로 **D5 소관** |
| ★상호작용 경고 | 영구 `IN_MULT` 패시브는 **막기(`×0.5`)와 곱해진다**(`×0.95 × ×0.5 = ×0.475`). 막기 하드카운터가 **강화**되므로 상성 B 검산에 반드시 반영할 것 |

**기각한 대안(비용 명기)**: 가산 채널 `FLAT_DEF` 신설 = ① `Channel` 어휘 +1 ② 데미지 공식의 방어 항에 삽입 지점 신설 ③ BP `switch` case +1 = **plan 판별기준 2의 "새 축" 영역** ④ §7 검산표 전면 재유도. **알파에서 이 비용을 지불할 이유가 없다.**

---

### R-9. ★§3-4 논거 재수립 — "턴 길이 데이터화 금지"는 철회한다 (Y9 해소)

~~*"`HitIntervalSec`가 스킬마다 다르면 턴 길이가 흔들려 SPD 원장에 노이즈"*~~ — **이 논거는 자기 설계와 충돌하므로 철회한다.** 사실:

> **턴 길이는 이미 데이터의 함수다.** `motions.FrameCount`(라이브), `skills.HitCount`(§3-4), `vfx[FxProjectileId].FrameCount/FPS`(§3-7), `vfx[FxImpactId]` — 넷 다 CSV 값이 턴 길이를 결정한다.

**대체 원칙 2개 (이게 진짜 이유다)**:

| # | 원칙 | 귀결 |
|---|---|---|
| **T-1** | **리듬 상수는 "축"이지 "행"이 아니다.** 간격·예산 같은 값은 전 스킬 공통 리듬이며, 스킬마다 다르게 하면 **balance가 `HitCount × HitIntervalSec` 2변수를 동시 조율**해야 하고 상한 린트가 곱 부등식이 된다(손검산 불가). **1변수(`HitCount ≤ MaxHitCount`)로 유지**하는 것이 상한을 기계 검증 가능하게 만든다 | `HitIntervalSec`·`ProjectileFlightBudgetSec`·`SlotBudgetSec` = **명명 상수(데이터 멤버)** 유지 |
| **T-2** | **턴 길이는 자유롭게 변해도 되지만, 상한이 기계로 강제돼야 한다.** 금지해야 할 것은 "변동"이 아니라 "**예측 불가능한 상한**"이다 | **턴 예산 계약 + 린트 L27 신설**(아래) |

**턴 예산 계약 (명문)**

```
EstimatedTurnSec(skill) =
      접근시간(AdvanceRatio > 0 ? ApproachSec : 0)
    + 캐스트FX지속   ( FxCastId>0      ? min(vfx.FrameCount/FPS, SlotBudgetSec) − FxSafetyMarginSec : 모션지속 )
    + 투사지속       ( FxProjectileId>0? min(vfx.FrameCount/FPS, ProjectileFlightBudgetSec) − FxSafetyMarginSec : 0 )
    + (HitCount − 1) × HitIntervalSec
    + 임팩트FX지속   ( FxImpactId>0    ? min(vfx.FrameCount/FPS, SlotBudgetSec) − FxSafetyMarginSec : 0 )
    + 복귀시간(AdvanceRatio > 0 ? ApproachSec : 0)
  ≤  SlotBudgetSec
```

> ★**3차 개정**: FX 3항에 **`− FxSafetyMarginSec`(초기값 0.05)** 추가 — §3-7 3차 개정. 라이브 실측 공식 계승이며, 이를 빼면 라이브 5스킬 전부에서 표시시간이 0.05s씩 길어져 **MA-1 diff가 무음으로 깨진다.** 예산 검증은 **완화 방향**(안전측)으로 이동한다.

- **L27**(신설, T3): 위 부등식을 전 액티브 행에 대해 기계 검증. 초과 행이 있으면 **임포트 금지** + 위반 행·초과량 출력.
- 상수 실값(`SlotBudgetSec`·`HitIntervalSec`·`ProjectileFlightBudgetSec`·`ApproachSec`)은 **D6 실측**. 그 전까지 L27은 **정의만 존재하고 실행 불가**로 표기한다(가짜 GREEN 방지).
- `MaxHitCount`는 **독립 상수가 아니라 L27의 귀결**이다: 최악 케이스(투사 최장 + 임팩트 최장)에서 부등식을 만족하는 최대 `HitCount`. → §10 L20의 "값 미확정"이 **유도식을 얻는다**.

---

### R-10. 아직 남은 미확정 (추측 금지 — 조회 방법 명시)

| # | 항목 | 어디서 닫는가 |
|---|---|---|
| **Q1** | `Kind`·`Target`·`Category`의 타입(FString vs enum) | **D4** — `ObjectTools.list_properties(/Game/Data/F_SkillsRow)` 타입 필드. enum이면 오너 세션 애셋 +3 |
| **Q2** | `DT_Skills` 행 이름 전량(5행인가 7행인가) | **D4** — `DataTableTools.get_row_names`. 프루브 2행 포함 시 X5 완전 확정 |
| **Q3** | `IfThenElse_6` True/False 핀 중 카메라 컷 방향 | **D4~D6** — `BP_BattleSpawnPoint` 노드 출력 핀 연결 대상 |
| ~~**Q4**~~ | 라이브에서 임팩트 FX(`HitMID`)를 재생하는 스킬 범위 | ✅ **닫힘 — D4.5-b.** `skills.Target=="ENEMY1"` **단일 문자열 게이트**(`BP_BattleManager` EventGraph, 호출 지점 1곳). ENEMY1 5행 재생 / 막기(SELF)·치유(ALLY1) 미재생 → **Z1·Z2 유발** |
| ~~**Q5**~~ | 현행 Smear/Hit 재생 지속시간(0.45가 재생인가 지연인가) | ✅ **닫힘 — D4.5-b.** `0.45`는 **표시 지속시간**(`SetVisibility(true)→Delay→false`). `FrameCount/FPS − 0.05`와 소수점 일치 ∧ MI `FPS=10` 직접 조회 = **이중 실증** → **Z3 유발** |
| **Q6** | F7b ⑩a 절차가 `data/skills.csv`를 재임포트하는가 | **D4** — F7b 인계 문서 ⑩a 절차. P2의 완화 여부 결정 |
| **Q7** | DT 임포터의 미매칭(`#`) 컬럼 경고 유무·문구 | **D6** — 첫 임포트 Output Log |
| **Q8** | `DT_Stagings`/`DT_Vfx`/`DT_Sfx` 애셋 생성이 MCP로 가능한가 | **D4~D6** — `AssetTools` 계열 toolset 조회. 가능하면 오너 작업은 struct 6건만 |
| **Q9** | `strings.csv` ↔ `ST_UI` 싱크 갭 원인 | **F7b 이월 중** — 81키(§12 B15) 착수 **선행조건** |
| N1·N2·N4·N5 | 기존 미확인 (§13) | 변동 없음 |

---

## 0. 결론 요약 (Director용 5줄)

| # | 답 |
|---|---|
| **①** | `PassiveId`는 **int32, `0` = NONE**(공란 금지·린트 FAIL). 4슬롯 = **"액티브 3 필수 + 패시브 1 선택"**. `PassiveId==0` 유닛은 패시브 경로에 **진입 자체를 안 한다**(호출 0·로그 0·상태 0). [[SPD원장_오라클_v1]] §0에 행 추가 완료 |
| **②** | 슬롯 조달 SSOT = **유닛 행 테이블**(현 `job_stats.csv` → A2 이후 `characters.csv`). `position_stats.csv`는 **스탯 전용·슬롯 미보유**(포지션 3행이 1000캐릭터의 슬롯을 소유할 수 없다). ~~스키마 계약 = `SkillIds`(3토큰) + `PassiveId`(int) 2컬럼, 양 테이블 동일~~ → **2차 개정**: 계약은 **3축**(`SkillIds` + `PassiveId` + **포지션 참조**)이고 `job_stats`는 3축 중 포지션이 결손이라 **레거시 픽스처**다(§2-2 v2 · R-6) |
| **③** | 연출 프로필 = **`stagings.csv` 4행**. 투사체는 프로필 축이 **아니라 이펙트층 축**(`FxProjectileId`) → 신규 행 0 / 도약은 접근 파라미터 2컬럼(`ApproachMotionRow`·`ApproachArcHeight`) → 신규 행 1 / 멀티히트는 `HitCount` **부활 + 연출 전용 계약** → 신규 행 0·컬럼 0. ★**2차 개정**: "신규 컬럼 0"은 `HitCount`에만 참이고 `FxProjectileId`는 **신규 struct 필드**다(§3-1 v2). **행 수 주장(4행)은 유효**(qa 30/30 실증) |
| **④** | **(a) 실제 Lerp 알파로 소비** 확정. 정의 = *"캐스터 → **접근종점** 벡터의 보간 비율"*, 범위 `[0,1]` 임포트 검증. 종점 = `ResolveApproachAnchor()`(SELF는 자기 자신 → 벡터 0 → 자동 제자리). 컬럼명 `AdvanceRatio` **유지**(문서 드리프트 회피) |
| **⑤** | **42행 단일 SSOT로 3분기 병합**(액티브 30 + 패시브 10 + 프루브 2). ~~`data/skills.csv`가 라이브 DT 소스이므로 그것을 기준선으로 삼는다~~ → **2차 개정(X5)**: 라이브 DT의 실질 소스는 **`skills_v2_draft.csv`(23컬럼)**다. `data/skills.csv`(15컬럼)로 재임포트하면 `category`·`hitCount`·`primaryTargetGroup` 값이 **전부 리셋**된다 → **P1~P6 값 소실 방지 절차(R-5) 필수**. 최종 SSOT 파일명은 `data/skills.csv` 유지(승격은 ⑩a 이후) |

**추가 산출물**: [[UI_화면_규약]] §E 갱신 완료 · ~~문자열 키 27종~~ → **81종**(§9-4 v2, D5 배정) · ~~CSV 린트 24규칙~~ → **27규칙 + 적용대상 열 + 3계층**(§10 v2) · `_tables.csv` Id **90001400/90001500/90001600**(§6-4) · **오너 struct 세션 6건·1세션(R-1)**.

---

## 1. ① `PassiveId = NONE` 어휘 + 4슬롯 계약

### 1-1. 어휘 정의 (정식)

| 항목 | 확정 |
|---|---|
| 컬럼명 | **`PassiveId`** ([[네이밍_폴더_규약]] §4-A 외래키 규칙 `<대상단수>Id` 준수) |
| 타입 | **int32** (FK → `skills.csv`, `Layer==PASSIVE` 행) |
| **NONE 값** | **정수 `0`** |
| CSV 셀 표기 | **`0` 명시. 공란 금지 — 린트 L07a FAIL** |
| 사람용 표기 | 문서·`Memo`·`#id_txt`에서는 `NONE`으로 쓴다 |

> **왜 공란이 아니라 명시적 `0`인가**: DT int 임포트는 공란을 **0으로 강제 캐스팅**한다. 즉 저장값은 같지만 *"패시브가 없다"*와 *"아직 안 채웠다"*가 **CSV 위에서 구별 불가**해진다. A2가 1000행을 굽는 순간 이 구별이 사라지면 미기입 버그가 무음으로 통과한다. 명시 `0` + 공란 FAIL 린트가 그 구별을 데이터 위에 남긴다.
> **왜 `-1`이 아닌가**: `-1`은 plan 판정1-C3에서 **`MotionRow`의 중립 sentinel**로 이미 쓰인다. FK 컬럼의 "참조 없음"은 `StagingId=0`·`FxCastId=0`과 동일하게 `0`이어야 어휘가 하나로 유지된다(`0` = "가리키는 행 없음", `-1` = "이 축을 쓰지 않음").

### 1-2. 4슬롯 계약 (★Z5의 답)

```
캐릭터 1행 = [ 평타 1 ] [ 공용 1 ] [ 직업 1 ]   +   [ 패시브 0..1 ]
             └────── SkillIds (3토큰, 필수) ──┘       └ PassiveId (int, 0 허용) ┘
                     "항상 3"                            "0 또는 1"
```

> ★**4슬롯은 "항상 채워지는 4"가 아니다.** 정식 명칭 = **"3 필수 + 1 선택"**. `PassiveId = 0`(NONE)은 **결함이 아니라 유효한 정상 상태**이며, 알파 테스트 8기·회귀 기준선·기존 6개 `job_stats` 행이 전부 이 상태다.

### 1-3. P-NONE 불변식 (원장 봉인의 근거)

`PassiveId == 0`인 유닛에 대해 런타임이 보장하는 것:

| # | 불변식 |
|---|---|
| **PN-1** | `RunEffectBucket(...)` 호출 **0회** (`ON_BATTLE_START`·`ON_TURN_START` 양쪽 모두) |
| **PN-2** | `PassiveLog\|` 방출 **0줄** |
| **PN-3** | `BattleLog\|` 행수·필드 **완전 불변** (plan Z2 방어) |
| **PN-4** | `ActiveStatuses` 배열 변동 **0** · `bBlockActive`/`BlockValue` 변동 **0** |
| **PN-5** | 턴 길이(연출 시간) 변동 **0** → MA-1 재생 원장 무영향 |

**게이트**: 8기 전원 `PassiveId=0`으로 [[SPD원장_오라클_v1]] §7 20행 재실행 → **행 단위 diff 0**. (H2 규칙 준수: 요약값 일치는 PASS 근거 아님.)

### 1-4. SPD 오라클 §0 갱신 (본 문서에서 실행 완료)

`docs/features/전투완성/raw/SPD원장_오라클_v1.md` §0 고정입력 표에 **행 1개 추가**:

| 항목 | 값 | 근거 |
|---|---|---|
| **패시브 슬롯** | **없음 — 8기 전원 `PassiveId = 0`(NONE)** | 본 문서 §1 |

- **수치·기존 행·§4 본표·§7 파싱 CSV는 무접촉.** 추가된 것은 §0 표의 1행 + 각주 1줄뿐이다.
- 목적: *"원장이 어떤 전제 위에 봉인됐는지"* 를 명시. 이후 누군가 8기에 패시브를 붙이면 **§0이 바뀌었으므로 전면 재유도**가 그 문서 자신의 규칙(`바뀌면 전면 재유도`)으로 강제된다.

---

## 2. ② 슬롯 조달 SSOT (plan L2 해소)

### 2-1. 판정

| 후보 | 판정 | 사유 |
|---|---|---|
| `job_stats.SkillIds`(라이브, 6행 3토큰) | **현 SSOT — 유지·확장** | 라이브 소비 경로가 이미 여기를 판다. 여기 말고 다른 곳에 슬롯을 두면 **이중 진실** |
| `characters.csv`(미존재) | **A2 이후 SSOT — 동일 스키마 승계** | 청사진의 1000행. 아직 파일 없음 → **본 문서는 스키마 계약만 확정, 파일 생성은 A2** |
| `position_stats.csv`(balance v1 예고) | **슬롯 미보유 — 스탯 전용** | ★결정적: 포지션은 **3행**이다. 슬롯을 포지션이 소유하면 1000캐릭터가 **3종류로 붕괴**한다. 포지션은 *"어떤 풀에서 뽑는가"*(게이팅)만 소유하고, *"무엇을 뽑았는가"*(인스턴스)는 유닛 행이 소유한다 |

> **확정 규칙 (한 줄)**: **슬롯은 "유닛 1행 = 캐릭터 1명"인 테이블만 소유한다.** 지금은 `job_stats.csv`, A2 이후 `characters.csv`. ~~두 테이블은 **동일한 2컬럼 계약**을 만족하므로~~ → **2차 개정(Y8)**: 아래 §2-2 v2 참조. **계약은 3축이고 `job_stats`는 1축이 결손**이다. 소비 코드가 조회 대상 DT만 바꾸면 되는 것(파싱 무변경)은 여전히 참이다.

### 2-2. ~~슬롯 2컬럼 계약 (양 테이블 공통)~~ → **v2: 슬롯 3축 계약 + 픽스처 예외** (Y8 정정)

~~"양 테이블이 동일한 2컬럼 계약을 만족한다"~~ 는 **부정확했다.** 계약의 술어(`PositionTag ∈ {행 포지션, UNIVERSAL}`)가 **포지션 축에 의존**하는데 `data/job_stats.csv`에는 **포지션 컬럼이 없다**(실측 컬럼 = `Id`·`#id_txt`·`JobId`·`GradeId`·`Hp`·`Atk`·`Def`·`Spd`·`CritRate`·`SkillIds`·`Memo`).

| 축 | 컬럼 | 타입 | 규칙 | `characters.csv` | `job_stats.csv` |
|---|---|---|---|---|---|
| **A1 인스턴스** | `SkillIds` | String | `;` 구분 **정확히 3토큰**. 순서 고정 = **`평타 ; 공용 ; 직업`** → `WBP_SkillMenu` Row 1/2/3 스탬프와 1:1 | **L09 전면 적용** | **L09 면제** → 축소판 **L09f**(3토큰 ∧ 존재만, 순서 무강제). 현재 **6/6 PASS** |
| **A2 인스턴스** | `PassiveId` | int32 | `0` = NONE. `!=0`이면 42행에 존재 ∧ `#Layer==PASSIVE` | **L07a/b 적용** | **L07a/b 적용**(전 행 `0` → 자동 PASS) |
| **A3 게이팅** | **포지션 참조**(`PositionId` 또는 그로 유도 가능한 키) | int/String | 직업 토큰·패시브의 `#PositionTag`를 검사할 **술어의 좌변** | **보유 — L08·L26 적용** | **부재 — L08·L26 평가 불가 → 면제**(R-6-A) |

> ★**"알파는 전 행 `PassiveId=0`이라 L08이 공허하게 참"이라는 상태가 위험하다.** 하나라도 `0`이 아니게 되는 순간 L08은 **FAIL이 아니라 조용히 침묵**한다. → **L08·L26은 "평가 불가 시 SKIP"이 아니라 "평가 불가 시 그 파일을 면제 레지스트리에 명시적으로 등재"**로 처리한다. 레지스트리에 없는 파일에서 포지션 축이 없으면 **린트 자체가 FAIL**(fail-closed).

**신설 L26 (JOB 토큰 게이팅 — 비대칭 해소)**: 지금까지 포지션 게이팅은 **패시브만**(L08) 강제했다. `SkillIds`의 직업 토큰에는 규칙이 없어 **손편집·생성기 버그가 통과**한다(실증: 라이브 마법사 행은 `DEALER` JOB + `SUPPORT` JOB을 동시 보유). → `SkillIds[2]`의 `#Layer==JOB` ∧ `#PositionTag ∈ {행 PositionId, UNIVERSAL}`. **적용 대상 = `characters.csv` 한정.**

**즉시 반영 (D6, 라이브)**: `job_stats.csv`에 `PassiveId` 컬럼 1개 추가, **6행 전부 `0`**. → §1-3 PN-1~5로 원장 무영향. **`SkillIds` 값은 무접촉**(R-6-A 픽스처 면제).

### 2-3. A2 조달 경로 (1000행 생성기가 무엇을 하는가)

```
positions.csv(3) ─┐
grades.csv(N) ────┤
                  ├─▶ [A2 생성기, 시드 고정] ─▶ characters.csv (1000행)
skills.csv ───────┘                              ├ PositionId, GradeId
  ├ Layer=BASIC   (10, PositionTag=SHARED)        ├ SkillIds  = "<평타>;<공용>;<직업>"
  ├ Layer=COMMON  (10, PositionTag=SHARED)        └ PassiveId = <패시브 Id> 또는 0
  ├ Layer=JOB     (10, PositionTag별 게이팅)
  └ Layer=PASSIVE (10, PositionTag별 게이팅)      ※ Layer=PROBE는 풀에서 제외(§5-3)
```

| 단계 | 규칙 |
|---|---|
| 풀 구성 | 평타 = `Layer==BASIC` 전체(10) / 공용 = `Layer==COMMON` 전체(10) / 직업 = `Layer==JOB ∧ (PositionTag==행포지션 ∨ UNIVERSAL)`(4) / **패시브 = `Layer==PASSIVE ∧ (PositionTag==행포지션 ∨ UNIVERSAL)`(4)** |
| 추첨 | 4축 독립 추첨. **시드 고정 + 산출물 헤더에 시드 기록**(plan §샘플링률) |
| 유일성 키 | **`(PositionId, 평타, 공용, 직업, PassiveId)`** — ★패시브 추가로 키가 4→5원소로 확장됨. 기존 중복 재배치 스크립트 수정 필요 |
| 블랙리스트 훅 | `(직업, 패시브)` 페어 금지 목록 파일 1개. 뽑힌 조합이 목록에 있으면 **재추첨**(거부 샘플링). 알파 = 빈 목록 |
| 직렬화 | `SkillIds` = 3토큰 `;` 조인. **패시브를 절대 조인하지 않는다**(판정2) |

> **알파 스코프 주의**: 패시브 풀이 D5에서 비어 있으면(행 0) 생성기는 **전 행 `PassiveId=0`**을 쓴다 — 이것이 정상 동작이며 fallback이 아니다(§1-2).

### 2-4. 안전망 — `NotifySkillSelected(패시브ID)` 거절 (판정2 요구)

**삽입 위치**: `NotifySkillSelected`의 **최상위 첫 가드**(기존 쿨다운 가드보다 **앞**). 뒤에 두면 쿨다운 조회가 공란 데이터에 먼저 닿는다.

~~원안 (폐기 — `row.Layer`는 struct에 존재하지 않는다. X2·R-2)~~

```
✗ 폐기:  if (row.Layer == "PASSIVE") → Reject("PASSIVE_NOT_SELECTABLE")
✗ 폐기:  if (row.Layer == "PROBE" && !bDebugProbesEnabled) → Reject("PROBE_NOT_SELECTABLE")
```

**v2 (확정) — `MotionRow < 0` 판정. struct 편집 0 · 신규 멤버 0**

```
NotifySkillSelected(SkillId):
  row = DT_Skills[SkillId]
  if (!row.Found)          → Reject("SKILL_ROW_NOT_FOUND")    ; return
  if (row.MotionRow < 0)   → Reject("PASSIVE_NOT_SELECTABLE") ; return   ← 패시브 = 재생할 모션이 없다
  … 기존 경로(쿨다운 가드 → PendingSkillId 세팅 → 상태 전이) …

Reject(reason):
  Log("PassiveLog| reject skill={SkillId} reason={reason} unit={ActiveUnitSlot} turn={T}")
  return   ← 부작용 0
```

| 근거 | |
|---|---|
| **정확성** | L02(`Layer==PASSIVE → MotionRow==-1`) ∧ L03(액티브 `MotionRow ∈ motions.RowIndex = 0..16`) → **`MotionRow < 0` ⟺ 패시브**. 양방향 불변식(R-3-A) |
| **fail-closed** | `== -1`이 아니라 `< 0` — 범위 밖 음수도 거절 |
| **PROBE 가드 폐기** | `#Layer`가 CSV 전용이므로 런타임은 PROBE를 모른다. **`Layer=PROBE`의 의미는 "A2 생성 풀 제외" 하나뿐**이고 런타임 차단이 아니다 → `bDebugProbesEnabled` **불필요**, **F7b ⑧ 프루브 E2E 게이트 영향 0**(Y7 해소) |
| **의존성 고지** | 이 가드는 **린트 L02·L03의 `MotionRow` 절에 정확성을 의존**한다 → 두 절을 **T1(구조) 계층으로 승격**(R-6-B) |

**"상태 무변"의 정확한 정의 (검증 가능한 형태)** — 거절 경로 통과 후 다음이 **전부** 성립해야 한다:

| # | 불변식 |
|---|---|
| RG-1 | `PendingSkillId` **미변경** |
| RG-2 | `PendingTargetToken` **미변경**(공란 `Target`이 타겟 풀 0을 만드는 F7a ⑤형 소프트락 차단) |
| RG-3 | `SelectedTargets[]` **미변경** |
| RG-4 | 전투 상태머신 전이 **0회** (`AWAIT_COMMAND` 유지) |
| RG-5 | `BattleLog\|` 방출 **0줄** (오라클 diff 무영향) |
| RG-6 | `PassiveLog\|` **정확히 1줄** |
| RG-7 | 위젯 `bSelected` 하이라이트 변동 **0** |

**상태 전이표 (`AWAIT_COMMAND` 진입 입력 전량)**

| 입력 | 조건 | 전이 | 부작용 |
|---|---|---|---|
| `NotifySkillSelected(S)` | 행 미발견 | **없음** | `PassiveLog\|` reject `SKILL_ROW_NOT_FOUND` |
| `NotifySkillSelected(S)` | **`MotionRow < 0`**(= 패시브) | **없음** | `PassiveLog\|` reject `PASSIVE_NOT_SELECTABLE` |
| ~~`NotifySkillSelected(S)`~~ | ~~`Layer==PROBE` ∧ 디버그 off~~ | **행 삭제 — PROBE 런타임 가드 폐기**(R-3-B) | — |
| `NotifySkillSelected(S)` | 쿨다운 중 | **없음** | 기존 경로 유지 |
| `NotifySkillSelected(S)` | `Target ∈ {SELF, ALLY1}` 등 자동 해결 | → `EXECUTING` | `PendingSkillId=S` |
| `NotifySkillSelected(S)` | `Target` 수동 지정 필요 | → `AWAIT_TARGET` | `PendingSkillId=S`, `PendingTargetToken=row.Target` |

**게이트 TC(qa D3 인계)**: 라이브에서 `NotifySkillSelected(35000000)`을 강제 호출 → RG-1~7 전량 확인 + **소프트락 0**(다음 입력이 정상 수락되는지까지).

### 2-5. 미확인 — D6 착수 전 조회할 것

| # | 미확인 | 조회 방법 |
|---|---|---|
| U-1 | 유닛 액터가 `SkillIds`를 **스폰 시 캐시**하는지, 매번 `job_stats` DT를 재조회하는지 | `BP_BattleUnit`(또는 스폰 액터) 멤버 변수 목록 + `SetActiveUnit` 그래프 조회. **캐시형이면 `PassiveId`도 같은 캐시에 얹어야** DT 재조회가 안 는다 |
| U-2 | 유닛 → `job_stats` 행 키가 무엇인지(`JobStatsId` FK인지 `JobId+GradeId` 복합인지) | 같은 그래프. A2가 `characters.csv`로 갈아탈 때 **이 키 1개만 바꾸면 되는지**가 이주 비용을 결정 |
| U-3 | `ResolveHit` 재진입 가드 유무 | `ResolveHit` 그래프. 없으면 §8 E11(멀티히트) 설계가 유일 방어선 |

---

## 3. ③ 연출 프로필 — 4행으로 6축을 덮는다

### 3-1. 핵심 판정 — 3축은 **서로 다른 층**에 속한다

qa가 찾은 3축을 전부 `stagings.csv` 행으로 표현하려 하면 프로필이 폭발한다(근접×투사×도약×멀티히트 = 조합 폭발). **축을 올바른 층에 배치하면 행이 늘지 않는다.**

~~원안 (비용 표기가 틀렸다 — Y2)~~: ~~투사체 = 신규 컬럼 0(기존 컬럼 부활) / 멀티히트 = 신규 컬럼 0(기존 컬럼 부활)~~ — **같은 라벨 아래 비용이 다른 두 축을 묶었다.** v2에서 분리 표기한다.

| qa 지적 축 | 원자 수 | **어느 층이 소유하는가** | stagings 신규 행 | **struct 비용 (v2 — 분리 표기)** |
|---|---|---|---|---|
| **투사체** | 7 | ④ **이펙트층** — `fxProjectileId` | **0** | ★**신규 struct 필드 1 + 구 필드 제거 1.** `projectileFX`는 **FString**이고 §5-2가 폐기 대상으로 잡았다 → "부활"이 아니라 **타입 변경 + 개명 + 신규 추가**(R-1-A #3·#8) |
| **도약 접근** | 3 | ③ **연출층** — 접근 *방식* 파라미터 | **+1** | 신규 테이블 `stagings` 컬럼 +2(`ApproachMotionRow`·`ApproachArcHeight`). `F_SkillsRow` 무영향 |
| **멀티히트** | 3 | ① **스킬 메타** — `hitCount` + **연출 전용 계약**(§3-4) | **0** | ★**진짜 부활 — struct 편집 0.** Director 실측: `hitCount` 필드 **실재** ∧ DT에 값 `1` 존재. **CSV 컬럼 승계만** |

> ★**행 수 주장은 유효하다.** qa TC-S01이 30원자 전수 배정으로 **12+3+8+7 = 30/30, 미배정 0·중복 0**을 실증했다. 반증된 것은 *비용 라벨*이지 *프로필 4행으로 충분하다*는 주장이 아니다.

> ★**투사체가 프로필이 아닌 이유(논증)**: 프로필이 결정하는 것은 **"캐스터가 어떻게 움직이는가"**다. 파이어볼을 던지는 마법사와 즉발 정령타를 쏘는 마법사는 **캐스터 동작이 완전히 동일**하다(제자리 시전). 다른 것은 *캐스터와 타겟 사이 공간에 무엇이 날아가는가*뿐이고 그것은 이펙트 슬롯이다. `FxProjectileId == 0` → 즉발 / `> 0` → 투사. **분기 1개, 행 0개.**

### 3-2. `stagings.csv` — 프로필 4행 전문 (★필드값 명시)

**스키마** (실컬럼 5 + 메타 3):

| 컬럼 | 타입 | 의미 | 중립값 |
|---|---|---|---|
| `Id` | int | PK (§6-4 발급) | — |
| `#id_txt` | String | 사람용 슬러그 | — |
| **`AdvanceRatio`** | float `[0,1]` | 캐스터 → **접근종점** 벡터의 보간 비율 (§4) | `0` = 제자리 |
| **`ApproachMotionRow`** | int | 접근 이동 중 재생할 `motions.RowIndex`. **`-1` = 기본 이동 모션**(`DefaultLocomotionMotionRow` 데이터 멤버 = Run1/행2) | `-1` |
| **`ApproachArcHeight`** | float (uu) | 접근 궤적 정점 높이. `0` = 직선 | `0` |
| **`IsCameraCut`** | bool | 카메라 컷 사용 의사 (실제 발동은 거리가드와 **AND**, §3-5) | `FALSE` |
| **`IsTargetFlinch`** | bool | 타겟 피격 반응(Hurt 모션) 재생 | `FALSE` |
| `Memo` | String | 기획 메모(게임 미사용) | — |

**행 4개 (전문 — 이 표가 프로필의 유일한 내역서)**

| Id | `#id_txt` | AdvanceRatio | ApproachMotionRow | ApproachArcHeight | IsCameraCut | IsTargetFlinch | 담당 원자 |
|---|---|---|---|---|---|---|---|
| **62000100** | `stg_melee_walk` | **1.0** | **-1** (기본 이동) | **0** | **TRUE** | **TRUE** | 근접 물리 전량 (평타 ATK1~3계·강타·속공·둔화타·위압·반격참·처형) |
| **62000200** | `stg_melee_leap` | **1.0** | **4** (`Motion.Jump`) | **96** | **TRUE** | **TRUE** | 도약참·낙하타·필살의일격 **(3원자)** |
| **62000300** | `stg_ranged_cast` | **0.0** | -1 | 0 | **FALSE** | **TRUE** | 원거리 전량 — 즉발(정령타)·투사(마력탄·원소구·제압사격·화염징표·파이어볼·연속마격·봉인) **모두 이 1행** |
| **62000400** | `stg_support_cast` | **0.0** | -1 | 0 | **FALSE** | **FALSE** | 자기·아군 대상 (막기·방어태세·수호방벽·기력모으기·치유·응급치유·축복) |

- **`ApproachArcHeight = 96`의 근거**: 스프라이트 그리드 기준 캐릭터 신장 ≈ 2칸 상당의 시각적 도약. **[검증 필요]** — 실제 값은 D4.5에서 스프라이트 실측 후 확정. **본 문서는 "컬럼이 존재하고 도약행이 0이 아니다"까지만 확정**하고 정확한 수치는 미확정으로 둔다.
- **`ApproachArcHeight`는 턴 길이에 영향이 없다** — 같은 이동 시간(상수 0.4s) 안에서 수직 오프셋만 얹는다. → **SPD 원장·MA-1 재생 원장 무영향**(리듬 상수 데이터화 금지 원칙 위반 아님).
- **`Motion.Jump`(행 4)는 현재 "미사용"**(`motions.csv` Memo). 도약 프로필이 이 행의 **첫 소비처**가 된다 — 재고 활용, 신규 아트 0.

### 3-3. ★과설계 방지 셀프체크 (프로필이 8이 되면 틀린 것)

| 체크 | 결과 |
|---|---|
| 프로필 수 | **4** (상한 8의 절반) |
| 새 원자 1개 추가 시 편집 지점 | `skills.csv` **1행**(`StagingId`에 기존 4개 중 1개 기입). BP 0노드 → **레고 검증 PASS** |
| 신규 프로필이 필요해지는 조건 | *캐스터의 이동 방식 자체*가 위 4개 중 어디에도 없을 때만(예: 순간이동·후퇴사격). 30원자 안에는 없다 |
| 프로필이 늘지 **않는** 것 | 색·속성·데미지·상태이상·투사체·타격 횟수·타겟 수 — 전부 다른 층 |

### 3-4. 멀티히트 계약 (★판정/연출 분리 — F7 §8-6 자충돌 방어)

| 항목 | 확정 |
|---|---|
| `HitCount` 소유 | `skills.csv` (기존 컬럼 부활) |
| **알파 의미** | **연출 전용** — 임팩트 **FX·SFX·flinch의 재생 횟수** |
| **판정 관계** | **`ApplySkillEffects`는 캐스트당 정확히 1회**(F7 §8-10 무변경). `HitCount`는 판정을 **곱하지 않는다** |
| 데미지를 N분할하려면 | `skill_effects.csv`에 **DAMAGE 엔트리 N행**을 쓴다(이미 표현 가능·SSOT 1곳) |
| 판정 실행 시점 | **첫 임팩트 시점 1회**(현행 `ResolveHit` 콜사이트 위치 유지 → 원장 무영향). 2번째 이후 임팩트는 **FX/SFX 전용** |
| 간격 | `HitIntervalSec` = **명명 상수(데이터 멤버)**. CSV 컬럼 아님 — ~~스킬마다 다르면 턴 길이가 흔들려 SPD 원장에 노이즈~~ **【논거 철회 — R-9】** → **v2 논거**: 간격을 데이터화하면 상한이 `HitCount × HitIntervalSec`의 **곱 부등식**이 되어 balance가 2변수를 동시 조율해야 하고 손검산이 불가능해진다. **1변수로 유지해 상한을 기계 검증 가능하게** 두는 것이 이유다(R-9 T-1) |
| 상한 | `HitCount ∈ [1, MaxHitCount]`. `MaxHitCount` = 데이터 멤버. **값 미확정** → **v2: 독립 상수가 아니라 턴 예산 린트 L27의 귀결**이다 — 최악 케이스(투사 최장 + 임팩트 최장)에서 `EstimatedTurnSec ≤ SlotBudgetSec`를 만족하는 최대 `HitCount`(R-9) |
| **턴 길이** | ★**v2 정직 고지**: 턴 길이는 **이미 데이터의 함수**다(`motions.FrameCount`·`HitCount`·`vfx[FxProjectileId]`·`vfx[FxImpactId]`). 금지 대상은 "변동"이 아니라 "**예측 불가능한 상한**"이며, **린트 L27(턴 예산 계약)**이 그것을 막는다. MA-1(TC-G07)은 기본공격 패턴 한정이므로 **이 축을 잡지 못한다** |
| 중립값 | `Layer==PASSIVE` → `HitCount = 0` |

> **왜 판정을 곱하지 않는가**: `HitCount`가 ON_HIT 버킷을 반복하면 (a) 쿨다운·`BattleLog` 캐스트당 1줄 원칙이 깨지고(F7 §8-6이 명시 경계) (b) 데미지 총량이 CSV 두 곳(`PowerRate`, `HitCount`)에 나뉘어 **이중 진실**이 되며 (c) 상태이상 롤이 N회 굴러 확률이 조용히 변한다. 셋 다 balance가 손으로 검산할 수 없게 만든다.

### 3-5. 카메라 컷 — 새 경로를 열지 않는 결합식 (plan §검증 "거리가드 신규 경로" 방어)

현행 `IfThenElse_6` = `OR(Target != "ENEMY1", dist < 1.0)`. `IsCameraCut` bool로 **단순 치환하면** *"근거리인데 컷"* 이라는 없던 경로가 열린다.

> **확정 결합식**: `bDoCameraCut = Staging.IsCameraCut  AND  (dist >= CameraCutMinDistance)`
> `CameraCutMinDistance` = **명명 상수(데이터 멤버)**, 초기값 = 현행 매직넘버 `1.0`. **거리가드는 제거하지 않고 AND로 보존**한다(매직넘버 → 명명 상수까지만, 데이터화 안 함).

### 3-6. ★극단 8표본 표현 실증 (D4 진입 조건)

**전 8축이 위 4행 + 기존 컬럼으로 표현된다.** (`>0` = D4.5에서 실제 Id 배정)

| # | 축 | 원자 | Layer | MotionRow | Target | **StagingId** | HitCount | FxCastId | **FxProjectileId** | FxImpactId | 판정 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 근접 | 31000000 기본공격 | BASIC | 5 | ENEMY1 | **62000100** | 1 | 0(→모션 폴백) | 0 | >0 | ✓ |
| 2 | **도약** | 31012000 도약참 | BASIC | 8 | ENEMY1 | **62000200** | 1 | 0 | 0 | >0 | ✓ |
| 3 | 원거리 **즉발** | 32011000 정령타 | BASIC | 11 | ENEMY1 | **62000300** | 1 | >0 | **0** | >0 | ✓ |
| 4 | **투사** | 32001000 파이어볼 | JOB | 10 | ENEMY1 | **62000300** | 1 | >0 | **>0** | >0 | ✓ ★3과 **StagingId 동일**, 차이는 `FxProjectileId`뿐 |
| 5 | **멀티히트** | 31011000 연타 | BASIC | 7 | ENEMY1 | **62000100** | **3** | 0 | 0 | >0 | ✓ |
| 6 | SELF | 33001000 막기 | COMMON | 15 | SELF | **62000400** | 1 | >0 | 0 | 0 | ✓ 제자리(벡터 0) |
| 7 | ALLY | 34001000 치유 | JOB | 11 | ALLY1 | **62000400** | 1 | >0 | 0 | **0** ★3차 개정 | ✓ 제자리·flinch 없음 |
| 8 | **패시브** | 35000000 (§9-4) | **PASSIVE** | **-1** | **NONE** | **0** | **0** | **0** | **0** | **0** | ✓ 전 필드 중립값 |

**회귀 표현(기존 5스킬)**: 기본공격 =1행 / 베기 = 1행과 동일 / 파이어볼 = 4행 / **막기 = 6행(`AdvanceRatio 0` → 제자리 + WalkBack 스킵)** / **치유 = 7행(제자리)**. → 파트3이 보류한 *"막기 시 run 1프레임 스침"* 이 `AdvanceRatio==0 → 접근·복귀 양방향 스킵`으로 **구조적 해소**.

> ★**2차 개정 2건**: ① 위 표의 **`FxImpactId` 열은 설계 배정이지 라이브 실측이 아니다.** 라이브에서 임팩트 FX(`HitMID`)가 실제로 어느 스킬에 재생되는지는 **미확인(Q4)** — D4.5 조회 후 불일치하면 이 열을 정정한다. ② `Layer` 열은 실제로는 **`#Layer`**(CSV 전용, R-2)다.

> ### ★3차 개정 (2026-08-11, D4.5-b 실측 반영) — 위 각주 ①의 **실제 발동**
>
> **Q4 닫힘**([[D4.5b_VFX행배정]] §2): 임팩트 FX(`HitMID`)는 스킬별 화이트리스트가 아니라 **`skills.Target == "ENEMY1"` 문자열 게이트 하나**로 켜진다. 호출 지점은 `BP_BattleManager` EventGraph의 **`PlayHurtReaction` 단 1곳**.
>
> | 행 | 원자 | `Target` | 설계 `FxImpactId` | **라이브 실측** | 판정 |
> |---|---|---|---|---|---|
> | 6 | 막기 | SELF | `0` | 미재생 | ✅ 일치 |
> | **7** | **치유** | **ALLY1** | ~~`>0`~~ | **미재생** | ❌ **불일치 → `0`으로 정정** |
>
> **정정 내용**: 표 7행의 `FxImpactId`를 ~~`>0`~~ → **`0`**으로 바꾼다. 사유는 "라이브가 그러니까"가 아니라 **다음 3개**다.
>
> 1. **알파 스코프**: 치유에 임팩트 FX를 다는 것은 *구조 데이터화*가 아니라 **없던 연출의 신규 추가**다. D4 게이트가 승인한 범위 밖.
> 2. **쓸 이펙트가 없다**: D4.5-b가 배정한 `63000200 vfx_legacy_hit`는 **타격 스파크**다. 회복 대상에게 타격 이펙트를 띄우는 것은 명백히 틀린 룩이다. 제대로 하려면 회복 전용 행이 필요하다.
> 3. **비용이 미뤄도 같다**: 나중에 넣고 싶어지면 `vfx.csv`에 **1행 복제**(`TexPath` 재사용, `ColorRow`만 변경 — D4.5-b §5-3이 명시한 경로)면 끝난다. **지금 결정해야 할 이유가 0이므로 옵션을 열어둔 채 미룬다.**
>
> ⚠ **D6 구현자에게**: 이 정정은 *"설계는 켜라는데 런타임이 안 켠다"*는 혼란을 없애기 위한 것이다. **`Target=="ENEMY1"` 문자열 게이트 자체는 D6에서 반드시 제거된다**(plan ③ 연출 데이터화의 목적). 게이트를 없앤 뒤 치유가 여전히 임팩트 FX를 안 내는 근거는 **런타임 하드코딩이 아니라 `FxImpactId = 0`이라는 데이터**여야 한다. 즉 **동작은 같고 근거가 코드에서 데이터로 이동**한다 — 이것이 이 정정의 요점이다.
>
> → **분해 방법과 `IsTargetFlinch`와의 축 판정은 §3-6b**(신설).

### 3-6b. ★임팩트 VFX 게이트 vs `IsTargetFlinch` — **다른 축이다. 그러나 컬럼은 늘지 않는다** (3차 개정 신설)

> **Director 질의**: *"둘 다 「타겟에게 뭔가 일어나는가」를 묻는데 하나는 피격 리액션이고 하나는 임팩트 VFX다. 같은 축이면 프로필 4행으로 커버되고, 다른 축이면 컬럼이 하나 더 필요하다."*

#### (1) 현행 구조 — ~~`IsAttackFamily`와 **똑같은 이중 게이트**다~~ → **정정(2026-08-12)**: 이 문서에서 처음 발견된 이중 게이트다(아래 참조)

D4.5-b 실측: `PlayHurtReaction` **커스텀 이벤트 하나가 두 가지를 동시에** 한다.

```
if (skill.Target == "ENEMY1")            ← 게이트 1개
    Target->PlayHurtReaction()
         ├ Hurt 모션 재생          (타겟 모션 축)
         └ SetMaterial(HitMID) → SetVisibility(true) → Delay(0.55) → false   (타겟 VFX 축)
```

★ ~~이것은 §3-8이 분해한 `IsAttackFamily`(런지 + 잔상 동시 게이트)와 구조적으로 동일한 함정이다.~~ → **정정(2026-08-12, [[전진로직_실체_확정]])**: 비교 대상이었던 `IsAttackFamily`는 실측 결과 런지와 무관한 **잔상 단일 축 게이트**였다(§3-8 정정 참조) — 따라서 이 비교는 "동일한 함정"이 아니라 **이 문서(`PlayHurtReaction`)에서 처음 발견된 함정**이다. 다만 판정 원칙 자체는 불변: **하나의 불리언/문자열 비교가 서로 다른 두 축을 묶고 있고, 한쪽만 원하는 원자가 생기는 순간 표현 불가능해진다.**

#### (2) 판정 근거 — 30원자 안에 **실제로 갈리는 원자가 있는가**

§3-8이 `IsAttackFamily`를 분해한 근거는 *"파이어볼이 전진은 빼고 잔상은 유지해야 한다"*는 **구체적 원자의 실재**였다. 같은 시험을 적용한다.

| 프로필 | 원자 수 | 타겟 Hurt 모션 | 타겟 임팩트 VFX | 갈리는가 |
|---|---|---|---|---|
| `62000100` 근접 | 12 | ON | ON | — |
| `62000200` 도약 | 3 | ON | ON | — |
| `62000300` 원거리 | 8 | ON | ON | — |
| `62000400` 지원-SELF | 4 (막기·방어태세·수호방벽·기력모으기) | OFF | OFF | — |
| **`62000400` 지원-ALLY** | **3 (치유·응급치유·축복)** | **OFF**(아군을 움찔시키면 안 된다) | **켜고 싶어질 수 있다** | ★**갈린다** |

> **판정: 두 축은 다르다.** 회복 대상은 **움찔하면 안 되지만**(flinch OFF) **회복 이펙트는 떠도 된다**(VFX ON). 이 조합은 단일 게이트로 표현 불가능하며, 파이어볼이 §3-8에 준 증거와 **정확히 같은 형태**다.

#### (3) ★그런데 신규 컬럼은 **0개**다 — 두 축은 이미 서로 다른 곳에 있다

| 축 | 소유자 | 표현 | 왜 그 층인가 |
|---|---|---|---|
| **타겟 모션(flinch)** | **`stagings.IsTargetFlinch`** (기존) | bool | 타겟이 *어떻게 움직이는가* = **연출층**. 프로필이 소유하는 것이 맞다 |
| **타겟 임팩트 VFX** | **`skills.FxImpactId`** (기존, §3-7) | `0`=없음 / `>0`=있음 | *공간에 무엇이 뜨는가* = **이펙트층**. §3-1이 투사체를 프로필에서 뺀 것과 **동일 논거** |

**→ 프로필 4행 유지. `stagings.csv` 컬럼 +0.** 두 축은 이미 **다른 테이블의 다른 컬럼**으로 분리돼 있었고, 묶여 있던 것은 **런타임 코드**뿐이다.

**`stagings`에 `IsImpactFx` bool을 신설하지 않는 이유** (기각 근거):
1. **이중 진실** — `FxImpactId == 0` ∧ `IsImpactFx == TRUE`라는 **모순 상태가 표현 가능**해진다. 어느 쪽이 진실인지 정하는 규칙이 또 필요해지고, 그걸 강제할 린트가 또 필요해진다.
2. **§3-1 층 배치 원칙 위반** — *"프로필이 결정하는 것은 캐스터가 어떻게 움직이는가"*. 타겟 공간에 뜨는 VFX는 프로필 축이 아니다.
3. **`FxImpactId`가 이미 3-값 이상을 표현한다** — bool은 ON/OFF뿐이지만 `FxImpactId`는 *어느 이펙트인가*까지 담는다. bool은 정보량이 더 적으면서 컬럼만 늘린다.

#### (4) D6 분해 명세 (gameplay 인계 — `IfThenElse` 1개 → 조건 2개)

```
✗ 현행:  if (skill.Target == "ENEMY1")  → Target->PlayHurtReaction()   [모션 + VFX 동시]

✓ D6:    if (Staging.IsTargetFlinch && Target.bAlive)  → Target->PlayFlinch()          [모션만]
         if (Skill.FxImpactId > 0)                     → PlayImpactFx(FxImpactId)      [VFX만]
```

- **`Target == "ENEMY1"` 문자열 게이트는 양쪽 모두에서 사라진다** — 이것이 plan ③ 연출 데이터화의 목적 그 자체다.
- `PlayHurtReaction`은 **둘로 쪼갠다**(`PlayFlinch` / `PlayImpactFx`). §11 **C4**(`FxCastId`+`StagingId`+`FxMotionId` 동일 커밋) 원칙과 같은 이유로 **이 두 조건도 같은 커밋**이어야 한다 — 하나만 바꾸면 이중게이팅 또는 게이트 소실이 난다.
- `Target.bAlive` 체크는 **E11(멀티히트 중 타겟 사망 → 시체 움찔 0)** 계약의 실행 지점이다.

#### (5) 회귀 대조 — 라이브 7행

| 스킬 | `Target` | 현행 Hurt모션 | 현행 HitMID | 신 `IsTargetFlinch` | 신 `FxImpactId` | 판정 |
|---|---|---|---|---|---|---|
| 기본공격·강타 | ENEMY1 | 재생 | 재생 | `62000100` → TRUE | `>0` | **회귀 0** |
| 파이어볼 | ENEMY1 | 재생 | 재생 | `62000300` → TRUE | `>0` | **회귀 0** |
| 막기 | SELF | 미재생 | 미재생 | `62000400` → FALSE | `0` | **회귀 0** |
| **치유** | ALLY1 | 미재생 | 미재생 | `62000400` → **FALSE** | **`0`**(3차 개정) | **회귀 0** ★정정 덕분 |
| 프루브 2행 | ENEMY1 | 재생 | 재생 | `62000100` → TRUE | `>0` | **회귀 0** |

**7/7 회귀 0.** 3차 개정으로 치유를 `0`으로 정정하지 않았다면 이 표는 **6/7**이 되고, 유일한 변경이 *"회복 대상에게 타격 스파크가 뜬다"*는 **명백한 룩 결함**이었다.

---

### 3-7. 이펙트층 배선 규칙 (연출과 짝을 이루는 부분)

| 슬롯 | `skills.csv` 컬럼 | 폴백 | 재생 시점 | 물리 슬롯 |
|---|---|---|---|---|
| 캐스트 | `FxCastId` | `0`이면 `motions.FxMotionId` | 모션 시작 프레임 | `SmearMID`(=캐스트 슬롯) |
| **투사** | **`FxProjectileId`** | 폴백 **없음**(`0` = 투사 없음) | 캐스트 FX 종료 직후 | `SmearMID` 재사용(캐스트 종료 후 비므로) |
| 임팩트 | `FxImpactId` | 폴백 **없음** | 아래 유도식 | `HitMID`(=임팩트 슬롯) |

```
투사지속 = (FxProjectileId > 0) ? min(vfx[FxProjectileId].FrameCount / FPS, ProjectileFlightBudgetSec) : 0
임팩트시각 = 캐스트FX 종료 + 투사지속
```
- `ProjectileFlightBudgetSec` = **명명 상수(데이터 멤버)** — 리듬 상수라 데이터화하지 않는다.

> ### ★3차 개정 — **표시 지속 공식에 `− 0.05`가 빠져 있었다** (D4.5-b Q5 실측)
>
> ~~`표시지속 = min(FrameCount/FPS, SlotBudgetSec)`~~ → **라이브는 이 식을 쓰지 않는다.**
>
> **실측**([[D4.5b_VFX행배정]] §1-2·§1-3): `BP_BattleSpawnPoint`의 FX 표시는 `SetVisibility(true) → RetriggerableDelay(D) → SetVisibility(false)` 패턴이고, `D`의 리터럴이 **Smear `0.45` · Hit `0.55`**다. 그리고 같은 그래프의 다른 노드가 **`PendingFrameCount / 8.0 − 0.05`** 식을 쓴다 — *"프레임수/FPS − 0.05 세이프마진"*이 **이 프로젝트에 이미 실재하는 패턴**이다. FPS=10을 대입하면 `5/10−0.05=0.45`, `6/10−0.05=0.55`로 **소수점까지 일치**한다.
>
> **★무음 회귀 발견**: 원안 공식을 그대로 D6에 배선하면 Smear 표시시간이 `min(5/10, SlotBudgetSec) = 0.50`이 되어 **현행 0.45보다 0.05s 길어진다.** 라이브 5스킬 **전부**에서 그렇다. → **MA-1 재생 원장(TC-G07) diff가 깨진다.** 값이 "거의 같아서" 육안으로는 잡히지 않는 종류의 회귀다.
>
> **확정 공식 (기존 계승 — 신규 상수 1개뿐)**
>
> ```
> 표시지속(fx) = min( vfx[fx].FrameCount / vfx[fx].FPS , <해당 슬롯 예산> ) − FxSafetyMarginSec
> ```
>
> | 항목 | 확정 |
> |---|---|
> | `FxSafetyMarginSec` | **명명 상수(데이터 멤버). 초기값 `0.05`** — 라이브 리터럴 계승이므로 **새로 발명한 값이 0개**다 |
> | 왜 상수인가 | `−0.05`는 *"다음 동작 전에 이펙트를 끄는 여유"*로 **전 이펙트 공통 리듬**이다. R-9 T-1(*"리듬 상수는 축이지 행이 아니다"*)이 그대로 적용된다 → CSV 컬럼 **아님** |
> | 클램프와의 관계 | `min(…)`은 **상한**, `−0.05`는 **여유**로 **서로 다른 목적**이라 배타적이지 않다. `min(A,B) − m ≤ B`이므로 슬롯 예산 상한은 그대로 지켜진다 |
> | 음수 방어 | `FrameCount/FPS ≤ FxSafetyMarginSec`이면 지속이 음수가 된다 → **린트 L19가 임포트에서 차단**(아래 §10). 런타임은 `max(0, …)` + **로그 1줄**(무음 클램프 금지) |
> | 적용 범위 | 캐스트·투사·임팩트 **3슬롯 전부**. 모션 폴백 경로(`FxCastId=0 → motions.FxMotionId`)도 같은 식 |
>
> **파급 정정 3곳**: §6-2 `FPS` 행 설명 · R-9 `EstimatedTurnSec` 식(3개 FX 항) · §10 L19(마진 하한 절 추가) · §11 C10(상수 목록에 `FxSafetyMarginSec` 추가). 전부 아래에 반영 완료.
- **텍스처 스왑 시 스칼라 4종 동시 세트 의무**: `RowIndex`(=ColorRow)·`FrameCount`·`GridX`·`GridY`. 하나라도 빠지면 이전 MI baked 값 잔존 → 무음 오작동(gameplay 정정2).
- **fail-loud**: `vfx.csv` 행 미발견 → 로그 1줄 + **미재생**. **다른 FX로 폴백 금지**(모션 폴백 5의 재발 차단).

### 3-8. `IsAttackFamily` 폐기 — 2축 분해 대응표

> ★**정정(2026-08-12, [[전진로직_실체_확정]])**: 아래 원문은 `IsAttackFamily`를 "런지(전진)+잔상 동시 게이트"로 서술했으나 **핀 원문 실측으로 반증됐다.** `IsAttackFamily`는 **잔상(`EffectQuad`) 전용 게이트**(소비처 `IfThenElse_10` 단 1개, `BP_BattleSpawnPoint.EventGraph` — ⚠`BP_BattleManager.ApplySkillEffects`에도 동명이인 `IfThenElse_10`이 있으나 무관한 `bAlive` 게이트, 그래프명 병기 필수)이고, 전진(`WalkForward`)은 `BP_BattleManager`의 `Target` 문자열 3-way 분기(`ALLY1`/`SELF` 예외 외 전부 전진, H31)가 **완전히 별개로** 통제한다. **이관 결론(전진→`stagings.AdvanceRatio`, 잔상→`motions.FxMotionId`, `IsAttackFamily` 컬럼 제거)은 그대로 유효**하다 — 애초에 전진과 잔상이 "하나의 게이트가 분해되는 것"이 아니라 **"이미 독립적이던 두 legacy 게이트가 각각 데이터 컬럼으로 이관되는 것"**일 뿐이다.

~~현행 `IsAttackFamily`는 **"런지(전진) + 잔상(FX)" 동시 게이트**(소비처 `IfThenElse_10` 단 1개, `BP_BattleSpawnPoint`). 이것이 파트3 무음 회귀의 원인이다.~~

| `IsAttackFamily`가 하던 일(★정정: 아래 좌열 "전진할 것인가"는 `IsAttackFamily`가 아니라 `Target` 게이트가 하던 일이다) | 이관 대상 |
|---|---|
| 전진할 것인가 | **`stagings.AdvanceRatio`** |
| 잔상 FX를 낼 것인가 | **`motions.FxMotionId`**(`0` = 없음) |

→ `motions.csv`에서 `IsAttackFamily` **컬럼 제거**, `FxMotionId`·`SfxMotionId` 추가. 치유(row 11)의 잔상 억제는 `FxMotionId=0`으로 표현되고, **"마법 캐스트에도 잔상"을 나중에 켜도 ~~런지가 같이 부활하지 않는다~~ → (정정) 런지는 애초에 이 게이트와 무관하므로 부활할 대상 자체가 없다.**

> ★**이관 매핑값은 R-4 표가 17행 전수 지정한다**(Y6 — 0 초기화하면 RowIndex 5·6·7·8·9·10의 잔상이 **전부 무음 소실**). **파이어볼(row10)이 이 분해의 산 증거다** — 전진은 제거(`AdvanceRatio=0.0`)하면서 **잔상은 유지**(`FxMotionId=<VFX_SMEAR_LEGACY>`)한다. ~~현행 단일 불리언으로는 표현 불가능한 조합이다.~~ → (정정) 현행 두 독립 게이트(Target/IsAttackFamily)를 각각 그대로 옮기면 자동으로 표현되는 조합이다 — 단일 불리언 분해가 필요했던 적이 없다.

---

## 4. ④ `AdvanceRatio` 소비 계약 — **(a) 실제 Lerp 알파** 확정

### 4-1. 판정

**(a) 채택.** (b) `{0,1}만 유효`는 기각.

| 기각 사유 (b) | |
|---|---|
| 1 | float 타입에 2값만 허용하면 **타입과 계약이 어긋난 채 영구 고착**된다. bool로 갈 거면 지금 bool이어야 하는데, plan이 이미 float 유지를 정당화했다 |
| 2 | **근접 정지 거리가 데이터가 될 자리를 영구히 잃는다.** 근접·도약이 타겟에 얼마나 붙느냐는 30원자에서 실제로 다를 수 있는 값이고, 지금 봉인하면 나중에 새 컬럼이 생긴다(= 축 추가) |
| 3 | 도약 프로필은 "완전히 붙지 않고 조금 앞에 착지"가 자연스럽다 — `{0,1}`로는 표현 불가 |

### 4-2. 정의 (명문 — 이 문장이 계약이다)

> **`AdvanceRatio` = 캐스터 현재 위치 → `ResolveApproachAnchor(Caster, PrimaryTarget)` 로 향하는 벡터의 보간 알파.**
> 접근 목표 위치 `P = Lerp(Caster.Loc, Anchor.Loc, AdvanceRatio)`.

~~원안 (Y12 — 4분기 중 2분기가 `PrimaryTarget`을 무시한다. `Target=ALLY1` ∧ `AdvanceRatio>0`이면 **치유하러 적진으로 걸어간다**)~~

```
✗ 폐기:  if (AttackPoint_<Caster.Team> exists) return AttackPoint.Loc   # 아군 타겟에도 적 방향 고정점을 반환
```

**v2 (확정) — 아군/자기 타겟은 적 앵커로 가지 않는다**

```
ResolveApproachAnchor(Caster, PrimaryTarget):
  if (PrimaryTarget == Caster)                 return Caster.Loc          # SELF → 벡터 0 → 자동 제자리
  if (AttackPointOverride is valid)            return AttackPointOverride # N6 경로 그대로 존치
  if (PrimaryTarget.Team != Caster.Team):                                 # ★신규 분기 — 적 타겟일 때만
      if (AttackPoint_<Caster.Team> exists)    return AttackPoint.Loc     # 현행 동작 = AdvanceRatio 1.0
  return PrimaryTarget.Loc                                                # 아군·최종 폴백 → 아군 방향
```

- **린트 L12b(신설, 보조 방어)**: 서포트 프로필(`stagings` 중 대상이 아군·자기인 행)의 `AdvanceRatio`는 **`0.0` 고정**. 알파에서 이 경로가 아예 발현되지 않게 데이터로도 막는다.
- 알파 발현 여부: **없음**(서포트 프로필 `AdvanceRatio=0.0`). 그러나 L12가 어떤 프로필에도 `[0,1]`을 허용하므로 **D5가 값을 잘못 넣으면 즉시 발현**한다 → 코드 분기와 린트 **둘 다** 둔다.

| 성질 | 결과 |
|---|---|
| **회귀** | ~~라이브 5스킬 이동 거리·시간 **무변**~~ → **거짓이었다(Y1).** 근접 2종(기본공격·강타)·치유는 무변, **막기는 의도된 개선**, **파이어볼은 전진 → 제자리로 바뀐다.** 정직 기록·판정·게이트 3건은 **R-7** 참조 |
| **SELF 자동 처리** | 벡터가 0이라 알파와 무관하게 제자리. plan이 예고한 *"프로필 1종 자연 절약"* 실현 |
| **fail-loud** | `0.9`를 적으면 **실제로 90%만 이동한다.** 무음 실패가 구조적으로 불가능 |
| **N6 방어 유지** | `AttackPointOverride` Clear 계약(`then` **AND** `RowNotFound` 양 경로)은 **무변경 존치**. 실패 시 *"다음 턴 파이어볼이 아군에게 걸어간다"* 무음 버그가 오라클 diff 0을 통과하므로, `StagingId` 조회 재배선 후 **Clear 지배 재확인이 D6 게이트 항목** |
| 복귀(WalkBack) | `AdvanceRatio == 0` → 접근·복귀 **양쪽 모두 스킵**(비대칭 금지 — 접근만 스킵하고 복귀를 남기면 캐스터가 뒤로 밀린다) |

### 4-3. 임포트 검증 (fail-closed)

| 규칙 | 동작 |
|---|---|
| `AdvanceRatio ∉ [0.0, 1.0]` | **임포트 FAIL** (외삽 금지 — 1.2는 타겟을 지나쳐 통과한다) |
| `AdvanceRatio` 공란 | **임포트 FAIL** (0으로 무음 캐스팅되면 근접이 제자리 타격이 된다) |
| 런타임 방어 | 그래도 범위 밖 값이 들어오면 `Clamp(0,1)` + **로그 1줄**(무음 클램프 금지) |

### 4-4. 명명 — `AdvanceRatio` **유지**

| 후보 | 판정 |
|---|---|
| `AdvanceRatio` **유지** | **채택.** plan·qa·gameplay·balance 4개 문서와 청사진에서 이미 이 이름으로 통용된다. §4-2 정의문이 모호성("아군 힐은 뒤로 가는데 Advance?")을 완전히 제거하므로, 개명 이득 < **크로스 문서 드리프트 비용** |
| `ApproachRatio` / `ApproachLerp` | 기각(위와 동일 사유). 단 `stagings.csv`의 **다른** 접근 컬럼은 `Approach*` 접두를 쓴다(`ApproachMotionRow`·`ApproachArcHeight`) — 같은 개념군임을 이름으로 묶는다 |

---

## 5. ⑤ CSV 3분기 병합 (plan L3 해소)

### 5-1. 실측 3파일 대조

~~원안: `data/skills.csv`가 라이브 DT 소스이므로 그것을 기준선으로 삼는다~~ → **★틀린 기준선이었다(Director 판정 X5).**

**Director MCP 라이브 실측이 뒤집은 것**: `DT_Skills` 행 3개(`31000000`/`33001000`/`34001000`)에 `category`=`"ATTACK"`/`"DEFEND"`/`"MAGIC"`, `hitCount`=`1`, `primaryTargetGroup`=`"SINGLE_ENEMY"`/`"SELF"`/`"SINGLE_ALLY"` **값이 채워져 있다.** 그런데 `data/skills.csv`(15컬럼)엔 **그 컬럼이 없다.** `skills_v2_draft.csv` 헤더에는 **정확히 그 3컬럼 + FX/SFX 5컬럼**이 있다.

| 파일 | 행 | 컬럼 | 고유 기여 | **처리 (v2)** |
|---|---|---|---|---|
| `data/skills_v2_draft.csv` | 7 | **23** | `Category`·`HitCount`·`PrimaryTargetGroup` + FX/SFX 문자열 5 + **프루브 2행** | ★**실질 기준선 — 라이브 DT를 만든 파일.** struct 23필드와 **1:1 대응**한다. 값 대조의 기준(R-5 P4) |
| `data/skills.csv` | 5 | 15 | 라이브 DT **소스가 아니다**(3필드 결손) | ⚠ **임포트 금지(P1).** 최종 SSOT **파일명**만 승계 — 42행 승격은 ⑩a 이후(P2) |
| `data/drafts/skills_30원자_초안.csv` | 30 | 18 (+3) | **30원자 행** + `Layer`·`PositionTag`·`Status` | **흡수 후 `#DEPRECATED`** |

> **확정 (v2)**: **최종 SSOT 파일명은 `data/skills.csv`를 유지**하되, **그 파일의 현 내용(15컬럼 5행)은 기준선이 아니다.** 병합의 값 기준선은 **`skills_v2_draft.csv`**이고, 행 집합 기준선은 **30원자 초안**이다. 드래프트 2종은 병합 후 **첫 행에 `#DEPRECATED …` 주석 행**을 넣는다([[데이터_서버_규약]] §4 `#` 주석 규약 — 파일 삭제 안 함, git 이력 보존).
>
> ⚠ **`skills.csv`로 재임포트하면 3필드가 전부 기본값으로 리셋된다.** 지금은 소비 노드 0이라 무증상이지만 §3-4가 `HitCount`를 소비하기로 확정한 순간 터진다. → **R-5 P1~P6 절차가 이 절의 실행 조건이다.**
>
> **미확인**: `DT_Skills`의 행이 5행인지 7행인지(프루브 2행 포함 여부) — `DataTableTools.get_row_names`로 **D4에서 확정**. 7행이면 X5 완전 확정 + 대조 범위 7행, 5행이면 대조 범위 5행.

### 5-2. 컬럼별 처리 결정표 (신규 컬럼을 어디에 넣는가)

| 컬럼 | 출처 | 처리 | struct |
|---|---|---|---|
| `Id`·`#id_txt` | live | **존치** | ★**둘 다 struct에 값이 안 들어간다**(`Id`=RowName 소비, `#id_txt`=R-2 규약). 사문 필드 — 런타임 참조 금지 |
| `NameKey`·`DescKey`·`Kind`·`MotionRow`·`Target`·`Cost`·`CooldownTurns`·`Memo` | live | **존치** | 무편집 |
| `Category` · `HitCount` · `PrimaryTargetGroup` | **v2_draft** | **존치**(F7 §3-1 계약). `HitCount`는 §3-4 계약으로 **진짜 부활** | ★**struct 실재 + DT에 값 존재** → 편집 0. **`skills.csv`로 재임포트 시 리셋되는 3필드가 정확히 이것**(R-5) |
| `CastFX`·`ProjectileFX`·`ImpactFX`·`CastSFX`·`ImpactSFX` (String) | v2_draft | **폐기** → 정수 FK 5종으로 교체. **라이브 실측 전 행 `""`이라 위험 0**, F7b ~~⑩b~~ **⑩a**에 합류 | **struct 제거 5**(R-1-A #7~11) |
| ~~`Layer` · `PositionTag`~~ → **`#Layer` · `#PositionTag`** | 30원자 초안 | **존치하되 CSV 전용**(R-2). 소비처 = 린트 · A2 생성기뿐 | ★**struct 편집 0**(X2·X3 해소) |
| ~~`Status`~~ → **`#DevStatus`** | 30원자 초안 | **개명 + CSV 전용.** 개명 사유: `status_effects`/`StatusEffectId`와 **의미 충돌** | **struct 편집 0** |
| **`StagingId`** | 신규 | **추가**(int, FK → `stagings.csv`) | struct 추가 |
| **`FxCastId`·`FxProjectileId`·`FxImpactId`** | 신규 | **추가**(int, FK → `vfx.csv`, `0`=없음) | struct 추가. ★`FxProjectileId`는 "부활"이 아니라 **신규**(Y2) |
| **`SfxCastId`·`SfxImpactId`** | 신규 | **추가**(int, FK → `sfx.csv`, `0`=없음) | struct 추가 |
| `PowerRate`·`EffectType`·`EffectValue`·`EffectDurationTurns`·`EffectChance` | live | **구컬럼 — F7b ~~⑩b~~ ⑩a에서 제거**(⑩a = 구컬럼 제거·비가역·스톱 / ⑩b = 스캐폴드 처분). 30원자 초안의 이 값들은 [[balance1_포지션킷_v1]] §9 매핑표로 `skill_effects.csv` 엔트리에 이관 | **struct 제거 5** — ⑩a 스톱 체크리스트 GREEN이 **선행** |

> ★**병존 창(window) 정정**: ~~"병합 시점(⑩a)에는 병존"~~ → **병존 구간은 ⑩a *이전*이고, ⑩a가 구컬럼을 없앤다.** 그리고 **병합 CSV의 DT 임포트 자체가 ⑩a 이후**(R-5 P6)이므로, **최종 42행 CSV는 구컬럼 5종을 아예 갖지 않는다.** 30원자 초안의 `PowerRate`/`Effect*` 값은 `skill_effects.csv` 이관 **소스로만** 쓰인다.

**★신규 컬럼은 전부 `skills.csv`에 들어간다.** 별도 테이블 분리 없음(판정1 준수 — 4번째 필드만 다른 테이블이면 카드·툴팁·생성기·중복검출이 전부 테이블 분기를 한다).

### 5-3. 최종 `skills.csv` 스키마 v2 (D6 임포트 SSOT — **⑩a** 완료 시점 기준)

~~원안: `Layer`·`PositionTag`·`DevStatus`를 일반 컬럼으로 두고 struct에도 넣는다~~ → **R-2 규약 적용으로 `#` 접두어 3종으로 전환. struct 편집 3건이 0건이 된다.**

```
Id, #id_txt, #Layer, #PositionTag, #DevStatus,
NameKey, DescKey, Kind, Category,
MotionRow, Target, PrimaryTargetGroup, HitCount, Cost, CooldownTurns,
StagingId, FxCastId, FxProjectileId, FxImpactId, SfxCastId, SfxImpactId,
Memo
```

| 집합 | 내역 |
|---|---|
| **CSV 컬럼 22** | 위 전량 |
| **struct 기입 대상 17** | `NameKey`·`DescKey`·`Kind`·`Category`·`MotionRow`·`Target`·`PrimaryTargetGroup`·`HitCount`·`Cost`·`CooldownTurns`·`StagingId`·`FxCastId`·`FxProjectileId`·`FxImpactId`·`SfxCastId`·`SfxImpactId`·`Memo` |
| **RowName 소비 1** | `Id` |
| **CSV 전용 4** | `#id_txt` · `#Layer` · `#PositionTag` · `#DevStatus` |
| **struct 사문 필드 2** | `id` · `idTxt`(값이 영구히 안 들어옴 — 존치하되 참조 금지) |
| **최종 struct 필드 수** | **19** = 17 + 사문 2 |

> ~~(⑩a~⑩b 사이 구컬럼 5 병존)~~ → **정정**: 구컬럼 제거가 **⑩a**이고 병합 CSV 임포트는 **⑩a 이후**이므로, **이 스키마에 구컬럼이 병존하는 시점은 존재하지 않는다**(§5-2 v2 각주).

**어휘 확정 (v2)**

| 컬럼 | 허용 토큰 |
|---|---|
| `Kind` | `PHYS` `MAG` `DEF` `HEAL` **`PASSIVE`(신규)** — ★enum이면 오너 세션 +1(R-1-B) |
| **`#Layer`** | `BASIC` `COMMON` `JOB` **`PASSIVE`** **`PROBE`** — **CSV 전용**. `PROBE`의 의미 = **"A2 생성 풀 제외" 하나뿐**(런타임 차단 아님, R-3-B) |
| `Category` | `ATTACK` `SKILL` `MAGIC` `DEFEND` / ~~`Layer==PASSIVE`면 공란~~ → **`#Layer==PASSIVE`면 `NONE`**(R-1-B: 공란은 enum에서 표현 불가 ∧ "없다"와 "안 채웠다"가 구별 안 된다. `EffectType=NONE` 라이브 선례) |
| **`#PositionTag`** | `SHARED` `TANK` `DEALER` `SUPPORT` `UNIVERSAL` — **CSV 전용** |
| `Target` | `ENEMY1` `ALLY1` `SELF` / **`#Layer==PASSIVE`면 `NONE`** |
| **`#DevStatus`** | `LIVE` `DRAFT` `STUB` `PROBE` — **CSV 전용** |

> **`#Layer=PROBE` 신설 (Director 판정 A — 채택 확정)**: F7b 프루브 2행(`31002000` 버프후공격 · `31003000` 집중일격)은 ⑦·⑧ 게이트의 회귀 자산이라 유지 가치가 있는데, `COMMON`으로 두면 **A2 1000행 생성 풀에 테스트 행이 샌다**. ~~`PROBE` 1토큰이 (a) A2 풀 필터 (b) 메뉴 노출 (c) 조합 유일성 키를 한 번에 차단한다~~ → **2차 개정(R-3-B)**: `#Layer`가 CSV 전용이므로 차단은 **2중**(A2 풀 필터 + L10)이고, **런타임 차단은 없다.** 프루브 2행은 F7b ⑧에서 **평소대로 선택·실행된다 → ⑧ 게이트 영향 0.**

### 5-4. 병합 절차 v2 (기계적 — 실행자용)

> ★**산출 파일 = `data/drafts/skills_merged_42.csv`**(작업 파일). `data/skills.csv` 승격은 **F7b ⑩a GREEN + struct 세션 완료 이후**(R-5 P2). DT 임포트는 **struct 세션 이후 1회만**(P6).

| # | 작업 | 검증 | 계층 |
|---|---|---|---|
| 0 | **P3 스냅샷** — `DataTableTools.get_rows(DT_Skills)` 전 행 JSON 보관 | 파일 존재 ∧ 행 수 기록(5인가 7인가 = Q2) | — |
| 1 | `drafts/skills_30원자_초안.csv` 30행을 **행 집합 기준선**으로 채택 | Id 유일 30개 (LIVE 5 포함) | T1 |
| 2 | **`skills_v2_draft.csv`를 값 기준선으로** `Category`·`HitCount`·`PrimaryTargetGroup`을 **Id 조인**으로 이식 | LIVE 5행 값 존재 → 나머지 25행은 D5가 채움. ★**P4 대조의 기준이 이 파일**이다 | T1 |
| 3 | 프루브 2행(`31002000`·`31003000`)을 **`skills_v2_draft.csv`에서 그대로 가져온다**(별도 작성 아님), `#Layer=PROBE`·`#DevStatus=PROBE` | 총 32행. 3필드 값이 v2_draft와 일치 | T1 |
| 4 | 패시브 10행 추가(§9-4 Id), 판정1-C3 중립값 전량 기입(`MotionRow=-1`·`Target=NONE`·`Category=NONE`) | 총 **42행**. **`MotionRow` 부호 불변식(L02/L03)** | **T1 ★** |
| 5 | 신규 6컬럼(`StagingId`·Fx×3·Sfx×2) 추가, **전 행 `0`으로 초기화** | 컬럼셋 일치(L01·L21) | T1 |
| 6 | 헤더 개명·접두: `Status`→**`#DevStatus`** / `Layer`→**`#Layer`** / `PositionTag`→**`#PositionTag`** | CSV-only 목록과 헤더 집합 일치 | T1 |
| 7 | ~~구컬럼 5종 병존 확인(⑩b 전) — §10 린트 전량 PASS~~ **【자기모순 — 폐기, Y4】** → **구컬럼 5종을 넣지 않는다**(§5-2 v2). 검증은 **T1만** 요구한다 — 액티브 25행은 이 시점에 `StagingId=0`·`Category` 미기입이라 **L03(T3)은 반드시 FAIL하는 것이 정상** | **T1 0건 위반**. T2·T3는 D5·D6 소관(R-6-B) | T1 |
| 8 | 드래프트 2파일 첫 행에 `#DEPRECATED merged-into skills.csv YYYY-MM-DD` | git diff에서 폐기 시점 추적 가능 | — |
| 9 | **P4 임포트 전 대조** — 42행 중 LIVE 5(+프루브 2)행 × struct 대상 필드를 P3 스냅샷과 **필드 단위 대조** | 불일치 **0**이어야 임포트 허가 | **게이트** |

> ⚠ **행 수 대조**: 42행 = 액티브 30 + 패시브 10 + 프루브 2. `PowerRate`·`Effect*` 값의 `skill_effects.csv` 이관은 **F7b가 이미 진행 중인 작업**이며 D1 범위 밖이다(중복 설계 금지).

---

## 6. 신설 테이블 스키마 + `_tables.csv` Id 발급

### 6-1. `stagings.csv` — §3-2에 전문 기재 (4행)

### 6-2. `vfx.csv` (행 목록 = **D4.5**, 본 문서는 스키마만)

| 컬럼 | 타입 | 비고 |
|---|---|---|
| `Id` | int | PK |
| `#id_txt` | String | |
| `TexPath` | **String** | 런타임 로드. SoftObjectPath DT 셀은 전례 0이라 미채택(gameplay 전례 준수) |
| `ColorRow` | int | 시트 내 색상 행 인덱스 (머티리얼 `RowIndex` 파라미터) |
| **`GridX`** | int | ★필수 — 실측: Smear GridX5 / Hit **GridX7**. `FrameCount`만으로 UV 폭 계산 불가 |
| `GridY` | int | 색상 행 수. **라이브 텍스처는 현재 `GridY=1`**(9색 시트 미제작 — D4.5 art-pipeline 판단 항목) |
| `FrameCount` | int | 실측: Smear 5 / Hit **6** (GridX와 불일치 — 별도 컬럼이어야 하는 근거) |
| `FPS` | float | 표시지속 = **`min(FrameCount/FPS, <슬롯예산>) − FxSafetyMarginSec`**(★3차 개정 §3-7 — 라이브 실측 공식 계승). legacy 2행은 **`10` 확정**(MI 직접 조회 + 역산 이중 실증, [[D4.5b_VFX행배정]] §1-3) |
| **`SrcFile`** | String | ★**2차 개정 추가** — 원본 상대경로(`_RawAssets/vfx/...`). `vfx_inventory_raw.csv`와의 **조인 키**. 이게 없으면 `GridX` 오타 1개 = 무음 화면 깨짐을 기계 검증할 방법이 없다(qa TC-N03 선결). legacy 2행은 `""`(UE 애셋 직접 제작물) |
| `Memo` | String | |

> ★**legacy 2행이 필수다 (R-4-B)** — `vfx.csv`는 D4.5의 180장 PNG(GridY=9) 유래 행 **외에** 라이브 `T_FX_Smear`/`T_FX_Hit`(**GridY=1의 별도 제작물**)을 감싸는 **legacy 2행**을 반드시 포함한다. 이관(`FxMotionId`·`FxImpactId`)이 신규 아틀라스 행을 가리키면 **첫 임포트에서 룩이 바뀐다 = 무음 회귀.**

### 6-3. `sfx.csv` (알파 = **슬롯만**, 전 행 참조 0)

| 컬럼 | 타입 | 비고 |
|---|---|---|
| `Id` | int | PK |
| `#id_txt` | String | |
| `SoundPath` | **String** | `vfx.TexPath`와 동형(검증 스크립트·린트 코드 공유) |
| `VolumeMult` | float | 기본 `1.0` |
| `PitchMult` | float | 기본 `1.0` |
| `Memo` | String | |

- **소비 노드는 유효성 체크 후 스킵** — 공란/미발견을 `PlaySound2D`에 물리면 None 참조.
- 훅 지점(신규 훅 0): 모션 SFX → `PlayAttackMotion` 시작 / 캐스트 SFX → 캐스트 FX와 동일 프레임 / 임팩트 SFX → `ResolveHit` 직전. **UI SFX는 범위 밖**(위젯 소유).

### 6-4. `_tables.csv` Id 발급 ★

실측 기존 일련: `2,3,4,5,6,9,10,11,12,13` (`Id = 90000000 + 일련×100`). **다음 가용 = 14, 15, 16.**

| Id | `#id_txt` | TableName | Domain | Version | OwnerSystem | Memo |
|---|---|---|---|---|---|---|
| **90001400** | `entity_stagings` | `stagings` | 개체 | 1 | **연출** | 연출 프로필(접근·카메라·flinch). skills.StagingId→Id |
| **90001500** | `entity_vfx` | `vfx` | 개체 | 1 | **연출** | VFX 시트 파라미터(경로·그리드·프레임·FPS) |
| **90001600** | `entity_sfx` | `sfx` | 개체 | 1 | **연출** | SFX 슬롯(경로·볼륨·피치). 알파 = 참조 0 |

- **`OwnerSystem = 연출`** — `90001100 entity_motions`가 이미 쓰는 값을 그대로 계승(plan 지시: 그 계열로). `Domain`은 `개체`(어휘가 아니라 파라미터 보유 테이블).
- **A2는 일련 17부터 시작**한다(`characters`·`positions`·`position_stats` 발급은 A2 소관 — 본 문서 미발급).
- 애셋 접두사: `T_FX_`·`MI_FX_`는 [[네이밍_폴더_규약]] §1-C에 **이미 예약**되어 있어 그대로 활성. `SFX_`도 §1-C 예약 슬롯에 존재.
  > **[Director 판단 1건]** plan은 §2 도메인 통제어휘에 **`Battle` 도메인 추가**를 예고했다. 그런데 §1-C의 `SFX_` 접두사 + §2의 *"도메인이 애매하면 생략이 원칙"* 을 쓰면 `SFX_Impact_Fire` 형태로 **도메인 추가 없이** 명명이 성립한다. 도메인 신설 여부는 art-pipeline/네이밍 소관이라 본 문서는 **선택지만 제시**하고 확정하지 않는다.

### 6-5. 기존 테이블 델타

| 테이블 | 델타 | struct 세션 |
|---|---|---|
| `motions.csv` | **+`FxMotionId`(int, 0=없음)** · **+`SfxMotionId`(int, 0=없음)** · **−`IsAttackFamily`(폐기, §3-8)**. ★**이관 매핑값은 R-4 표가 17행 전수 지정**(0 초기화하면 RowIndex 5·6·7·8·9·10의 잔상이 무음 소실 — Y6) | **필요**(#3) |
| `status_effects.csv` | ~~+`AuraFxId`(int, 예약 — 전 행 0)~~ → **`#AuraFxId`로 CSV 전용 예약**(R-2). 알파 소비 노드 0이므로 struct에 넣을 이유가 없다 · `DefaultDuration = -1` **허용**(영구 sentinel — **컬럼 실재, 타입 변경 없음 → 편집 0**) | ★**불필요 — 세션 1건 소거**(Y11) |
| `job_stats.csv` | **+`PassiveId`(int) — 6행 전부 `0`**. `SkillIds`는 **무접촉**(R-6-A 픽스처 면제) | **필요**(#2) |
| `skills.csv` | §5-3 v2 | **필요**(#1) |

---

## 7. 패시브 실행 계약 (구현 명세)

### 7-1. 호출 대상 — `ApplySkillEffects` 아님 (판정3)

```
RunEffectBucket(Caster, SkillId, Timing, ContextTargets[])   ← 신규(현행 3-패스 버킷 파라미터화 추출)
ApplySkillEffects(...)  =  쿨다운 세팅 + BattleLog 1줄 + RunEffectBucket ×3(PRE/ON/POST)
패시브                  =  RunEffectBucket(Owner, Owner.PassiveId, <Timing>, [Owner])
                              ↑ 쿨다운 0 · BattleLog 0 → 오라클 diff 무영향
```
⚠ `RunEffectBucket` 추출은 **F7b 수술** → **⑦ 게이트 GREEN 이후 별도 커밋**(⑦ 커밋에 절대 포함 금지 — ⑦은 단독 게이트).

### 7-2. 훅 2종 (알파). 나머지 5종은 **어휘만 예약**

| 훅 | 콜사이트 | 순회 순서 | 진입 조건 | 로그 |
|---|---|---|---|---|
| **`ON_BATTLE_START`** | `StartBattle` 말미 — **턴 큐 정렬 후, 첫 `EnterTurnStart` 전** | **슬롯 인덱스 오름차순**(A1..A4, B1..B4) 고정 = 결정론 | `PassiveId != 0` | `PassiveLog\| phase=BATTLE_START unit=<slot> passive=<id> applied=<n>` |
| **`ON_TURN_START`** | `EnterTurnStart` 말미 — **SKIP_TURN(TS4) 판정 뒤** | 단일 유닛 | `PassiveId != 0` ∧ `bAlive` ∧ `!bBattleOver` ∧ **`!bSkipThisTurn`** | `PassiveLog\| phase=TURN_START unit=<slot> passive=<id> turn=<T> applied=<n>` |

**예약(어휘만, `Timing` 화이트리스트 미포함 — 임포트 FAIL 대상)**: `ON_DAMAGED` · `ON_KILL` · `ON_DEATH` · `ON_HEALED` · `ON_TURN_END`.

> ★**`SKIP_TURN` 시 `ON_TURN_START` 패시브는 발동하지 않는다** (본 문서 확정). 사유 3: (a) `ON_TURN_START`는 *"자기 턴에 무언가를 한다"* = 행동성이 있고, STUN이 행동을 무는 것이 어휘상 자연스럽다 (b) **fail-closed** — 덜 발동하는 쪽이 안전측 (c) plan §패시브 위험이 지적한 *"패시브만 무적"* 문제에 **STUN이라는 카운터 손잡이 1개**를 남긴다. `ON_BATTLE_START`(상시형)는 여전히 카운터 불가이므로 balance 상한(§12)이 그쪽을 담당한다.
> 이견 시 변경 지점 = 위 표의 `!bSkipThisTurn` 조건 1개.

### 7-3. 상시형 패시브의 구현 경로 (신규 코드 0)

```
ON_BATTLE_START → RunEffectBucket → EffectType=STATUS → ApplyStatus(Owner, <영구 상태>)
                                                          ↑ 검증된 기존 함수
이후 곱연산은 GetOutgoingAtkMult / IN_MULT 채널이 자동 처리 → 신규 훅 0
```
- **영구 sentinel**: `status_effects.DefaultDuration = -1`. `TickStatusesAtTurnEnd`에 **분기 1개** 추가 — `if (dur == -1) continue`(차감 안 함).
- `StackPolicy=REFRESH`가 `-1`을 `-1`로 덮으므로 재부여도 무해.
- 사망 시 상태 Clear 경로는 **기존 그대로**(영구 상태도 예외 없이 정리).
- 부수 이득: F7 §8-7(b)가 베타로 미룬 *"지속형 SELF_BUFF skip 마커"* 문제가 **영구 상태엔 만료가 없어 자연 회피**된다.

### 7-4. 재귀 가드 (필수 — 스코프 축소와 무관)

| 항목 | 확정 |
|---|---|
| 필드 | `PassiveReentryDepth : int` (`BP_BattleManager` 멤버) |
| 규칙 | `RunEffectBucket` **패시브 경로** 진입 시 `+1`, 이탈 시 `-1` |
| 상한 | **`> 1`이면 실행 안 함** + `PassiveLog\| reject reason=REENTRY_DEPTH depth=<n>` (fail-closed) |
| 초기화 | `StartBattle`에서 `0`. **`EnterTurnStart` 진입마다 `0`으로 재설정**(이전 턴의 누수가 다음 턴을 영구 차단하지 않도록) |

---

## 8. 엣지케이스 명세 (방어적 설계 — qa D3 입력)

| # | 케이스 | 규정 동작 |
|---|---|---|
| **E1** | `PassiveId = 0` | 패시브 경로 **미진입**. §1-3 PN-1~5 |
| **E2** | `PassiveId`가 존재하지 않는 Id | **린트 L07이 임포트에서 차단.** 런타임 도달 시 `PassiveLog\| reject reason=PASSIVE_ROW_NOT_FOUND` + 스킵(fail-closed) |
| **E3** | `PassiveId`가 패시브가 아닌 행을 가리킴 | 린트 L07b 차단. ★**런타임 판정식 정정(R-3)**: ~~`row.Layer != "PASSIVE"`~~ → **`row.MotionRow >= 0`** → `reason=PASSIVE_LAYER_MISMATCH` + 스킵(fail-closed). `Layer`는 struct에 없다 |
| **E4** | `PassiveId`의 `PositionTag`가 유닛 포지션과 불일치 | 린트 L08 **FAIL**(정체성 붕괴 방지 — "탱 패시브 + 딜 킷" 금지). 런타임 가드는 두지 않음(생성 시점 문제) |
| **E5** | 패시브 소유자가 `ON_TURN_START` 훅 시점에 사망 | `bAlive` **선체크**로 미진입(시체 라이더 방지, F7 §4 계약 승계) |
| **E6** | `bBattleOver == true` 이후 훅 도달 | 미진입. `bBattleOver`는 `ResolveHit` 내부에서만 세팅(H-5 불변식 무손상) |
| **E7** | `SKIP_TURN`(STUN) 턴 | `ON_TURN_START` 패시브 **미발동**(§7-2 확정) |
| **E8** | 패시브 효과가 자기 자신을 재트리거 | `PassiveReentryDepth ≤ 1`에서 정지(§7-4) |
| **E9** | `NotifySkillSelected(패시브Id)` 강제 호출 | §2-4 거절 경로. RG-1~7 전량 |
| **E10** | `NotifySkillSelected(0)` / 미존재 Id | 동일 거절 경로, `reason=SKILL_ROW_NOT_FOUND` |
| **E11** | `HitCount ≥ 2` 도중 타겟 사망 | **판정은 이미 1회로 끝났다**(§3-4). 잔여 임팩트는 **FX·SFX만 재생, `IsTargetFlinch` 스킵**(`bAlive==false` 체크) → 시체 움찔 0 |
| **E12** | 투사 FX 비행 중 타겟 사망 | 투사 FX는 **도착 지점에서 소멸**, 임팩트 FX 재생 후 flinch 스킵. 판정 재실행 0 |
| **E13** | 타겟이 없는 스킬(`Target=NONE`, 패시브) | `ContextTargets = [Owner]` **고정**. `ResolveEntryTargets`에 `NONE` 케이스를 추가하지 않는다(패시브는 `TargetGroup=SELF` 엔트리만 쓴다 — 린트 L05b) |
| **E14** | `StagingId = 0`인 액티브 스킬 | 린트 L03 FAIL. 런타임 도달 시 **`DefaultStagingId`(데이터 멤버) 폴백 + 로그 1줄**. ★fail-loud — 조용히 제자리 타격이 되면 안 된다 |
| **E15** | `AdvanceRatio` 범위 밖 | 임포트 FAIL. 런타임 `Clamp(0,1)` + 로그 1줄 |
| **E16** | `ApproachMotionRow`가 `motions.csv`에 없음 | 린트 L13 FAIL. 런타임 → `-1`(기본 이동 모션) + 로그 1줄 |
| **E17** | `vfx`/`sfx` 행 미발견 | **미재생 + 로그 1줄. 다른 FX/SFX로 폴백 금지**(무음 오작동 차단) |
| **E18** | 동시 사망(A가 B를 죽이고 B의 훅이 A를 죽임) | **알파에서 구조적으로 발생 불가** — `ON_DEATH`/`ON_KILL`이 훅 화이트리스트에 없다(판정4). 베타 도입 시 `EnterTurnEnd` 단일 관문 재검토 필수 |
| **E19** | `Timing=ON_TURN_START` 행이 `ApplySkillEffects` 경로에 섞임 | 린트 L06이 임포트에서 FAIL. **무음 폐기(3-패스 필터 미매칭) 0회** 검증이 D6 게이트 |
| **E20** | 패시브가 `BattleLog` 행수를 바꿈 | 설계상 불가(`RunEffectBucket`은 방출 안 함). **게이트로 실증**: 8기 + 패시브 부여 시에도 `BattleLog\|` 행수 불변 |

---

## 9. UI — 3+1 비대칭 + 문자열 키

### 9-1. `WBP_CharacterCard` 레이아웃 (3+1)

```
┌─ WBP_CharacterCard ───────────────────┐
│  [ 초상 ]   Txt_NameKey               │
│             Txt_Position · Txt_Grade  │
├───────────────────────────────────────┤
│  Txt_SlotActiveLabel   UI_DRAFT_CARD_SLOT_ACTIVE   │
│  ┌──────┬──────┬──────┐                │
│  │Icon_ │Icon_ │Icon_ │  ← 가로 3칸 균등(평타/공용/직업)
│  │Basic │Common│ Job  │                │
│  └──────┴──────┴──────┘                │
├───────────────────────────────────────┤
│  Txt_SlotPassiveLabel  UI_DRAFT_CARD_SLOT_PASSIVE  │
│  ┌────────────────────────────────┐   │
│  │ Icon_Passive  Txt_PassiveName  │   │ ← 1행 **전폭**(비대칭)
│  │               UI_COMMON_PASSIVE_ALWAYS_ON │
│  └────────────────────────────────┘   │
└───────────────────────────────────────┘
```

| 규칙 | 내용 |
|---|---|
| **비대칭 근거** | (a) 4칸 균등이면 *"4번째도 클릭해서 쓸 수 있다"* 고 오독된다 (b) 밴픽에서 패시브는 *"항상 켜져 있음"* 이라 정보 가중치가 다르다 |
| **PvP 공정성** | 패시브가 카드에 **미표시면 밴픽이 정보 불완전**. → 표시 **필수** |
| **`PassiveId == 0`일 때** | 패시브 행을 **숨기지 않는다.** `Icon_Passive` = 빈 슬롯 아트 + `Txt_PassiveName` = **`UI_COMMON_PASSIVE_NONE`**. ★행을 숨기면 카드 높이가 흔들려 그리드 정렬이 깨지고, "패시브가 없다"는 **밴픽 판단 정보 자체**다 |
| 툴팁 | 패시브 아이콘 hover → `Skill.<Name>.Desc` + `UI_COMMON_PASSIVE_ALWAYS_ON` |

### 9-2. 전투 HUD

| 항목 | 확정 |
|---|---|
| 스킬 메뉴 | **Row 3개 고정. 패시브 미노출**(판정2) — `WBP_SkillMenu` 구조 무변경 |
| 발동 피드백 | 패시브 발동 시 **HUD 배지 표시**(`status_effects.IconKey`) + `UI_BATTLE_PASSIVE_TRIGGERED` 토스트 |
| 배지 접두 | `UI_BATTLE_STATUS_AURA_PREFIX` (상시형 패시브 유래 상태를 일반 상태이상과 구분) |
| **오라 VFX** | **알파 STUB 확정.** `EffectQuad` 1개가 캐스트/임팩트에 이미 점유되어 지속 오라 슬롯이 없다. 알파 = **배지만**, `status_effects.AuraFxId` 컬럼만 예약 |
| **로그 분리** | 패시브 로그는 **`PassiveLog\|` 접두어**. ★`BattleLog\|`에 넣으면 오라클 비교기가 **행수부터 FAIL**(`EffectLog\|` 선례) |

### 9-3. 입력 흐름 (패시브가 끼어드는 지점 = 없음)

```
[BATTLE_START] ──(전 유닛 ON_BATTLE_START 패시브)──▶ [TURN_START]
                                                        │
                                         SKIP_TURN? ──yes──▶ [TURN_END]
                                                        │no
                                            (ON_TURN_START 패시브)
                                                        ▼
                                                 [AWAIT_COMMAND]
                                                        │ 클릭(Row 1~3만 존재)
                                              NotifySkillSelected
                                                        │
                              ┌─── 거절(§2-4) ──▶ [AWAIT_COMMAND] 유지 ─┐
                              │                                          │
                              ▼                                          │
                     [AWAIT_TARGET] ─▶ [EXECUTING] ─▶ [RESOLVE] ─▶ [TURN_END]
```
> **패시브는 플레이어 입력 경로에 어떤 노드도 추가하지 않는다.** 입력 흐름의 유일한 변화는 §2-4 거절 가드 1개이고, 그것은 정상 플레이에서 **도달 0회**다.

### 9-4. ★신규 문자열 키 ~~전량 (27종)~~ → **실제 총량 81종** (X4 정정 — 하드코딩 금지)

> ~~"신규 문자열 키 전량 = 27종"~~ 은 **누락이었다**(설계 모순 아님 → Critical 아님, **D5 이관**).
>
> | 구분 | 수 | 배정 |
> |---|---|---|
> | UI 키 | **7** | 아래 표 — D1이 확정 |
> | 패시브 원자 | **20** (10원자 × 2) | 아래 표 — Id·형식은 D1, **이름은 D5** |
> | **액티브 25원자** | **50** (25 × `NameKey`+`DescKey`) | ★**누락분 — §12 B15로 D5 편입** |
> | **프루브 2행** | **4** | ★**누락분 — §12 B15로 D5 편입** |
> | **합계** | **81** | |
>
> ⚠ **선행조건 (Director 판정 X4)**: **`strings.csv` ↔ `ST_UI` 싱크 갭이 원인 미규명 상태로 F7b 이월 중**이다. **그 갭 위에 81종을 얹는다.** 갭이 살아 있는 채로 81키를 넣으면 (a) 어느 키가 안 뜨는 게 갭 탓인지 신규 오타 탓인지 **판별 불가**해지고 (b) L17은 CSV만 보므로 **PASS인데 화면은 빈칸**이 된다. → **B15 착수 전 갭 재현·원인 규명을 선행조건으로 못 박는다**(Q9).

**UI 키 (7종)** — `strings.csv`에 등록. [[UI_화면_규약]] §G `UI_<SCREEN>_<ELEMENT>` 준수.

| 키 | 화면 | 용도 |
|---|---|---|
| `UI_DRAFT_CARD_SLOT_ACTIVE` | Draft | 카드 액티브 3슬롯 구역 라벨 |
| `UI_DRAFT_CARD_SLOT_PASSIVE` | Draft | 카드 패시브 슬롯 구역 라벨 |
| **`UI_COMMON_PASSIVE_LABEL`** | 공용 | "패시브" 표기 |
| **`UI_COMMON_PASSIVE_ALWAYS_ON`** | 공용 | "항상 발동" 배지/툴팁 |
| **`UI_COMMON_PASSIVE_NONE`** | 공용 | ★**`PassiveId==0` 표시** — §1-2 "3 필수 + 1 선택"의 UI 표현. 이 키가 없으면 빈 슬롯이 하드코딩되거나 무표시가 된다 |
| `UI_BATTLE_PASSIVE_TRIGGERED` | BattleHUD | 패시브 발동 토스트 |
| `UI_BATTLE_STATUS_AURA_PREFIX` | BattleHUD | 상시형 패시브 유래 상태 배지 접두 |

> **plan 원안에서 2건 정정**: plan은 `UI_PASSIVE_LABEL`·`UI_PASSIVE_ALWAYS_ON`을 제시했으나 **SCREEN 세그먼트가 없어 §G 규약 이탈**이다. 두 문자열은 Draft 카드와 Battle HUD **양쪽**에서 쓰이므로 `UI_COMMON_` 접두를 채택한다(`common/` 위젯 폴더가 이미 §C-1/§D에 존재해 어휘 정합). **`UI_COMMON_PASSIVE_NONE` 1건은 신규 발견**(Z5의 UI 대응물 — 4슬롯이 항상 채워지지 않는다는 사실이 화면에 표현되어야 한다).

**패시브 원자 키 (20종 = 10원자 × 2)** — `Skill.<Name>` / `Skill.<Name>.Desc`, dot-namespace([[데이터_서버_규약]] §4).

| # | **예약 Id** | `Layer` | `PositionTag` | `NameKey` | `DescKey` | 채널 상한(§12) |
|---|---|---|---|---|---|---|
| P1 | **35000000** | PASSIVE | TANK | `Skill.<D5>` | `Skill.<D5>.Desc` | Def ≤ +3 |
| P2 | **35001000** | PASSIVE | TANK | 〃 | 〃 | 〃 |
| P3 | **35002000** | PASSIVE | TANK | 〃 | 〃 | 〃 |
| P4 | **35003000** | PASSIVE | DEALER | 〃 | 〃 | Atk ≤ +7% · OUT_MULT ≤ +0.10 |
| P5 | **35004000** | PASSIVE | DEALER | 〃 | 〃 | 〃 |
| P6 | **35005000** | PASSIVE | DEALER | 〃 | 〃 | 〃 |
| P7 | **35006000** | PASSIVE | SUPPORT | 〃 | 〃 | 〃 |
| P8 | **35007000** | PASSIVE | SUPPORT | 〃 | 〃 | 〃 |
| P9 | **35008000** | PASSIVE | SUPPORT | 〃 | 〃 | 〃 |
| P10 | **35009000** | PASSIVE | **UNIVERSAL** | 〃 | 〃 | 〃 |

- **`<D5>` = balance-designer가 D5에서 확정하는 원자명.** 본 문서는 **이름을 지어내지 않는다**(근거 없는 값 금지). 확정되는 것은 **Id·Layer·PositionTag·키 개수(정확히 2개/원자)·키 형식**이다.
- **린트 L16**: `NameKey`/`DescKey`가 `strings.csv`에 없으면 FAIL → **CSV 행과 문자열 키는 같은 커밋에 들어간다**(하드코딩 유혹 차단).
- ⚠ **`strings.csv` ↔ `ST_UI` 싱크 갭이 원인 미규명 상태로 F7b 이월 중**이다. 그 위에 27키를 더 얹는다는 것을 인지하고 간다 — D6 착수 시 **싱크 갭 재현/원인 규명이 선행 권고**.

**Id 발급 근거 [확정 필요]**: 실측 `skills.csv` Id 형식 = `3 <Kind> <원자3자리> <변형3자리>` (`1`=PHYS `2`=MAG `3`=DEF `4`=HEAL). **`5`=PASSIVE는 이 관찰에서 유도한 제안**이며, [[데이터_서버_규약]] §4가 *"대분류 자릿수 배분은 오너 확정"* 으로 규정하므로 **오너/Director 확정 대상**이다. `stagings`/`vfx`/`sfx` 행 Id의 `62/63/64` 계열도 동일하게 `motions.csv`의 `6xxxxxxx`(연출 계열)에서 유도한 제안 — **D4.5에서 확정**한다.

---

## 10. CSV 린트 규칙 전량 (실행 가능 형태)

> 실행 주체 = 오프라인 검증 스크립트(`validate_ids.py` 계열, [[네이밍_폴더_규약]] §6). **임포트 전 게이트**. 해당 계층에 FAIL 1건이라도 있으면 그 게이트를 통과시키지 않는다.
>
> ★**2차 개정 3건**: ① **「적용 대상」 열 신설**(어느 규칙이 픽스처에도 적용되는지 구분 안 되면 X1 같은 사고가 반복된다) ② **「계층」 열 신설**(T1/T2/T3 — R-6-B) ③ **L25·L26·L27 신설, L09 범위 축소, L19·L23 정정.**
>
> **적용 대상 어휘**: `skills42`(병합 42행) · `characters`(A2 산출) · `job_stats`(**픽스처 — R-6-A 면제 레지스트리 참조**) · `stagings` · `vfx` · `sfx` · `motions` · `status_effects` · `skill_effects` · `전 CSV`

| # | **적용 대상** | 규칙 | **계층** | 위반 |
|---|---|---|---|---|
| **L01** | skills42 | `#Layer ∈ {BASIC, COMMON, JOB, PASSIVE, PROBE}` | T1 | FAIL |
| **L02a** ★ | skills42 | `#Layer==PASSIVE` → **`MotionRow == -1`** | **T1** | FAIL ★**거절 가드(R-3)의 정확성이 여기 걸려 있다** |
| **L02b** | skills42 | `#Layer==PASSIVE` → `Target=="NONE"` ∧ `StagingId==0` ∧ `FxCastId==FxProjectileId==FxImpactId==0` ∧ `SfxCastId==SfxImpactId==0` ∧ `CooldownTurns==0` ∧ `HitCount==0` ∧ `Cost==0` ∧ ~~`Category==""`~~ **`Category=="NONE"`** ∧ `Kind=="PASSIVE"` | T2 | FAIL |
| **L03a** ★ | skills42 | `#Layer ∈ {BASIC,COMMON,JOB,PROBE}` → **`MotionRow ∈ motions.RowIndex`**(전부 ≥ 0) ∧ `MotionRow != 0` | **T1** | FAIL ★L02a와 함께 **`MotionRow<0` ⟺ 패시브** 양방향 불변식을 성립시킨다 |
| **L03b** | skills42 | 〃 → `StagingId>0` ∧ `HitCount>=1` ∧ `Category ∈ {ATTACK,SKILL,MAGIC,DEFEND}` ∧ `Target ∈ {ENEMY1,ALLY1,SELF}` | **T3** | FAIL (값 배정은 D5 소관 — 병합 직후 FAIL이 **정상**) |
| **L04** | skill_effects | `Timing ∈ {PRE_HIT, ON_HIT, POST_HIT, ON_BATTLE_START, ON_TURN_START}` | T1 | FAIL (★무음 폐기 차단 — plan Z1) |
| **L05a** | skill_effects | 소유 스킬 `#Layer==PASSIVE` → `Timing ∈ {ON_BATTLE_START, ON_TURN_START}` | T2 | FAIL |
| **L05b** | skill_effects | 소유 스킬 `#Layer==PASSIVE` → `TargetGroup=="SELF"` | T2 | FAIL (E13) |
| **L06** | skill_effects | `Timing ∈ {ON_BATTLE_START, ON_TURN_START}` → 소유 스킬 `#Layer=="PASSIVE"` (역방향) | T2 | FAIL (★액티브가 패시브 타이밍을 쓰면 3-패스 필터에서 무음 폐기) |
| **L07a** | **characters + job_stats** | `PassiveId` 셀 **공란 금지**(명시 `0`) | T1 | FAIL |
| **L07b** | **characters + job_stats** | `PassiveId != 0` → skills42에 존재 ∧ `#Layer=="PASSIVE"` | T2 | FAIL |
| **L08** | **characters 한정** | `PassiveId != 0` → `skills42[PassiveId].#PositionTag ∈ {행 포지션, UNIVERSAL}` | T2 | FAIL. ★`job_stats`는 **포지션 컬럼 부재로 평가 불가 → 면제 레지스트리 등재**(Y8). 레지스트리에 없는 파일에서 포지션 축이 없으면 **린트 자체가 FAIL**(fail-closed) |
| **L09** | **characters 한정** ★ | `SkillIds` = 정확히 3토큰 ∧ 각 토큰이 skills42에 존재 ∧ **`#Layer` 순서 == `BASIC;COMMON;JOB`** | T2 | FAIL. ★~~job_stats 포함~~ → **X1 판정으로 범위 축소.** `job_stats`는 회귀 기준선 픽스처라 **면제 + 무접촉**(R-6-A) |
| **L09f** 신설 | **job_stats(픽스처판)** | `SkillIds` = 정확히 3토큰 ∧ 각 토큰이 skills42에 존재. **순서 무강제** | T2 | FAIL. **현재 6/6 PASS** — 무접촉으로 오타 방어만 유지 |
| **L10** | characters + job_stats | `SkillIds`에 `#Layer ∈ {PASSIVE, PROBE}` 토큰 **불포함** | T2 | FAIL (판정2 메뉴 무오염) |
| **L11** | skills42 | `StagingId != 0` → `stagings.csv`에 존재 | T2 | FAIL |
| **L12** | stagings | `AdvanceRatio ∈ [0.0, 1.0]` ∧ 공란 아님 | T1 | FAIL (§4-3) |
| **L12b** 신설 | stagings | 대상이 **아군·자기**인 프로필의 `AdvanceRatio == 0.0` 고정 | T2 | FAIL (Y12 — "치유하러 적진으로 걸어간다" 데이터 차단) |
| **L13** | stagings | `ApproachMotionRow == -1` ∨ `∈ motions.RowIndex` | T2 | FAIL |
| **L14** | stagings | `ApproachArcHeight >= 0` | T1 | FAIL |
| **L15** | skills42 / motions | `Fx*Id != 0` → `vfx.csv`에 존재 | T2 | FAIL |
| **L16** | skills42 / motions | `Sfx*Id != 0` → `sfx.csv`에 존재 | T2 | FAIL |
| **L17** | skills42 | `NameKey`·`DescKey`가 `strings.csv` `Key`에 존재 | T2 | FAIL. **현재 54건 미등록**(X4) → §12 B15가 채운다 |
| **L18** | status_effects | `DefaultDuration >= -1` ∧ `!= 0`. `-1`은 `Family=="SELF_BUFF"` 행만 | T2 | FAIL |
| **L19** ★정정 | vfx | `GridX >= 1` ∧ `GridY >= 1` ∧ `FrameCount >= 1` ∧ `FPS > 0` ∧ ~~`FrameCount <= GridX × GridY`~~ **`FrameCount <= GridX`** ∧ **`FrameCount / FPS > FxSafetyMarginSec`**(3차 개정) | T1 | FAIL. ★원안은 **너무 느슨**했다 — `FrameCount=20, GridX=13, GridY=9`가 통과하는데 그 값은 **다음 색상 행으로 UV가 넘어간다**(한 행 = 한 애니메이션). 실측 180/180이 `FrameCount == GridX`(Y3). ★**마진 하한 절 추가**: 표시지속 = `min(…) − FxSafetyMarginSec`이므로 이 조건이 깨지면 **지속이 0 이하**가 되어 이펙트가 안 보인다(§3-7 3차 개정). 현재 5행 전부 PASS(최소 `5/10=0.5 > 0.05`) |
| **L20** | skills42 | `HitCount ∈ [1, MaxHitCount]` (액티브 한정). `MaxHitCount`는 **L27의 귀결**(R-9) | T3 | FAIL |
| **L21** | skills42 | `Id` 전역 유일 ∧ `#DevStatus ∈ {LIVE, DRAFT, STUB, PROBE}` | T1 | FAIL |
| **L22** | skills42 × (characters+job_stats) | `#Layer==PASSIVE` 행의 `Id`가 **어느 행의 `SkillIds`에도 등장하지 않음**(전역 대조) | T2 | FAIL |
| **L23** ★정정 | skills42 × status_effects | `#Layer==PASSIVE` 행이 참조하는 상태의 누적: `Atk` 배율 합 ≤ **+0.15**(절대) · `OUT_MULT` ≤ **+0.10** · ~~`Def` 가산 ≤ +3~~ **`IN_MULT` 합 ≥ `PassiveInMultFloor`**(R-8 — 가산 채널이 존재하지 않으므로 곱연산 하한으로 재정의. 값은 D5) | T3 | FAIL / **Atk > +0.07(보수)면 WARN** |
| **L24** | 전 CSV | `#id_txt`가 컬럼값에서 자동 생성한 슬러그와 일치 | T1 | WARN ([[데이터_서버_규약]] §4 기존 규약) |
| **L25** 신설 ★ | vfx | **`ColorRow ∈ [0, 5]`**(6~8은 알파 예약 — Director 판정 D) ∧ **`ColorRow < GridY`** | T1 | FAIL. ★~~L01~L24에 `ColorRow` 규칙이 하나도 없었다~~ — 판정 D를 **강제할 규칙이 존재하지 않았다**(Y3). legacy 2행은 `ColorRow=0`·`GridY=1` → PASS |
| **L26** 신설 | **characters 한정** | `SkillIds[2]`(직업 토큰)의 `#Layer=="JOB"` ∧ `#PositionTag ∈ {행 PositionId, UNIVERSAL}` | T2 | FAIL. ★포지션 게이팅이 **패시브(L08)에만 있고 직업 토큰엔 없던 비대칭** 해소(Y8). `job_stats` 면제(R-6-A) |
| **L27** 신설 | skills42 × vfx × stagings | **턴 예산**: `EstimatedTurnSec(skill) <= SlotBudgetSec` (식은 R-9) | T3 | FAIL. ★상수 실값이 **D6 실측 전엔 계산 불가** → 그때까지 **"정의됨·실행 불가"로 표기**(가짜 GREEN 방지) |
| **L28** 신설 | vfx | 각 행의 `SrcFile`이 `vfx_inventory_raw.csv`에 조인되고 `GridX`·`GridY`·`FrameCount` 3값이 **일치** (legacy 2행은 `SrcFile==""`로 조인 면제) | T1 | FAIL (qa TC-N03 선결 해소 — §6-2 `SrcFile` 컬럼 신설) |

**계층 요약**: T1 = L01·L02a·L03a·L04·L07a·L12·L14·L19·L21·L24·L25·L28 / T2 = L02b·L05a/b·L06·L07b·L08·L09·L09f·L10·L11·L12b·L13·L15·L16·L17·L18·L22·L26 / T3 = L03b·L20·L23·L27.

---

## 11. gameplay-engineer 구현 명세 요약 (D6 편집 지점)

> ⚠ **전제 (2차 개정)**: ~~F7b ⑦ 게이트 GREEN 확정 이후~~ → **struct 의존 작업(C1·C2·C3·C4·C5)은 「⑦ 이후」가 아니라 「⑩a 이후」다.** 구컬럼 5종 제거가 ⑩a 수술 자체이고, 오너 struct 세션을 1회로 묶으려면 그 시점이어야 한다(R-1-C). ⑦ 커밋에 FX/Staging 배선을 **절대 포함하지 않는다**는 원칙은 그대로.
> **struct 세션 진입 조건**: **D4.5 완료** ∧ **⑩a 스톱 체크리스트 GREEN**. 세션 중 MCP 의존 작업 금지(함정㉓).

| 커밋 | 편집 지점 | 비고 |
|---|---|---|
| **C1** | `F_SkillsRow` struct 편집 **1회** — **추가 6 / 제거 10**(FX·SFX 문자열 5 + 구컬럼 5) | **R-1-A 표가 오너 지시서다.** 따로 하면 하드레퍼런스 DT 갱신 2회 + 함정㉓ 노출 2회 |
| **C2** | `F_JobStatsRow`에 `passiveId` 추가 · `F_MotionsRow`에 `fxMotionId`·`sfxMotionId` 추가 / `isAttackFamily` 제거 | **C1과 동일 세션 — 권고가 아니라 요구**(세션 수가 비용이다) |
| **C3** | 신규 struct 3종 (`F_StagingsRow`·`F_VfxRow`·`F_SfxRow`) + DT 3종 (`DT_Stagings`·`DT_Vfx`·`DT_Sfx`) | 순수 추가(무파괴). **D4.5 완료 후에만** 만든다(스키마가 흔들리면 2세션). DT 애셋 생성이 MCP로 가능한지는 **미확인(Q8)** |
| ~~C3b~~ | ~~`F_StatusEffectsRow`에 `AuraFxId` 추가~~ | ★**삭제 — `#AuraFxId`로 CSV 전용 예약**(R-1-C). 오너 세션 1건 소거 |
| **C4** | **`FxCastId` + `StagingId` + `FxMotionId` 3개를 같은 커밋** | ~~`IsAttackFamily`가 런지+잔상 **동시** 게이트라~~ → **정정(2026-08-12, [[전진로직_실체_확정]])**: `IsAttackFamily`는 **잔상 단일 축** 게이트(전진은 `Target` 3-way가 별도 통제)다. 근거는 "두 축 분리"에서 **"잔상 단일 축 내부의 이중게이팅 방지"**로 바뀌지만 **결론(한 커밋)은 불변** — 하나만 바꾸면 이중게이팅 또는 게이트 소실. 소비처는 `IfThenElse_10` **단 1개**, `BP_BattleSpawnPoint.EventGraph`라 F7b(`BP_BattleManager`)와 **그래프가 물리적으로 다르다**(⚠동명이인 `IfThenElse_10`이 `BP_BattleManager.ApplySkillEffects`에도 있으나 무관한 `bAlive` 게이트) |
| **C5** | `IfThenElse_4/5/6`의 **Condition 입력 핀만 교체** → `Staging.AdvanceRatio>0` / `Staging.IsTargetFlinch` / `Staging.IsCameraCut AND dist>=CameraCutMinDistance`. **exec 위상 무변경** | §3-5 AND 결합 필수 |
| **C6** | `NotifySkillSelected` **최상위 거절 가드**(§2-4 v2) — 판정식은 **`row.MotionRow < 0`** | 단독으로 검증 가능(BP 1함수). ★`row.Layer` 참조 **금지**(struct에 없다) · PROBE 가드 **없음** · `bDebugProbesEnabled` **신규 멤버 없음** |
| **C7** | `RunEffectBucket` 추출 + `ApplySkillEffects` 래퍼 축소 | **F7b 수술 — 별도 커밋** |
| **C8** | `StartBattle` 말미 + `EnterTurnStart` 말미 훅 2개(§7-2) | 신규 콜사이트 2개 |
| **C9** | `TickStatusesAtTurnEnd`에 `dur == -1` 스킵 분기 1개 | |
| **C10** | 매직넘버 → **명명 상수(데이터 멤버)**: `CameraCutMinDistance`(1.0) · `DefaultLocomotionMotionRow`(2) · `DefaultAttackMotionRow`(5) · `DefaultStagingId`(**62000100**) · `HitIntervalSec` · `ProjectileFlightBudgetSec` · `SlotBudgetSec` · `MaxHitCount` · **`FxSafetyMarginSec`(0.05 — 3차 개정 §3-7)** · **`ApproachSec`(0.4 — 현행 `WalkForward` latent)** | **데이터화 아님, 상수화까지만.** ★`FxSafetyMarginSec`는 라이브 리터럴 `0.45`/`0.55`의 유일한 재현 경로다 — 누락 시 MA-1 무음 회귀 |
| **C11** | `motions.csv` **이중 진실 해소** — Idle/Hurt/Dying의 `RowIndex`·`FrameCount` BP 재하드코딩 제거, DT 조회로 | plan §실태조사 필수 항목 |
| **C12** | ③④ 구현 후 **MA-1(재생 원장 diff 0) 재실행** | 턴 길이 변동 확인 |

**BP 신규 함수 2개**: `RunEffectBucket(Caster, SkillId, Timing, ContextTargets[])` · `ResolveApproachAnchor(Caster, PrimaryTarget) → Vector`(**§4-2 v2 — `Target` 팀 분기 포함**).
**BP 신규 멤버 1개**: `PassiveReentryDepth : int`. ★~~`bDebugProbesEnabled`~~ **불필요**(R-3-B — PROBE 런타임 가드 폐기로 Y7 해소).

**C13 (신규)** — `motions.csv` 이관: R-4 매핑표대로 `FxMotionId` 기입(**6행 전부 `63000100`** · 11행 `0`). **C4와 같은 커밋.** 값은 [[D4.5c_연출SFX행확정]]에서 확정됨(`data/drafts/motions_draft.csv`).

**C14 (신규 — 3차 개정)** — **`PlayHurtReaction` 2분해** (§3-6b (4)). `BP_BattleManager` EventGraph의 `Target=="ENEMY1"` 문자열 게이트를 제거하고 조건 2개로 대체:

```
if (Staging.IsTargetFlinch && Target.bAlive) → Target->PlayFlinch()       [타겟 모션만]
if (Skill.FxImpactId > 0)                    → PlayImpactFx(FxImpactId)   [타겟 VFX만]
```

- ★**두 조건은 반드시 같은 커밋.** C4가 `IsAttackFamily`(★정정 2026-08-12: 잔상 단일 축 게이트, 런지는 무관 — [[전진로직_실체_확정]])에 대해 요구한 것과 **동일한 이유** — 하나만 바꾸면 이중게이팅 또는 게이트 소실이 난다.
- `Target.bAlive`는 **E11**(멀티히트 중 타겟 사망 → 시체 움찔 0)의 실행 지점.
- **회귀 게이트**: 라이브 7행에서 두 조건의 결과가 현행과 **7/7 일치**해야 한다(§3-6b (5) 표). 치유 1행은 §3-6 3차 개정(`FxImpactId=0`)이 있어야 일치한다.
- 그래프 소재지가 `BP_BattleManager`라 **C4(`BP_BattleSpawnPoint`)와 물리적으로 다른 파일**이다 — 별도 커밋 가능.

---

## 12. balance-designer 수치 스키마 (D5가 채울 것)

> **본 문서는 계수를 채우지 않는다.** 아래는 *"어떤 값이 · 어느 컬럼에 · 어떤 타입·범위로 필요한가"* 의 명세다.

| # | 대상 | 컬럼 | 타입/범위 | 행 수 | 제약 |
|---|---|---|---|---|---|
| **B1** | `skills.csv` 액티브 30행 | `StagingId` | int ∈ {62000100, 62000200, 62000300, 62000400} | 30 | §3-2 4행 중 택1. 도약 3원자는 `62000200` |
| **B2** | 〃 | `HitCount` | int ∈ [1, `MaxHitCount`] | 30 | **연출 전용**(§3-4). 데미지 분할은 `skill_effects.csv` 행으로 |
| **B3** | 〃 | `Category` | `ATTACK`/`SKILL`/`MAGIC`/`DEFEND` | 25 (LIVE 5는 기존) | 메뉴 탭 축 |
| **B4** | 〃 | `PrimaryTargetGroup` | `SINGLE_ENEMY`/`SINGLE_ALLY`/`SELF` | 25 | |
| **B5** | 〃 | `FxCastId`·`FxProjectileId`·`FxImpactId` | int (`0`=없음) | 30 | **평타 10종은 전부 `FxCastId=0`**(모션 폴백) → 작업량 0. ★D4.5에서 `vfx.csv` 행이 확정된 뒤에 채운다 |
| **B6** | 〃 | `SfxCastId`·`SfxImpactId` | int | 30 | **알파 = 전 행 `0`**(슬롯만) |
| **B7** | **`skills.csv` 패시브 10행** | `NameKey`·`DescKey` | dot-namespace | 10×2 | `strings.csv` 동시 등록(L17) |
| **B8** | 〃 | `PositionTag` | TANK×3 / DEALER×3 / SUPPORT×3 / UNIVERSAL×1 | 10 | 자유조합 금지 |
| **B9** | 〃 | 나머지 전 필드 | **판정1-C3 중립값 고정** | 10 | L02가 강제 |
| **B10** | `skill_effects.csv` 패시브 엔트리 | `Timing`·`TargetGroup`·`EffectType`·`StatusEffectId` | `ON_BATTLE_START`\|`ON_TURN_START` / `SELF` / `STATUS` / FK | 10~20 | L05a/L05b |
| **B11** ★재정의 | `status_effects.csv` 패시브 상태 | `DefaultValue`·`DefaultDuration`·`Channel`·`StatTarget` | float / **`-1`(영구)** / **기존 4채널만** | 신규 N행 | ~~Def ≤ +3(가산)~~ → ★**가산 채널이 존재하지 않는다**(`SKIP_TURN`/`DOT`/`OUT_MULT`/`IN_MULT` 4종뿐). **Def 패시브 = `SELF_BUFF/IN_MULT/DEF/<음수 float>/-1`**(라이브 `DMG_REDUCE`와 동형 → 신규 코드 0). 상한: 상시 Atk ≤ +7%(보수)/+15%(절대) · `OUT_MULT` ≤ +0.10 · `IN_MULT` ≥ `PassiveInMultFloor`(**D5가 산출**). 상세 = **R-8** |
| **B12** | `status_effects.csv` | ~~`AuraFxId`~~ → **`#AuraFxId`** | int | 전 행 | **전 행 `0`**(예약). ★**`#` 접두어 = CSV 전용**이라 struct 편집 0(R-1-C) |
| **B13** | `stagings.csv` | `ApproachArcHeight`(도약행) | float (uu) | 1 | 초안 `96` — **D4.5 스프라이트 실측 후 확정** |
| **B14** | `job_stats.csv` | `PassiveId` | int | 6 | **전 행 `0`** — 원장 봉인(§1-3). ★`SkillIds`는 **무접촉**(픽스처 면제 R-6-A) |
| **B15** ★신규 | **`strings.csv`** | `Key`·`Ko`(외 로케일) | dot-namespace `Skill.<Name>` / `Skill.<Name>.Desc` | **54행** = 액티브 25원자 ×2 + 프루브 2행 ×2 | ★**X4 이관분.** B7(패시브 20)과 UI 7을 더해 **신규 총 81종**. ⚠ **선행조건: `strings.csv` ↔ `ST_UI` 싱크 갭 원인 규명**(Q9) — 미규명 상태로 81키를 얹으면 L17 PASS인데 화면은 빈칸이 되고 원인 판별이 불가능해진다 |
| **B16** ★신규 | `vfx.csv` legacy 2행 | `FPS` | float | 2 | `FPS = FrameCount / 현행 재생시간`. **현행 재생시간은 D4.5 BP 실측**(Q5). 추측 금지 — 틀리면 MA-1 diff가 깨진다(R-4-B) |

> ★~~**스탯은 "가산"이 아니라 "이전"**: `탱 Def 11 + 패시브 +3 = 14`(✗) 대신 `탱 Def 11 → 8, 패시브 +3`(✓)~~ → **곱연산 재정의(R-8)로 이 예시는 성립하지 않는다.** 원칙(**§7 검산표 총합 불변**)은 유지하되, **`탱 Def 11 → X, 패시브 IN_MULT v`로 유효 피격량이 현행과 같아지는 `(X, v)`를 D5가 산출**한다.
> ★**막기와 곱해진다**: 영구 `IN_MULT` 패시브는 막기(`×0.5`)와 **곱연산으로 중첩**된다(`×0.95 × ×0.5 = ×0.475`). 막기 하드카운터가 **강화**되므로 상성 B 검산에 반드시 반영.
> ★**D5는 D4.5 이후에만 착수 가능** — `vfx.csv`/`sfx.csv` 행이 없는 상태에서 B5/B6을 시키면 **존재하지 않는 Id를 지어낸다.**

---

## 13. 미확인 · 확정 필요 (추정 금지 — 조회 방법 명시)

| # | 항목 | 상태 | 무엇을 조회하면 아는가 |
|---|---|---|---|
| **N1** | 유닛의 `SkillIds` 캐시 여부 / `job_stats` 행 조회 키 | **미확인** | `BP_BattleUnit`(또는 스폰 액터) 멤버 변수 + `WBP_SkillMenu:SetActiveUnit` 그래프 (§2-5 U-1·U-2) |
| **N2** | `ResolveHit` 재진입 가드 유무 | **미확인** | `ResolveHit` 그래프 |
| **N3** | 라이브 `F_SkillsRow` 필드 목록 | ✅ **닫힘 — Director MCP 실측 2026-08-11** | **23필드 확정.** gameplay("8필드 존재")와 D1("CSV 15컬럼")은 **둘 다 맞았다** — struct와 CSV라는 다른 대상을 봤을 뿐이다. 이 실측이 **X2·X3·X5를 새로 드러냈다** → R-1·R-2·R-5 |
| **N4** | `ApproachArcHeight` 실값 | **미확정** | D4.5 — 스프라이트 실측 |
| **N5** | `MaxHitCount` · `SlotBudgetSec` · `ProjectileFlightBudgetSec` 실값 | **미확정** | D6 — 라이브 재생 시간 실측 |
| **N6** | `Kind=PASSIVE → Id 35xxxxxx` 발급 승인 | **확정 필요(오너/Director)** | [[데이터_서버_규약]] §4 "대분류 자릿수 배분은 오너 확정" |
| **N7** | `stagings`/`vfx`/`sfx` **행 Id** 계열(`62/63/64`) | **확정 필요** | D4.5 |
| **N8** | `Layer=PROBE` 신설 vs 프루브 2행 폐기 | **Director 선택 1건** | §5-3 |
| **N9** | 오디오 `Battle` 도메인 신설 vs `SFX_` 접두사만 활성 | ✅ **닫힘 — Director 판정 B**(추가 없음) | §6-4 |
| **N10** | `strings.csv` ↔ `ST_UI` 싱크 갭 원인 | **F7b 이월 중** | ★**§12 B15(81키) 착수 선행조건으로 승격**(Q9) |
| **N11~N19** | 2차 개정 신규 미확정 | — | **R-10 표(Q1~Q9)** 참조 — `Kind`/`Target`/`Category` 타입 · `DT_Skills` 행 수 · `IfThenElse_6` 컷 방향 · 라이브 `HitMID` 재생 범위 · 현행 FX 재생시간 · ⑩a 재임포트 여부 · 임포터 `#`컬럼 경고 · DT 애셋 MCP 생성 가능성 |

---

## 14. 갱신한 외부 문서 (본 작업에서 실제 편집)

| 파일 | 편집 내용 |
|---|---|
| `docs/features/전투완성/raw/SPD원장_오라클_v1.md` | §0 고정입력 표에 **`패시브 슬롯 = 없음(PassiveId=0)` 행 추가** + 각주 1줄. **수치·§4 본표·§7 파싱 CSV 무접촉** |
| `docs/UI_화면_규약.md` | §E `WBP_CharacterCard` 행의 **"스킬3슬롯" → "스킬 3+1 슬롯(액티브 3 + 패시브 1, 비대칭)"** + 각주 |

---

## 산출물 경로

- 본 문서: `D:\unreal\Resource\docs\features\스킬연출구조\raw\D1_4슬롯구조_확정.md` (**2차 개정 2026-08-11**)
- 갱신: `D:\unreal\Resource\docs\features\전투완성\raw\SPD원장_오라클_v1.md` · `D:\unreal\Resource\docs\UI_화면_규약.md`
- 다음: **D4 Director 재판정**(진입 조건 = §3-6 극단 8표본 **+ R-1 오너 struct 목록 확정 + R-10 Q1·Q2 조회**) → **D4.5**(`vfx`(+legacy 2행)/`stagings`/`sfx` 행 확정) → **D5**(balance §12 B1~B16) → **오너 struct 세션 1회**(⑩a 시점, R-1-C) → **D6**(gameplay §11)

### 2차 개정에서 D3(qa)에 되돌려주는 정정 3건

| # | TC | 정정 |
|---|---|---|
| 1 | **TC-N04** | 판정 기준 *"`not a member` 경고 **0건**"* → **`set(경고 컬럼) == set(선언된 CSV-only 컬럼)`**. `#` 컬럼은 정확히 그 경고를 낼 수 있고, 0건을 요구하면 **정상 설계가 FAIL**한다(R-2) |
| 2 | **TC-N01** | *"`bDebugProbesEnabled` 미정의 → 실행 불가"* → **선결 소멸.** PROBE 런타임 가드 자체를 폐기했으므로 검증 항목은 **A2 풀 필터 + L10 2중**만 남고, **F7b ⑧ 영향 0**(R-3-B) |
| 3 | **TC-N07** | 판정 기준 강화 — `FxMotionId>0` 집합 `{5,6,7,8,9,10}` **∧ 그 6행의 값이 전부 동일**. 값이 갈리면 이관이 아니라 **신규 기능이 섞인 것**이므로 FAIL(R-4) · ★**폐기(2026-08-12)** — 목적 소멸, 대체 불변식은 `D3_TC_확정.md` TC-N07 참조 |

## 관련

[[features/스킬연출구조/청사진|청사진]] · [[F7_스킬아키텍처_확정]] · [[F7b_재개계획_초안]] · [[SPD원장_오라클_v1]] · [[balance1_포지션킷_v1]] · [[모션연결_규칙안]] · [[UI_화면_규약]] · [[네이밍_폴더_규약]] · [[데이터_서버_규약]] · [[전투VFX_방향]] · [[상태이상_확정]] · [[언리얼_MCP_실전노하우]]
