---
type: log
feature: 기본전투무대
status: 진행중
updated: 2026-07-05
---

# 🧾 기본전투무대 진행로그

> 시간순 append. 게이트 결과·결정·이슈 기록. 상위: [[청사진]] · [[plan]]
#projectTP/기본전투무대

---

## P0 — 준비 (완료)
- CLAUDE.md에 저비용 모델 룰 + 중간산출물 wiki 기록 룰 추가.
- [[문서화_규칙]] 제정, 허브 등록.
- 기능 폴더 생성, [[청사진]] 작성.

## P1 — 세부 plan (완료)
- art-pipeline(sonnet): 8종 레시피(전 레이어 실측) + 합성순서 + **idle Row0 6프레임**(frameguide+실측 이중검증) → [[P1_레시피_idle조사]]. 발견: weapon5만 색상변형.
- scene-builder(sonnet): "게이트 남쪽 마당" 스팟(Z=486, 국지 기복→snap 권장), 8기 좌표표, **카메라 안1 실증완료**(0,-7550,750 / -6,90,0) → [[P1_무대_배치조사]].
- **Director 결정**: 스케일=셀비율 2.5:1 유지·절대크기는 S3 오너확정(balance 무관) / yaw=전원 정면 / 카메라=안1 / 원본맵 무수정 확인.
- 종합 → [[plan]] 세부 명세(레시피·시트·임포트·머티리얼·배치·카메라·DoD) 작성 완료.

## P2 — 단계별 TC 작성 (완료)
- qa-critic(opus): S1~S5 TC 42개 심각도순 설계 → [[P2_TC설계]]. plan.md에 삽입 완료.
- 핵심 발견: **S2↔S4 지연발현**(FrameCount 오설정은 S4에서만 터짐 → S2 통과해도 S4 재검), 이월 TC 2건(절대스케일·FOV), yaw 정면응시 확정 검증([S3][06]).
- **준비 단계(P0~P2) 완료.** 다음: S1(합성)부터 게이트 진행.

## S1 — 캐릭터 8종 합성 (완료 · 게이트 PASS)
- art-pipeline(sonnet): `compose_party.py`로 8종 합성 → `_composed/party/T_Party_{A1..A4,B1..B4}.png`. [[S1_합성결과]].
- **verifier(haiku) 독립 게이트: TC 8/8 PASS** (해상도·bbox·SHA256·RGBA·weapon5 반영·레이어순서·원본무오염).
- 부가발견: idle=Row0, 다른 행에 RUN/JUMP/ATTACK 등(rows 2·3·4·14·16) — 후속 애니 확장 시 참고.
- 게이트 통과 → S2(UE 임포트·플립북 머티리얼) 진입.

## S2 — UE 임포트 + 플립북 머티리얼 (완료 · 게이트 PASS)
- art-pipeline(sonnet): 텍스처8(Nearest/NoMip/TC_EditorIcon) + `M_Sprite_Flipbook_Lit`(SubUV, FrameCount=6) + MI8. [[S2_임포트머티리얼]].
- **verifier(haiku) 독립 게이트: TC 10/10 PASS.** FrameCount=6 확정(S4 빈프레임 차단), frame0 도트 선명.
- ⚠ **S3 인계**: `SM_SpriteQuad`는 두께0 평면 → **roll=90 필수**. 머티리얼은 컴포넌트 **overrideMaterials**로(set_material 금지). plan §5 반영 완료.
- 게이트 통과 → S3(무대·배치·카메라) 진입.

## S3 — 무대·배치·카메라 (진행중)
- scene-builder(sonnet): `map_battle_village` 복제(원본 무오염), 8기 배치, 카메라 안1.
- 1차 게이트: **세로 왜곡 버그**(회전 yaw=90이 스프라이트를 측면화) + A2 지형 둔덕(+320cm) 튐 발견.
- 수정: 회전 `(0,0,90)`, 스케일 `(6.48,2.59,1.0)`, 8기 Y=-6700 후퇴. 비율 정상화, 전원 화면 안(실측).
- 스크린샷(python decode 파이프라인으로 확보): ![[S3_비율수정.png]]
- ✅ 비율 정상·옥토패스 디오라마 느낌 확인. ⚠ **신규 이슈**: 일부 캐릭터 **가로 스캔라인 글리치**(TAA 의심). hd2d-art-director 진단 중.
- 📌 스크린샷 확보법 확립: `call_tool CaptureViewport` → 결과 tool-results 파일 → python으로 `image.data` base64 decode → PNG → Read.
- 글리치 진단: hd2d(sonnet) TSR→FXAA 변경했으나 무효. **Director가 PIE(Simulate) 캡처로 AA 무관 확정** — TSR·FXAA·PIE 모두 동일 글리치. 원인 = **SubUV UV이동 × 모션블러/velocity 스미어**(저채도 캐릭터 집중). ⚠ hd2d 에이전트에 Bash 없어 스크린샷 검증 불가였음(에이전트 정의 보완 필요).
- **오너 결정: 크기 6.48배 확정 / 글리치 S5 이연.**
- **S3 게이트 PASS**(배치·비율·카메라 — Director 스크린샷 2장 실증 + scene-builder 실측). [S3][G] 글리치만 S5 이월.
- 게이트 통과 → S4(idle SubUV 검증) 진입.

## S4 — idle SubUV 애니 (완료 · 게이트 PASS)
- art-pipeline(sonnet): Simulate 3회 캡처(t=0,+2s,+5s) 비교. [[S4_애니검증]].
- **[S4][02] 빈프레임 깜빡임 없음 = FrameCount=6 실효** (S2↔S4 지연발현 함정 통과). 8기 애니 재생·독립 확인.
- Director 실증: 비교 스택 육안 — 실루엣 변화(재생)+깜빡임 없음. ![[S4_compare_stack.png]]
- [S4][04] 도트무손상은 글리치로 S5 이연. FPS8 유지.
- 게이트 통과 → **S5(룩 패스 + 글리치 해결)** 진입.

## S5 — 글리치 근본원인 추적 (Fable 직접 수사, 오너 특별승인)
- hd2d(sonnet) 16종 배제실험 미해결 → Fable 투입. 오너: "해결 못하면 프로젝트 못함".
- **원인 #1 확정·해결**: 에디터 자동 스크린퍼센티지(디스플레이 기반 다운스케일→업스케일)가 도트 뭉갬. → EditorPerformanceSettings **Manual 100%** 고정. 절반 해결(밝은 캐릭터 선명).
- 배제 확정(증거 기반): 유령액터(8기 확인), 텍스처 임포트 손상(원본=UE 세팅 전수 일치), 섀도우 애크니(태양 그림자 off에도 재현), Lumen GI/Reflection(차단해도 재현), 스페큘러(Rough1/Spec0 적용, 잔존), 모션블러, TSR/FXAA/AA전부, 볼류메트릭 포그(끄고도 재현), VSync 테어링, **머티리얼 그래프(전 노드 판독 — TexCoord 기반 결백)**, MI↔텍스처 매핑(8/8 정확).
- **결정적 판별들**: Unlit 실루엣 완벽(=마스크/UV/지오메트리 결백) / **MI 스왑 테스트: 문제는 자리(트랜스폼)에 결박, 머티리얼 무죄** / 위치·높이(56cm!)·거리에 민감 / 오너 육안으로 에디터 실화면 줄무늬 확인(캡처툴 유령설 폐기).
- **원인 #2 유력(최종 용의자)**: **VRS(Variable Rate Shading)** — 2x2 타일 셰이딩률 저하가 확대 Nearest 도트에 행 뭉갬. 저대비(어두운) 캐릭터 집중(Contrast-Adaptive), 타일 그리드라 위치·높이 민감, 래스터 단계라 머티리얼/AA/라이팅 전부 면역 — 전 증거 정합. HW VRS(project) off 적용했으나 **Software VRS 체인(r.VRS.EnableSoftware=1 + r.Nanite.SoftwareVRS=1) 잔존** → DefaultEngine.ini [SystemSettings]에 3종 킬스위치 기록. **에디터 재시작 필요.**
- 부수 변경(기록): 스프라이트 머티리얼 임시 **Unlit+Emissive(EmissiveBoost)** 전환(아티팩트 내성 — VRS 해결 후 Lit A/B 예정), Roughness=1/Specular=0 추가(스프라이트 정석), 랜턴 161개 그림자 off(주간 무영향), 8기 Y=-7000 이동+카메라 -7850(빈 실험, VRS 확인 후 -6700 복귀 검토), 볼류메트릭 포그 off(분위기용 일반 포그는 유지), MI스왑 원복, PPV GI/Reflection 오버라이드 원복, **전체 저장 완료**.
- VRS(HW+SW) 전부 off + 재시작 → 잔존. 픽셀 법의학(RGB 전수): 줄 행 = 제3의 색(다크네이비) = 스프라이트 팔레트. 평면 레이 스캔: z≥560 교차 지오메트리 0. 펜스 5종 제거 → 무변화(교차 이론 기각).

## 🎯 S5 — 근본원인 확정·완전 해결 (Fable)
- **근본원인: 공면(coplanar) 이웃 쿼드 Z-파이팅.** 폭 648cm 쿼드 8개가 동일 Y평면에서 이웃과 77%(498cm) 겹침 — 몸통 불투명 픽셀끼리도 겹쳐(간격 150 < 몸폭 ~250) 동일 깊이 픽셀이 행 단위로 fight.
- **해결: Y 3cm 계단 배치**(-7000, -7003, …, -7021 — 육안 식별 불가) → **8기 전원 완벽. 줄무늬 0.** ![[S5_글리치해결_최종.png]]
- 전 미스터리 정합: S0·S2 단일쿼드 깨끗(이웃 없음)→S3 8기부터 발생 / 위치·높이 민감=겹침 조합 / MI스왑 자리결박 / 전 렌더설정 면역(지오메트리 깊이 문제) / 어두운 캐릭터 심함(밝은 이웃과 fight 시 대비 최대).
- 진단 부산물(가치): SP Manual100(원인#1, 실재), VRS off(픽셀아트 보호에 유익, 유지), 램프그림자 off, decode_capture.py 파이프라인, MCP 툴 함정 다수 기록.
- 레벨 저장 완료. **오너 육안 승인: "완벽함 — 해결 확인".**
- 오너 결정으로 **Lit 복원**(원래 설계): ShadingModel=DefaultLit, EmissiveBoost=0(파라미터 보존 — 향후 옥토패스식 미세 자체발광 튜닝용), Roughness=1/Specular=0 유지. **Lit에서도 z-fight 재발 없음, 라이팅·캐스트섀도우 정상.** ![[S5_Lit최종.png]]
- 지식베이스 함정표 갱신(★★★ 공면 z-fight — 스폰 로직에 스태거 내장 지침 포함).
