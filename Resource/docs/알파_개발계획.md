---
type: plan
project: projectTP
updated: 2026-07-08
status: 큰 그림 v0 — 오너 논의 중 (승인 후 단계별 features/ 상세화)
---

# 🅰 알파 개발 계획 — 큰 그림

> [[로드맵_버전계획]]의 알파(대기실→전투→결과 클라 단독 루프)를 **시스템 인벤토리 + 작업 flow**로 분해한 버전-레벨 지도.
> 각 단계는 착수 시 `features/<기능>/` 폴더로 상세화한다(청사진→plan→TC→게이트 — [[개발_워크플로우]]).
#projectTP/알파

## 1. 만들어야 하는 시스템 (인벤토리 9종)

| # | 시스템 | 내용 | 기반 |
|---|---|---|---|
| S1 | **캐릭터 코어(데이터)** | 파츠 카탈로그(직업/종족/성별 태그 ← **오너 가이드**)·캐릭터 레시피 `{파츠 ID 5종+메타}`·보유함·최소 스탯 | 신규 — 알파의 척추 |
| S2 | **렌더 조합** | 레시피→외형. 합성 머티리얼(파츠 5레이어, 동일 8×17 그리드 SubUV 공유) | 신규 — **스파이크 필요** |
| S3 | **뽑기** | 버튼 1개 → 랜덤 레시피 생성 → 획득 연출 → 보유 추가 | 신규 — 알파 핵심(오너 명시) |
| S4 | **저장** | SaveGame: 보유 캐릭터·파티 편성(레시피 직렬화) | 신규 |
| S5 | **대기실** | UMG 씬: 보유 목록·4슬롯 편성·뽑기 버튼·출전 버튼 | 신규 |
| S6 | **전투 완성** | HP·데미지·사망·승패 + 스킬 소수 + **편성 파티 스폰**(레시피→전투 유닛) | 기존 확장(턴제전투MVP) |
| S7 | **AI 상대** | 적 커맨드 자동 선택(초기: 랜덤 타겟+공격 수준) | 신규(소형) |
| S8 | **씬 플로우** | GameInstance(편성→전투, 결과→대기실 데이터 운반)·레벨 전환·결과 화면 | 신규 |
| S9 | **UI 기반(UMG)** | UMG 파이프라인 돌파 — SetText 런타임 무반영 이력 해결·MCP 위젯 생성 검증·터치 친화 | 신규 — **스파이크 필요** |
| S10 | **SettingTool** | 개발 중 생기는 각종 세팅(카메라 튜닝 6종류)을 언리얼 안에서 통합 관리하는 에디터 툴 | 신규(소형) — A1부터 점진 구축 |

## 2. 작업 flow (단계·게이트)

```
A0 기술 스파이크 (병렬 3건)            A1 전투 완성 (S6 일부)
 ①합성 머티리얼(S2)                    HP·데미지·사망·승패·스킬 소수
 ②UMG 파이프라인(S9)+클로드디자인 E2E   (현행 씬에서, A0과 병렬)
 ③CSV 데이터 파이프라인(2.5절)
 +규약 3종 초안→확정(2.6절)
      │                                    │
      ▼                                    │
 [오너 게이트] 직업/종족/성별 가이드        │
   ← 파츠 인벤토리 표 제공(작성 지원)       │
      │                                    │
A2 캐릭터 코어 (S1+S4: 파츠DB·레시피·보유·저장)
      │                                    │
A3 뽑기 (S3: 랜덤 생성+획득 연출)          │
      │                                    │
A4 대기실 (S5: 목록·편성·뽑기 UI)          │
      └──────────────┬─────────────────────┘
A5 연결: 편성 파티 → 전투 스폰 + AI 상대 (S6 나머지+S7)
      │
A6 결과 + 씬 플로우 (S8: 루프 폐쇄)
      │
A7 알파 풀테스트 (DoD: 외부인이 설명 없이 루프 완주 + 뽑을 때마다 다른 캐릭터)
```

### 순서 논리 3가지
1. **미지수 조기 제거**: A0 스파이크 2건이 전체 구조를 좌우한다 — 합성 머티리얼이 안 되면 렌더 전략 전환(RenderTarget 굽기/소규모 풀), UMG가 안 뚫리면 UI 전략 전환. 뒤에서 터지면 재작업 폭탄.
2. **오너 가이드 대기 중 공회전 방지**: 전투 완성(A1)은 캐릭터 시스템과 독립(현행 8기 프리셋으로 진행) → A0·오너 가이드와 병렬.
3. **데이터 먼저, UI 나중**: A2(레시피·저장)가 서고 나서 A3(뽑기)·A4(대기실)가 그 위에 얹힌다. A5에서 두 갈래(캐릭터·전투)가 합류.

### 단계별 의존 메모
- A1의 HP 표시: UMG 스파이크(A0②) 결과 나오면 UMG로, 아니면 임시 월드공간 게이지 후 교체.
- A2는 **오너 가이드 + A0①** 양쪽 뒤. 가이드가 늦으면 레시피 스키마에 태그 자리만 예약하고 진행 가능(폴백).
- A5의 "편성 파티 스폰"은 BP_BattleSpawnPoint가 레시피를 받아 외형(S2)+스탯(S1)을 구성하는 접점 — 알파 최대 통합 지점.

## 2.5 데이터 관리 방침 (오너 확정 2026-07-08)

| 데이터 | 방식 | 비고 |
|---|---|---|
| 캐릭터 세팅(파츠 카탈로그·스탯·뽑기 테이블) | **CSV → UE DataTable 임포트** | CSV 원본은 git 추적 위치(`Resource/data/` 제안)에 — Content가 git 제외인 구조에서 **게임 데이터가 버전관리에 들어옴** |
| 스트링 | **CSV 다국어 테이블**(하단 상세) → DataTable로 임포트 | 기존 ST_UI의 set_entry dirty 무음 소실 버그 계열을 **원천 회피**(디스크 CSV = 단일 출처). 기존 4키도 CSV로 이관 |
| 개발 중 생기는 각종 세팅 | **언리얼 내 SettingTool**(S10) 제작해 통합 관리 | 형태(Editor Utility Widget+DataAsset 등)는 착수 시 확정. 현재 흩어진 튜닝값(카메라 6종 등) 수용 |

- CSV↔UE 임포트 파이프라인(DataTable·StringTable)의 MCP 지원 여부는 미검증 → **A0에 ③데이터 파이프라인 프로브 추가**(소형 — 행 스키마 1개 왕복 확인. 첫 실데이터 = strings.csv).

### 스트링 다국어 구조 (오너 확정 2026-07-08 — 알파에 뼈대, 콘텐츠는 ko만)
- **스키마**: `Resource/data/strings.csv` — `Key,ko,ja,en` (컬럼 추가 = 언어 추가. 컬럼명은 ISO 639-1 코드 권장 — UE 문화권 코드와 1:1 대응, 오너 표기 "jp"→"ja")
- **구현**: UE StringTable이 아니라 **DataTable**(행 구조체에 언어 컬럼) 채택. 이유 ①컬럼 확장 자유 ②StringTable dirty·런타임 SetText 버그 이력 회피 ③MCP·BP에서 완전 제어
- **런타임 전환**: GameInstance `CurrentLanguage`(SaveGame 영속) → `GetLocalizedString(Key)` 헬퍼가 현재 언어 컬럼 반환(빈칸 폴백=ko) → 언어 변경 시 `OnLanguageChanged` 브로드캐스트 → 열린 위젯 일괄 재바인딩
- **UI**: 옵션 팝업 `WBP_Options`(대기실에서 열림)에 언어 선택 버튼 — 알파 화면 목록에 오버레이로 추가
- **폰트 메모**: 언어별 글리프 커버(ja 등)는 폰트 선정에 영향 — UMG 폰트 확정(A0②) 시 확장 가능성 고려, 실제 ja/en 폰트 검증은 콘텐츠 추가 시(베타 후보)

## 2.6 사전 규약 3종 (오너 요청 2026-07-08 — A0에서 초안→확정)

A0 완료 시 규약 문서로 확정한다: `UI_제작_가이드.md` + `화면_리소스_규약.md` (designer·qa 검토 포함). 아래는 Director 초안 방향.

### ① UI 제작 가이드 + 클로드 디자인 파이프라인
- 메뉴·HUD류 UI는 전부 **UMG WidgetBlueprint**(월드공간 버튼은 전투 연출 전용으로 강등). MCP로 UMG 생성·배선이 되는가 = A0② 스파이크의 본론.
- **클로드 디자인 → 언리얼 4단 파이프라인**(A0②에 E2E 1장 검증 포함):
  1. **디자인**: 클로드가 HD-2D 톤의 화면을 HTML로 제작 → **DesignSync**로 claude.ai/design 프로젝트에 동기화 — 오너가 브라우저에서 보고 피드백/승인
  2. **스펙화**: 승인 화면을 디자인 스펙(레이아웃 토큰 — 위치·크기·색·폰트·문자열 키 표)으로 추출
  3. **전사**: 스펙 기반으로 MCP가 UMG 위젯 구성(HTML→UMG 자동 변환은 없음 — **규약 기반 재구성**이 현실적) + 이미지 에셋(9-slice 프레임·아이콘)은 PNG 추출→표준 임포트
  4. **문자열**: 전부 CSV 스트링 키(2.5절 — 하드코딩 금지 룰 연결)
- 터치 친화 규칙 포함(최소 터치 타겟 크기, 해상도 앵커링 — 베타 플랫폼 결정 대비).

### ② 화면 정의 (Unity Scene/Prefab ↔ UE 대응 규약)
| Unity 개념 | UE 대응 | projectTP 규약 |
|---|---|---|
| Scene | **Level(.umap)** | 3D 무대가 있는 화면만 레벨로 |
| Prefab(월드) | **Blueprint 액터** | `BP_` (기존 관행) |
| Prefab(UI) | **UMG WidgetBlueprint** | `WBP_` — 화면당 루트 1장 + 부품(버튼·리스트항목·HP바) WBP 분리 |
| 화면 전환 | **GameInstance + OpenLevel / 위젯 스택** | 세션 데이터(편성·결과)는 GameInstance가 운반 |

- **화면(Screen) = "레벨 + 루트 WBP" 쌍**으로 정의. 알파 화면 3종:
  - `SCR_Lobby` 대기실 = 로비 레벨(캐릭터 진열 무대) + `WBP_Lobby`
  - `SCR_Battle` 전투 = `map_battle_octopath` + `WBP_BattleHUD`
  - `SCR_Result` 결과 = 전투 레벨 위 `WBP_Result` 오버레이(레벨 전환 없음 — 연출 연속성)
  - +오버레이: `WBP_Options`(대기실에서 열림 — 언어 선택 등, 2.5절 다국어 구조 참조)

### ③ 리소스 관리 규약
- **3계층 흐름**: `Resource/_RawAssets/`(구매 원본+합성 스크립트, git 제외) → `Resource/data/`(CSV 게임 데이터, **git 추적**) → `projectTP/Content/`(UE 임포트 결과, git 제외 — 재현성은 임포트 표준 문서로)
- Content 폴더 규약: `/Game/{Sprites, Materials, VFX, UI, Data, Blueprints, Stages}/`
- 명명 접두사: `T_` 텍스처 / `M_`·`MI_` 머티리얼 / `SM_` 메시 / `BP_` 액터 / `WBP_` 위젯 / `DT_` 데이터테이블 / `ST_` 스트링테이블
- 임포트 표준: 픽셀퍼펙트 3종 세팅(TF_Nearest·NoMip·EditorIcon) 의무, `import_file` 기존 애셋 삭제 함정 등 [[언리얼_MCP_실전노하우]]의 함정 회피를 규약으로 승격.

## 3. 오너 입력 게이트
| 시점 | 필요한 것 | 지원 |
|---|---|---|
| A2 착수 전 | **직업/종족/성별 파츠 구분 가이드**(오너 예고) | heroes99 파츠 인벤토리 표 제공 → [[파츠_인벤토리]] |
| 상시 | 미적 판단(획득 연출·대기실 룩·전투 타격감) | 단계별 오너 육안 확인(기존 방식) |

## 4. 리스크·미지수 (스파이크로 해소할 것)
- **UMG를 MCP로 만들 수 있는가** — TextRender SetText 런타임 무반영 이력(공격버튼데모). UMG 위젯 생성·바인딩·문자열 테이블 연동이 MCP로 가능한지 미답. → A0②
- **합성 머티리얼 성능·품질** — Masked 5레이어 알파-오버 순서, 파츠 간 z 정렬, 픽셀퍼펙트 유지. → A0①
- **CSV↔DataTable/StringTable 임포트의 MCP 지원** — 미검증. → A0③
- **리소스 재활용 성립 여부** — 파츠 조합 자유도(성별/체형 제약 축)·모션 행 커버리지 실측 중 → [[파츠_인벤토리]] (art-pipeline 조사, 2026-07-08)
- **씬 전환 미답 영역** — 레벨 전환 시 MCP 세션·GameInstance 지속성 함정 미검증. → A6 착수 시 파일럿 프로브(§4 원칙).
- **"스킬 소수"의 범위** — balance-designer 첫 투입(A1)에서 확정(예: 강공격·힐 정도).

## 5. 예약 항목
- **베타 작업 flow 선행 작성**(오너 요청 2026-07-08): 알파 중반(A4 전후)에 같은 틀로 작성 — 네트워크 스파이크(베타 직전) 일정 포함.
- C++ 이관 검토 = 베타 초입(기존 결정 유지).

## 다음 단계
오너 큰 그림 승인 → A0·A1 세부 plan(designer+관련 agent 검토) → 단계 진행. 각 단계 완료 시 wiki 기록+커밋(기존 룰).
