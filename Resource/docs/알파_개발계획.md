---
type: plan
project: projectTP
updated: 2026-07-08
status: 큰 그림 v0 — 오너 논의 중 (승인 후 단계별 features/ 상세화)
---

# 🅰 알파 개발 계획 — 큰 그림

> [[로드맵_버전계획]]의 알파(대기실→전투→결과 클라 단독 루프)를 **시스템 인벤토리 + 작업 flow**로 분해한 버전-레벨 지도.
> 각 단계는 착수 시 `features/<기능>/` 폴더로 상세화한다(청사진→plan→TC→게이트 — [[개발_워크플로우]]).
#projectTP/알파

## 1. 만들어야 하는 시스템 (인벤토리 9종)

| # | 시스템 | 내용 | 기반 |
|---|---|---|---|
| S1 | **캐릭터 코어(데이터)** | 파츠 카탈로그(직업/종족/성별 태그 ← **오너 가이드**)·캐릭터 레시피 `{파츠 ID 5종+메타}`·보유함·최소 스탯 | 신규 — 알파의 척추 |
| S2 | **렌더 조합** | 레시피→외형. 합성 머티리얼(파츠 5레이어, 동일 8×17 그리드 SubUV 공유) | 신규 — **스파이크 필요** |
| S3 | **뽑기** | 버튼 1개 → 랜덤 레시피 생성 → 획득 연출 → 보유 추가 | 신규 — 알파 핵심(오너 명시) |
| S4 | **저장** | SaveGame: 보유 캐릭터·파티 편성(레시피 직렬화) | 신규 |
| S5 | **대기실** | UMG 씬: 보유 목록·4슬롯 편성·뽑기 버튼·출전 버튼 | 신규 |
| S6 | **전투 완성** | HP·데미지·사망·승패 + 스킬 소수 + **편성 파티 스폰**(레시피→전투 유닛) | 기존 확장(턴제전투MVP). **설계 완료**(2026-07-08): [[스탯_전투공식_v1]]·CSV 6종 — 감산공식·스탯5·스킬5·TTK 3타 검증(qa 조건부 GO). 구현은 gameplay-engineer 대기(TakeHit §8 인계) |
| S7 | **AI 상대** | 적 커맨드 자동 선택(초기: 랜덤 타겟+공격 수준) | 신규(소형) |
| S8 | **씬 플로우** | GameInstance(편성→전투, 결과→대기실 데이터 운반)·레벨 전환·결과 화면 | 신규 |
| S9 | **UI 기반(UMG)** | UMG 파이프라인 돌파 — SetText 런타임 무반영 이력 해결·MCP 위젯 생성 검증·터치 친화 | 신규 — **스파이크 필요** |
| S10 | **SettingTool** | 개발 중 생기는 각종 세팅(카메라 튜닝 6종류)을 언리얼 안에서 통합 관리하는 에디터 툴 | 신규(소형) — A1부터 점진 구축 |

## 2. 작업 flow (단계·게이트)

```
A0 기술 스파이크 (병렬 3건)            A1 전투 완성 (S6 일부)
 ①합성 머티리얼(S2)                    HP·데미지·사망·승패·스킬 소수
 ②UMG 파이프라인(S9)+클로드디자인 E2E   (현행 씬에서, A0과 병렬)
 ③CSV 데이터 파이프라인(2.5절)
 +규약 3종 초안→확정(2.6절)
      │                                    │
      ▼                                    │
 [오너 게이트] 직업/종족/성별 가이드        │
   ← 파츠 인벤토리 표 제공(작성 지원)       │
      │                                    │
A2 캐릭터 코어 (S1+S4: 파츠DB·레시피·보유·저장)
      │                                    │
A3 뽑기 (S3: 랜덤 생성+획득 연출)          │
      │                                    │
A4 대기실 (S5: 목록·편성·뽑기 UI)          │
      └──────────────┬─────────────────────┘
A5 연결: 편성 파티 → 전투 스폰 + AI 상대 (S6 나머지+S7)
      │
A6 결과 + 씬 플로우 (S8: 루프 폐쇄)
      │
A7 알파 풀테스트 (DoD: 외부인이 설명 없이 루프 완주 + 뽑을 때마다 다른 캐릭터)
```

### 순서 논리 3가지
1. **미지수 조기 제거**: A0 스파이크 2건이 전체 구조를 좌우한다 — 합성 머티리얼이 안 되면 렌더 전략 전환(RenderTarget 굽기/소규모 풀), UMG가 안 뚫리면 UI 전략 전환. 뒤에서 터지면 재작업 폭탄.
2. **오너 가이드 대기 중 공회전 방지**: 전투 완성(A1)은 캐릭터 시스템과 독립(현행 8기 프리셋으로 진행) → A0·오너 가이드와 병렬.
3. **데이터 먼저, UI 나중**: A2(레시피·저장)가 서고 나서 A3(뽑기)·A4(대기실)가 그 위에 얹힌다. A5에서 두 갈래(캐릭터·전투)가 합류.

### 단계별 의존 메모
- A1의 HP 표시: UMG 스파이크(A0②) 결과 나오면 UMG로, 아니면 임시 월드공간 게이지 후 교체.
- A2는 **오너 가이드 + A0①** 양쪽 뒤. 가이드가 늦으면 레시피 스키마에 태그 자리만 예약하고 진행 가능(폴백).
- A5의 "편성 파티 스폰"은 BP_BattleSpawnPoint가 레시피를 받아 외형(S2)+스탯(S1)을 구성하는 접점 — 알파 최대 통합 지점.

## 2.5 데이터 관리 방침 (오너 확정 2026-07-08)

| 데이터 | 방식 | 비고 |
|---|---|---|
| 캐릭터 세팅(파츠 카탈로그·스탯·뽑기 테이블) | **CSV → UE DataTable 임포트** | CSV 원본은 git 추적 위치(`Resource/data/` 제안)에 — Content가 git 제외인 구조에서 **게임 데이터가 버전관리에 들어옴** |
| 스트링 | **CSV 다국어 테이블**(하단 상세) → DataTable로 임포트 | 기존 ST_UI의 set_entry dirty 무음 소실 버그 계열을 **원천 회피**(디스크 CSV = 단일 출처). 기존 4키도 CSV로 이관 |
| 개발 중 생기는 각종 세팅 | **언리얼 내 SettingTool**(S10) 제작해 통합 관리 | 형태(Editor Utility Widget+DataAsset 등)는 착수 시 확정. 현재 흩어진 튜닝값(카메라 6종 등) 수용 |

- CSV↔UE 임포트 파이프라인(DataTable·StringTable)의 MCP 지원 여부는 미검증 → **A0에 ③데이터 파이프라인 프로브 추가**(소형 — 행 스키마 1개 왕복 확인. 첫 실데이터 = strings.csv).

### 스트링 다국어 구조 (오너 확정 2026-07-08 — 알파에 뼈대, 콘텐츠는 ko만)
- **스키마**: `Resource/data/strings.csv` — `Key,ko,ja,en` (컬럼 추가 = 언어 추가. 컬럼명은 ISO 639-1 코드 권장 — UE 문화권 코드와 1:1 대응, 오너 표기 "jp"→"ja")
- **구현**: UE StringTable이 아니라 **DataTable**(행 구조체에 언어 컬럼) 채택. 이유 ①컬럼 확장 자유 ②StringTable dirty·런타임 SetText 버그 이력 회피 ③MCP·BP에서 완전 제어
- **런타임 전환**: GameInstance `CurrentLanguage`(SaveGame 영속) → `GetLocalizedString(Key)` 헬퍼가 현재 언어 컬럼 반환(빈칸 폴백=ko) → 언어 변경 시 `OnLanguageChanged` 브로드캐스트 → 열린 위젯 일괄 재바인딩
- **UI**: 옵션 팝업 `WBP_Options`(대기실에서 열림)에 언어 선택 버튼 — 알파 화면 목록에 오버레이로 추가
- **폰트 메모**: 언어별 글리프 커버(ja 등)는 폰트 선정에 영향 — UMG 폰트 확정(A0②) 시 확장 가능성 고려, 실제 ja/en 폰트 검증은 콘텐츠 추가 시(베타 후보)

## 2.55 데이터 포맷 판단 — CSV 채택 근거 (오너 재검토 2026-07-08, Director 판정)

**결론: CSV 유지.** 우리 데이터는 전부 **평면 테이블**(행=레코드, 참조는 ID 문자열, 중첩 아님)이라 CSV가 최적.

| 포맷 | UE 임포트 | 손편집 | git diff/머지 | 판정 |
|---|---|---|---|---|
| **CSV** | DataTable 네이티브 | 스프레드시트 최적 | 행 단위 깔끔 | **채택** |
| JSON | 지원 | 나쁨 | 장황 | 평면 데이터엔 과함(중첩 강점 안 살아남) |
| Excel(.xlsx) | CSV export 필요 | 최고 | **바이너리(불가)** | 진실 소스 이원화로 탈락 |
| UE DataTable(.uasset) | 네이티브 | 에디터만 | **Content/ git 제외** | 우리 구조에서 탈락(★버전관리 불가) |

- **.uasset 탈락이 핵심**: "게임 데이터를 git에 넣는다"가 CSV 채택의 근본 이유(`Resource/data/` = 첫 git 추적 게임 데이터).
- **보완 2**: ①다국어 값의 쉼표/개행 → RFC 4180 큰따옴표 이스케이프 규약(UE 임포터 처리 여부는 A0③ 프로브 확인) ②타입·주석 부재 → 검증 스크립트(참조 무결성·#id_txt 대조)+`#` 주석 규약이 메움.
- **예외 조항**: 깊은 중첩 테이블(스킬 이펙트 트리·대화 분기 등)이 생기면 그 테이블만 JSON(UE DataTable은 JSON 임포트도 지원). 기획자 다인화 시 Google Sheets 상위 소스→CSV export 파이프라인(현재 오너 1인이라 오버킬).

## 2.56 스킬 로직 아키텍처 — 스크립트(Lua) 도입 판단 (오너 질문 2026-07-08, Director 판정)

**결론: 알파는 스크립트 불필요. 스킬 효과는 "효과 원자 + 데이터 조합"(데이터 드리븐)으로 심는다.** 오너가 과거 Lua로 푼 두 문제를 우리는 다른 축으로 해결:

| Lua를 쓰던 이유 | 우리의 해법 | 시점 |
|---|---|---|
| ① 핫패치(앱스토어 심사 우회) | **서버 권위 판정**이 구조적으로 해결 — 판정(결과 수치)이 서버에서 확정되면 클라 재심사 없이 서버 배포로 핫패치. 수치는 CSV를 서버가 내려줌 | 베타(서버 도입) |
| ② 복잡한 버프/디버프 | **데이터 드리븐 효과 시스템** — skills.csv의 `EffectType/EffectValue/EffectDurationTurns`를 효과 원자 조합 구조로 확장. 기존 효과 조합이면 새 스킬=데이터만 | 알파부터 |

- **지금 심을 규율**: 스킬 효과를 코드에 하드코딩하지 말고 **효과 원자(enum+파라미터)를 데이터로 조합**한다. 이게 미래 유연성의 90%이며, 언어(BP/C++)와 직교 — C++로 짜도 효과가 데이터 드리븐이면 유연성은 데이터층에서 나온다.
- **스크립트 재검토 = 라이브, 서버가 못 덮는 부분이 남을 때만**. 후보: UnLua·sol2(UE Lua 바인딩, UE 네이티브 Lua 미지원)·UE-Angelscript 포크. 서버 권위+데이터 드리븐이 Lua 용도의 대부분을 선점하므로 실제 도입 가능성은 낮음.
- BP-only MVP 원칙 유지. 전투 코어 C++ 이관은 베타 초입 검토(기존 결정) — 그때도 효과 시스템은 데이터 드리븐 유지.

### 서버 권위의 정확한 범위 (오너 우려 2026-07-08 — "서버가 전투 다 돌리면 비용·속도 터진다")
**서버 권위 ≠ 풀 서버 시뮬레이션.** 오너 우려는 "서버가 모든 스킬 동작을 실행"할 때만 성립 — 우리는 그렇게 안 한다.

| 층 | 담당 | 비용 |
|---|---|---|
| 스킬 **동작**(애니·VFX·카메라·걸어나가기) | **클라**(항상) | 서버 부하 0 |
| 스킬 **결과**(데미지·상태 확정) | 서버 = `max(1,floor(Atk×PR−Def))` 정수 곱셈 몇 번 | 사실상 0 |
| 결과 브로드캐스트 | 서버 → 클라들 | 경량 메시지 |

- **싸게 만드는 특성**: 턴제/ATB(초당 60틱 물리 아님, 이벤트성)·소규모 4v4(8유닛/매치)·결정론 공식(부동소수점 시뮬 없음) → 판정 서버 1대가 수천~수만 동시 매치 감당.
- **진짜 병목 = 계산이 아니라 동시 세션 관리·라우팅.** 클라로 계산 빼도 안 풀림 → 아키텍처(리슨서버 P2P↔전용서버, 매치메이킹, 지역분산)로 해결.
- **클라 예측 + 서버 확정**: 결정론이라 예측=확정 거의 일치 → 롤백 없음. 레이턴시(100~200ms)는 걸어나가기+컷 연출이 은폐(턴제라 관대).
- **실측은 베타 네트워크 스파이크**: "매치당 판정 비용"·"서버당 동시 세션 한계"를 숫자로 뽑아 규모 산정([[로드맵_버전계획]] 스파이크 항목).

### 클라 표시 데이터의 hotfix — 원격 데이터 배송 (오너 질문 2026-07-08)
클라가 표시하는 데이터(스킬 설명·value·밸런스 수치)를 앱스토어 재심사 없이 갱신하려면 **바이너리에 굽지 말고 런타임 원격 페치**. 순수 데이터라 **앱스토어 정책상 합법**(로직 원격 갱신만 회색지대 4.7 — Lua가 걸렸던 지점. 우리 value·텍스트는 로직 아님).

| 원격 갱신 | iOS 정책 |
|---|---|
| 데이터(설명·value·테이블·이미지) | ✅ 명시적 허용(라이브 게임 표준) |
| 실행 로직(BP·스크립트) | ⚠ 회색(4.7) → 효과 원자 데이터 조합으로 회피 |

- **배송 흐름**: 클라 시작 → `_tables.csv`의 `Version` 확인 → 로컬<원격이면 CDN에서 CSV 다운로드 → 런타임 DataTable 채움 → **원격 실패 시 구운 첫 출시 스냅샷 폴백**(오프라인 대비).
- **로컬 내장 데이터 = 정식 요구사항(오너 확정 2026-07-08)**: 원격 없이도 게임이 돌아야 한다. 필요 이유 ①첫 실행(아직 못 받음) ②오프라인·CDN 장애 ③**알파는 서버 없어 로컬이 유일 소스** ④부팅 속도(네트워크 왕복 대기 없이 즉시 시작, 원격은 백그라운드 갱신).
- **3계층 우선순위**: **다운로드 캐시(최신) > 클라 내장(출시 스냅샷) > (서버=PvP 판정 authoritative, 표시와 별개)**. 부팅은 로컬로 즉시 시작 → 백그라운드로 원격 Version 확인 → 최신이면 캐시 저장(다음 부팅부터 적용) → 실패면 로컬 유지. **로컬 내장 = 우리 `Resource/data/` CSV가 클라 패키지에 구워진 것** — 별도 제작 아님, 단일 출처가 폴백 역할 겸함.
- **낡음 안전장치(베타 예약)**: 앱 장기 미업데이트 시 로컬 내장본이 크게 낡을 수 있음 → 원격 데이터에 "요구 최소 클라 버전" 표시로 업데이트 유도. 지금은 자리만 예약.
- **이미 절반 완료**: CSV 단일 출처(`Resource/data/`)+`Version` 컬럼이 토대. 남은 것 = 런타임 원격 페치 계층(UE HTTP 모듈 / Chunk Downloader) — **베타 추가**, 알파는 구워서 배포(구조만 CSV 분리 유지 = 로컬 내장 그 자체).
- **안전망(PvP)**: 클라 value = 표시용, 진짜 판정 = 서버 데이터 → 클라 변조·낡음도 결과는 서버 확정. 단 UX 어긋남 방지 위해 **같은 CSV를 서버·클라 양쪽 단일 배송**.

## 2.6 사전 규약 3종 (오너 요청 2026-07-08 — A0에서 초안→확정)

A0 완료 시 규약 문서로 확정한다: `UI_제작_가이드.md` + `화면_리소스_규약.md` (designer·qa 검토 포함). 아래는 Director 초안 방향.

### ① UI 제작 가이드 + 클로드 디자인 파이프라인
- 메뉴·HUD류 UI는 전부 **UMG WidgetBlueprint**(월드공간 버튼은 전투 연출 전용으로 강등). MCP로 UMG 생성·배선이 되는가 = A0② 스파이크의 본론.
- **클로드 디자인 → 언리얼 4단 파이프라인**(A0②에 E2E 1장 검증 포함):
  1. **디자인**: 클로드가 HD-2D 톤의 화면을 HTML로 제작 → **DesignSync**로 claude.ai/design 프로젝트에 동기화 — 오너가 브라우저에서 보고 피드백/승인
  2. **스펙화**: 승인 화면을 디자인 스펙(레이아웃 토큰 — 위치·크기·색·폰트·문자열 키 표)으로 추출
  3. **전사**: 스펙 기반으로 MCP가 UMG 위젯 구성(HTML→UMG 자동 변환은 없음 — **규약 기반 재구성**이 현실적) + 이미지 에셋(9-slice 프레임·아이콘)은 PNG 추출→표준 임포트
  4. **문자열**: 전부 CSV 스트링 키(2.5절 — 하드코딩 금지 룰 연결)
- 터치 친화 규칙 포함(최소 터치 타겟 크기, 해상도 앵커링 — 베타 플랫폼 결정 대비).
- **UI 파일 관리** (git 추적 = `Resource/ui/`가 단일 출처, DesignSync 프로젝트는 오너 리뷰 창구):
  ```
  Resource/ui/
  ├─ screens/<화면명>/       SCR_Lobby 등 화면 단위 폴더
  │  ├─ design_v<N>.html    클로드 디자인 목업(버전별 보존)
  │  ├─ approved.html       오너 승인본 스냅샷(전사 기준 고정)
  │  └─ spec.md             전사 스펙 — 레이아웃 토큰 표(UMG 재구성의 단일 기준)
  ├─ components/             공용 부품 목업(버튼·팝업 프레임 등)
  └─ assets/                 추출 PNG(9-slice·아이콘) — UE 임포트 원본
  ```
  - Content 측: `/Game/UI/{Screens,Components,Textures}/` — `WBP_<화면명>`·`WBP_<부품명>`·`T_UI_<이름>`
  - 순서 강제: **spec.md 없이 UMG를 만들지 않는다**(디자인→승인→스펙→전사). 승인본과 스펙이 어긋나면 스펙이 기준.

### ② 화면 정의 (Unity Scene/Prefab ↔ UE 대응 규약)
| Unity 개념 | UE 대응 | projectTP 규약 |
|---|---|---|
| Scene | **Level(.umap)** | 3D 무대가 있는 화면만 레벨로 |
| Prefab(월드) | **Blueprint 액터** | `BP_` (기존 관행) |
| Prefab(UI) | **UMG WidgetBlueprint** | `WBP_` — 화면당 루트 1장 + 부품(버튼·리스트항목·HP바) WBP 분리 |
| 화면 전환 | **GameInstance + OpenLevel / 위젯 스택** | 세션 데이터(편성·결과)는 GameInstance가 운반 |

- **화면(Screen) = "레벨 + 루트 WBP" 쌍**으로 정의. 알파 화면 3종:
  - `SCR_Lobby` 대기실 = 로비 레벨(캐릭터 진열 무대) + `WBP_Lobby`
  - `SCR_Battle` 전투 = `map_battle_octopath` + `WBP_BattleHUD`
  - `SCR_Result` 결과 = 전투 레벨 위 `WBP_Result` 오버레이(레벨 전환 없음 — 연출 연속성)
  - +오버레이: `WBP_Options`(대기실에서 열림 — 언어 선택 등, 2.5절 다국어 구조 참조)

### ②-b CSV 기획 데이터 규약 — top-down (오너 확정: 마스터→세부 분해 + 상위→하위 단방향 참조)
- **테이블 3분류와 계층**:
  1. **마스터 인덱스** `_tables.csv` — 전체 테이블 목록(테이블명·버전·소유 시스템·설명). 새 테이블은 반드시 여기 등록(총괄시트).
  2. **정의(어휘) 테이블** — enum성 정의: `jobs.csv`·`races.csv`·`genders.csv`·(추후 `rarities.csv` 등). Id+NameKey 수준. 모든 테이블이 **값으로 사용 가능**(어휘 사용은 참조 계층 위반이 아님).
  3. **개체 테이블** — 조립 개념이 위, 부품이 아래: 레시피/캐릭터 규칙 → `parts.csv` → … **위가 아래의 Id를 보유**하고, 아래는 위를 모른다. 역참조·순환·형제 간 횡참조 금지.
- **동일 스키마명(명명 사전)** — 같은 의미 = 어느 테이블에서든 같은 컬럼명:
  - 0번 컬럼은 항상 `Id`(UE DataTable RowName) — **세그먼트 숫자 ID**(전역 유일, 오너 확정 2026-07-08). 예시 8자리: `[1 대분류][2 타입][1 성별][1 등급][3 일련]` → `10112001` = 캐릭터·직업01·남·2등급·001. 대분류: 1=캐릭터 2=파츠 3=스킬 4=아이템…(자릿수 배분은 축 확장 한계를 고려해 오너 확정).
  - **스마트 ID 안전장치 2규칙**: ①ID에 넣는 축은 **불변 취급**(기획 변경 시 신규 ID 발급+구 ID 폐기 — 가변값(성장 레벨 등)은 ID 인코딩 금지) ②**로직은 ID를 파싱하지 않는다** — 코드의 진실은 항상 컬럼, ID 세그먼트는 가독성·유일성·발급 규칙용(이중 진실 방지).
  - **런타임 인스턴스 ID는 별개**: 뽑기로 생성된 개별 캐릭터(SaveGame) 등 동적 개체는 스마트 ID가 아니라 단순 증가값/GUID 사용.
- **`#` 주석 규약(오너 확정 2026-07-08)**: `#`로 시작하는 **컬럼 헤더** = 주석 컬럼, `#`로 시작하는 **행(첫 셀)** = 주석 행 — 게임/임포터가 읽지 않음(임포트 전처리에서 제거, A0③ 파이프라인에 포함).
  - **`#id_txt`**: Id 바로 옆에 두는 사람용 번역 컬럼 — 숫자 ID의 중요 정보를 슬러그로: `10112001` → `char_knight_m_rare`, `24150100` → `part_cloth_s15_c1`.
  - `#id_txt`는 데이터 진실이 아님(진실은 컬럼) — 손으로 쓰면 시간이 지나며 거짓말하므로 **검증 스크립트가 컬럼에서 자동 생성/대조**(불일치 리포트)하는 것을 표준으로.
  - 외래키 = `<대상단수>Id`(`SkillId`), 다중 = `<대상단수>Ids`(구분자 `;`).
  - 문자열은 원문 저장 금지 — 항상 `NameKey`/`DescKey`(strings.csv 키).
  - 수치는 단위 내장: `Hp`·`Atk`·`CooldownSec`·`Rate`(0~1). 불리언 `Is~`/`Can~`. 기획 메모는 `Memo`(게임 미사용).
  - 파일명 snake_case(`parts.csv`) ↔ DataTable `DT_Parts` 1:1.
- **순환 예외(오너 확정 2026-07-08)**: 순환이 본질인 도메인은 예외 허용 — 예) 랜덤박스: `items` → 박스 내용(`rewards`) → reward가 다시 `items`를 참조. 단 ①예외는 `_tables.csv`에 "순환 예외" 표기로 **명시 등록**(무단 순환 금지) ②구현 시 무한 재귀 가드(중첩 깊이 상한) 필수.

### ③ 리소스 관리 규약
- **3계층 흐름**: `Resource/_RawAssets/`(구매 원본+합성 스크립트, git 제외) → `Resource/data/`(CSV 게임 데이터, **git 추적**) → `projectTP/Content/`(UE 임포트 결과, git 제외 — 재현성은 임포트 표준 문서로)
- Content 폴더 규약: `/Game/{Sprites, Materials, VFX, UI, Data, Blueprints, Stages}/`
- 명명 접두사: `T_` 텍스처 / `M_`·`MI_` 머티리얼 / `SM_` 메시 / `BP_` 액터 / `WBP_` 위젯 / `DT_` 데이터테이블 / `ST_` 스트링테이블
- 임포트 표준: 픽셀퍼펙트 3종 세팅(TF_Nearest·NoMip·EditorIcon) 의무, `import_file` 기존 애셋 삭제 함정 등 [[언리얼_MCP_실전노하우]]의 함정 회피를 규약으로 승격.

## 3. 오너 입력 게이트
| 시점 | 필요한 것 | 지원 |
|---|---|---|
| A2 착수 전 | **직업/종족/성별 파츠 구분 가이드**(오너 예고) | heroes99 파츠 인벤토리 표 제공 → [[파츠_인벤토리]] |
| 상시 | 미적 판단(획득 연출·대기실 룩·전투 타격감) | 단계별 오너 육안 확인(기존 방식) |

## 4. 리스크·미지수 (스파이크로 해소할 것)
- **UMG를 MCP로 만들 수 있는가** — TextRender SetText 런타임 무반영 이력(공격버튼데모). UMG 위젯 생성·바인딩·문자열 테이블 연동이 MCP로 가능한지 미답. → A0②
- **합성 머티리얼 성능·품질** — Masked 5레이어 알파-오버 순서, 파츠 간 z 정렬, 픽셀퍼펙트 유지. → A0①
- **CSV↔DataTable/StringTable 임포트의 MCP 지원** — 미검증. → A0③
- ~~리소스 재활용 성립 여부~~ → **실측 완료, 성립 판정**(2026-07-08): 761개 전수 캔버스 일치·모션 17행 전 파츠 커버 100%·구조적 제약축 없음(단일 바디, hair m/f는 라벨일 뿐). "직업/성별 안에서만"은 순수 오너 정책으로 설계 — 상세·오너 결정 8항목: [[파츠_인벤토리]]
- **씬 전환 미답 영역** — 레벨 전환 시 MCP 세션·GameInstance 지속성 함정 미검증. → A6 착수 시 파일럿 프로브(§4 원칙).
- **"스킬 소수"의 범위** — balance-designer 첫 투입(A1)에서 확정(예: 강공격·힐 정도).

## 5. 예약 항목
- **스프라이트 상시 빌보딩 옵션**(오너 리포트 2026-07-08, village 자유 카메라에서 종이 현상): 스프라이트는 두께 0 평면 쿼드라 카메라가 돌면 종이로 보임 — 구조상 정상. 전투 OTS 컷은 스냅 정렬(`AlignSpriteToCamYaw`)로 기해결. 카메라가 움직이는 화면(로비 진열대 등)엔 **상시 빌보딩**(틱마다 yaw 정렬 — 기존 함수 재사용, 옥토패스 방식) 적용. 트레이드오프: 라이팅/그림자 방향 미세 변화 — 적용 시 오너 육안 확인. 소관: hd2d([[카메라연출_원칙]] 빌보딩 절).
- **베타 작업 flow 선행 작성**(오너 요청 2026-07-08): 알파 중반(A4 전후)에 같은 틀로 작성 — 네트워크 스파이크(베타 직전) 일정 포함.
- C++ 이관 검토 = 베타 초입(기존 결정 유지).

## 다음 단계
오너 큰 그림 승인 → A0·A1 세부 plan(designer+관련 agent 검토) → 단계 진행. 각 단계 완료 시 wiki 기록+커밋(기존 룰).
