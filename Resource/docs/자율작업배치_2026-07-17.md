---
type: ops_plan
project: projectTP
scope: 오너 부재 중 자율 선진행 배치
status: v2 — qa 적대 피드백 반영(H1~H3·M1~M4·L1~L3 전 수용) — **전 트랙 실행 완료**(2026-07-17). SP1(파트4 봉인, 커밋 `c237ad7`·`81dcb47`·`81daffe`, 태그 `save/파트4_라벨힐`) · SP2a(SPD 오라클, `66659db`, 태그 `save/SP2a_오라클`) · SP2b(백업 결정패키지, `50e6da0`, 태그 `save/SP2b_백업패키지`) · SP2c(balance#1 초안, `76a3a23`, 태그 `save/SP2c_balance1초안`) · SP3(F7b 재개계획, `f654a5f`, 태그 `save/SP3_F7b계획`) · 2d(문서 정합화 — 본 작업, 2026-07-17) 전부 완료. **잔여 = 오너 게이트 S1~S5**(아래 §스톱 지점).
created: 2026-07-17
updated: 2026-07-17
approved_by: 오너 ("내가 필요 없는 부분만 선진행, 단계별 세이브포인트, 오너 확인 필요 지점 정지")
---

# 자율 작업 배치 — 2026-07-17 오후

> 근거: MVP 가이드(§3.2 백업 게이트·§5 완료기준·§7 지시서 형식) + qa A1 판정(파트4 미봉인·P2-R01 무감각 게이트 교정) + balance 착수 판정(#1·#2 선결 해제).

## 대원칙 (이 배치의 안전 속성)

- **이 배치는 uasset을 1바이트도 수정하지 않는다.** 전 트랙이 문서·CSV·백업 복사·검증뿐. PIE도 안 돌린다.
- **세이브포인트** = 스테이지별 git 커밋+태그(`save/SP<N>_<이름>`) + uasset은 파일 복사 백업(git 밖이므로). 롤백 = `git revert`(문서) / 파일 복원(uasset — 단 이번 배치에선 수정 자체가 없어 복원 시나리오 없음).
- 커밋은 **정확한 경로만** (`git add -A`·`.`·`-a` 금지). stray `D:\unreal\docs` 무접촉. push 없음.
- 지시서는 가이드 §7 형식(Goal/Non-goals/Allowed/Invariants/Verification/Stop).

## 스톱 지점 (오너 복귀 후에만)

| # | 항목 | 이유 |
|---|---|---|
| S1 | **even-trade 런(기본공격만·최저 인덱스, ~5분) + F9b 풀플레이(5스킬+광폭화, ~10분)** — 세션 분리(로그 롤오버) | 원장 봉인·A1 닫기는 오너 플레이가 곧 수집 수단 |
| S2 | **first-party 백업 방식 결정** (Git LFS / 파일 스냅샷 / Perforce) | 가이드 §3.2 오너 결정 사항 (Codex FULL-001) |
| S3 | **balance#1 결재** — 등급 3단계 유지? 직업 축(전사/마법사) 존치/흡수? | 오너 결정 2건. 자율 구간은 "안(proposal)"까지만 |
| S4 | **F7b 착수 승인** | 기준선(원장) 봉인 선행 원칙(가이드 §2.2) — S1 뒤에만 가능 |
| S5 | **신규 오라클 정본화 확인** — S1 실행 카드에 "diff 대상은 구 F9a가 아니라 신규 SPD 오라클(오너 검증 대기)" 명시, 오너 확인 후 정본 | qa Q1/L3 — 자율 유도 원장이 오너 미확인 상태로 "정답의 정의"가 되면 안 됨 |

**배치 중 즉시 정지(halt) 트리거** (오너 복귀 대기와 별개): ① uasset 백업 복사 전 대상 파일 mtime이 Director 검증 기록(13:03~13:38)과 불일치 → 중단·보고 ② 복사 후 SHA256 원본↔사본 불일치 → 중단·보고 ③ 편집 대상 문서에서 타 에이전트 동시 수정 흔적 → 보류·보고.

## Stage 1 — 파트4 봉인 [SP1] (진행 중)

- **실행**: sonnet(문서 세트 — 완료문서·TC frontmatter·plan/청사진 반영·F0 TC 상태·노하우 §34 신규·st_ui.csv 미러·motions.csv 메모 정정) — **이미 가동 중**
- **Director 게이트**: 산출물 스팟 검증 → 커밋 3분할
  ① `[C] chore: CLAUDE.md 문서갱신 룰` ② `[C] docs(파트4): 봉인 세트` ③ `[C] docs: codex_review 미러 추적 시작(MVP 가이드 포함)`
- **태그**: `save/파트4_라벨힐` (② 뒤)
- **백업**: 파트4 터치 uasset 5종(BP_BattleManager·WBP_SkillMenu·BP_BattleSpawnPoint·ST_UI·map_battle_octopath) 파일 복사 + 크기·mtime 대장 기록. 경로는 **기존 `_savepoints/` 컨벤션 확인 후 그에 따름**(gameplay가 `_savepoints/F7b_인터프리터골격/` 실존 언급 — Glob으로 위치 확정; 없으면 `D:\unreal\backups\2026-07-17_SP1_파트4\`)
- **Task**: #97 완료 처리

## Stage 2 — 병렬 3트랙 (plan 업그레이드 후 배포)

### 2a. 신규 기대 원장(오라클) 손계산 [SP2a]
- **balance-designer**: SPD 순서(이름1~8 = A3·B3·A4·B4·A1·B1·A2·B2, spd 97~90) + 고정 입력 정책(기본공격 31000000만·살아있는 적 중 최저 인덱스)으로 even-trade 전판을 **게임 코드 없이** 결정론 공식(스탯_전투공식_v1)만으로 유도. 총 턴 수는 23이 아닐 수 있음 — 유도 결과가 정의.
- **qa-critic 교차검증**: 오라클 자체의 산술·규칙 오류 적대 검증 (오라클이 틀리면 diff가 헛돎 — F9a 때도 정답지는 balance 산출+qa 검증이었다)
- **산출물**: `docs/features/전투완성/raw/SPD원장_오라클_v1.md` (턴별 공격자·대상·dmg·hpAfter·died 표 + 유도 근거) + 오너용 실행 카드(런 절차·로그 롤오버·중단 기준 — 가이드 §2.1 형식)
- **자동수집(Method A)은 이번 수집에 쓰지 않는다**: gameplay 조사로 `NotifySkillSelected(SkillId)`·`NotifyUnitClicked(Unit)`이 위젯 없이 직접 호출 가능한 순수 함수임이 실증됐고, `EnterAwaitCommand`/`EnterAwaitTarget` 말단 훅(~20-40노드, `bAutoScenarioActive` 기본 false)으로 클릭 0회 자동 재생이 가능하다. **그러나** BP 수정이 필요해 "검증 전용 동결"(가이드 §2.1)과 이 배치의 uasset 무접촉 속성을 깬다. 기준선 수집은 오너 수동 1판(~5분)이 리스크 0. **Method A는 F7b·A2 회귀 인프라로 이관** — 기준선 봉인 후 구축하고, 오너 원장과 diff 0으로 도구 자체를 먼저 검증한 뒤 이후 회귀 반복 실행에 쓴다.
- **커밋+태그**: `save/SP2a_오라클`

### 2b. 백업 결정 패키지 + 즉시 임시 백업 [SP2b]
- **sonnet**: ① first-party 에셋 전수 목록·크기 실측(가이드 §3.2 최소 보존 대상 기준, third-party 분리) ② Git LFS vs 파일 스냅샷 vs Perforce 비교(용량·비용·잠금·최초 반입 절차 — Codex 리뷰 요구 항목) ③ **권고안 1개** ④ 결정과 무관하게 안전한 **즉시 임시 백업 실행**: first-party 전수 → `D:\unreal\backups\2026-07-17_full_firstparty\` (복사만 — 무결정·무해·PC 크래시 2회 대비)
- **산출물**: `docs/백업_결정패키지_2026-07-17.md` — S2에서 오너가 보고 결정
- **커밋+태그**: `save/SP2b_백업패키지`

### 2c. balance#1 설계 초안 [SP2c]
- **balance-designer(별도 인스턴스)**: 포지션(탱/딜/서포터)×등급 스탯/킷 템플릿 + 스킬 원자 30개 카탈로그 초안. **명시 가정**: 등급 3단계(grades.csv 계승) / 직업 축은 존치·흡수 **양안 병기**. 포함: ⚠Spd 능동 스탯 승격(파트2가 넘긴 미결 — 고Spd 딜 선공 버스트 지배 검토), v1 §0 가정표 갱신, #7(3슬롯 재편) 흡수, #8(카운터 축 최소 1개) 흡수, 30원자 MotionRow 배정.
- **산출물**: `docs/features/캐릭터시스템/raw/balance1_포지션킷_v0.md` — **결재 대기 안** 상태로. balance#2는 #1 결재 후(순차 의존).
- **커밋+태그**: `save/SP2c_balance1초안`

### 2d. 문서 묶음 (2b sonnet에 병합 또는 별도) — **완료 2026-07-17**
- MVP 가이드 §2·§10 갱신(현재 게이트 = "오라클-diff 원장 봉인 + F9b" — 가이드 자체 유지 규칙 준수) + 파트2(31)·파트3(32) 이월 TC 처분 명기("코어 게이트 흡수, A2 회귀 스위트 이월" — qa 판정 인용, 승인자 Director)
- **실행 결과**: [[MVP_개발_핵심_운영가이드]](frontmatter+§2+§10 갱신) · [[야간F9a_풀회귀_완료]](superseded 배너) · [[파트2_SPD_TC]](P2-R01 23→20턴 정정 + 이월 31건 ID별 처분)+[[파트2_SPD_완료]](동일 정정) · [[파트3_연출_TC]](이월 32건 ID별 처분, 완료문서는 무접촉) · [[plan]](A1 잔여 명확화+F7b 링크) · 본 문서(frontmatter). 처분 결과: 코어게이트흡수 19 · S1오라클런흡수 15 · F9b흡수 14 · A2회귀스위트 11 · **폐기 2**(P3-P02·P3-P03 — 파트4 오너 재설계로 전제 무효, Director 판정) · **근거미확인 잔류 2**(P2-R07 스캐폴드 규약줄 미기재·P2-R09 노하우 N1/N2/N4 미등재 — 실재 문서부채, 이월 유지).

## Stage 3 — F7b 준비만 [SP3] (gameplay 조사 도착 — 확정)

- **Director**: F7b 세부 plan 초안 + 로그 스키마 확정안(§8-9 미결 — 가이드 §3.4 필드 포함) 문서화. **착수는 안 함**(S4 게이트). gameplay 실측 반영 필수 항목:
  - **④ 재산정 + 충돌 조항**: `ResolveHit` step2(HEAL 숏컷) 이관 시 **파트4 힐 표시 6노드(HpBefore·델타·SpawnDamageNumber#2)를 함께 포팅** — F7b 인계문서는 파트4 이전 작성이라 이 로직의 존재를 모른다. 조항 없이 §8-5 원문대로 수술하면 힐 표시가 조용히 삭제된다(gameplay CONFIRMED).
  - **⑦ 정정**: "1노드 스왑"이 아니다 — `Target`(단일) → `SelectedTargets`(배열) 타입 불일치로 `GetSelectedTargets` 신규 배선 필요.
  - **`ResolveHit` 노드수 대사**: 169 실측 vs 인계문서 149 — 파트4分 6노드 특정, **+14 미상**. 수술 전 `_savepoints/F7b_인터프리터골격/` 대조로 출처 확정.
  - **Method A 스캐폴드 채택안**(2a 참조): 구축 시점 = 원장 봉인 후, 검증 = 오너 원장과 diff 0.
  - **재수집 방식 영구 교정**: 향후 모든 원장 수집은 오라클-diff(관측-봉인 금지 — qa Critical).
- **커밋**: `save/SP3_F7b계획`

## 오너 복귀 시 브리핑 항목

1. SP 통과 현황 + 각 태그명 2. S1 실행 카드(2판 절차) 3. S2 백업 권고안 4. S3 balance#1 결재 요점(질문 2건) 5. 발생 이슈·중단 사항
