---
type: design
project: projectTP
updated: 2026-07-11
status: ★방향성 2 공식 확정 (오너 2026-07-11). 방향성 1은 백업 보관 — docs/방향성1_백업/
---

# 🎲 기획 방향성 (2안 비교)

> projectTP의 근본 게임 구조에 대한 두 방향. 오너가 방향성 2를 새로 제안(2026-07-10) — 1인 개발 적합성 때문에 전환 검토 중.
> 상위: [[projectTP_허브]] · 영향: 확정 시 [[로드맵_버전계획]]·[[알파_개발계획]] 대폭 재작성 필요.
#projectTP/기획방향성

## 방향성 1 — 수집형 (기존, 모바일 라이브)
**캐릭터를 랜덤으로 뽑아 내 자산으로 모으고 편성해 싸우는 게임.**
- 대기실(뽑기·편성) → 전투(vs AI/PvP) → 결과 → 대기실 루프 + 로컬/서버 저장
- **캐릭터 = 영속 자산**(뽑으면 내 것, 계속 보유)
- 타겟: **모바일 라이브 서비스**
- 기존 [[로드맵_버전계획]]·[[알파_개발계획]]·[[파츠_인벤토리]]·A0 스파이크가 전부 이 방향 기준
- **오너 우려(2026-07-10)**: 1인 개발로 **작업량·라이브 서비스 대응이 어렵다** — 지속 콘텐츠 공급·과금·계정/자산 관리·고객 대응 부담.

## 방향성 2 — 드래프트 단판 (신규 검토, Steam)
**매판 새로 뽑은 캐릭터로 밴픽하고 단판 4:4로 싸우는 오락실형 PvP.**
- **1:1 PvP 유지**
- 게임 시작 → **미리 만든 ~1000개 캐릭터 중 6개 랜덤 지급**(포지션별 2개씩: 탱/딜/힐) → **5개 선택** → **상대가 1개 밴** → **4:4 배틀**
- 전투 승리 → **골드 획득** → 골드는 **캐릭터 재굴림(리롤)**에 사용(맘에 안 드는 6개를 다시 굴림). **골드는 판 간 이월, 재굴림 전용**(오너 확정 2026-07-10)
- **캐릭터 = 자산 아님.** 오락실 게임처럼 단판 플레이하고 끝, 쌓이는 자산 사실상 없음(골드만 약하게 이월)
- 타겟: **Steam**
- **리텐션**(오너 확정): Steam 리더보드(승률·승수) 연동. 랭크/MMR은 출시 성과 보고 결정(우선 제외).

### 캐릭터 1000개 = 스킬 원자 조합 생성 (오너 확정 2026-07-10 — 밸런스 실현가능성의 열쇠)
"고유 1000개"를 한땀한땀이 아니라 **정형화된 원자의 조합**으로 생성:
- **모든 캐릭터 = 평타 1 + 공용스킬 1 + 직업스킬 1** (3슬롯)
- 평타 10종 × 공용 10종 × 직업 10종 = **10³ = 1000 조합** → **설계는 스킬 원자 30개만**
- AI/스크립트로 정형 규칙에 따라 1000개 데이터 생성(parts.csv 387콤보를 스크립트로 구운 것과 동일 방식)
- 목적: 매판 다른 조합이 나와 **"다른 랜덤 도파민"** — 질리지 않는 재플레이
- **핵심 이점**: 밸런스 대상이 1000개가 아니라 **원자 30개 안팎(+조합 상호작용)** → 1인 개발로 관리 가능. 우리 [[알파_개발계획]] §2.56 **데이터 드리븐 효과 시스템**(스킬=원자 조합)과 정확히 동일 철학.

### 생성 절차 (오너 확정 2026-07-10)
1. **1000개 캐릭터 ID 선정** → 각 ID에 **포지션 배정**(탱/딜/힐 ≈ 333/333/333).
2. **포지션별 직업스킬 ≈ 4종** 제작(포지션이 직업스킬 풀을 규정) + **공용·평타 ≈ 12종** 제작(전 포지션 공유).
3. **스크립트로 1000개에 조합 랜덤 배치** → **중복 조합 검출 시 재배치**(같은 조합 2개 방지). ← parts.csv 387콤보 생성 스크립트와 **동일 패턴**(조합 생성+무결성 검증, 우리 검증된 방식).
- **외형 ↔ 포지션 느슨한 매핑**: 탱커 외형이 마법 쓰면 위화감 → 외형(무기·의상)이 포지션을 **어느정도 준수**. 의도적 예외는 허용하되 기본은 포지션 일치. → parts.csv에 **포지션 태그** 추가(방향1의 미결 `JobTags` 문제가 여기서 자연 해결 — 무기 SPEAR/WAND 등 실측 5종을 포지션에 매핑).

## 대비표
| 축 | 방향성 1 (수집형) | 방향성 2 (드래프트 단판) |
|---|---|---|
| 캐릭터 | 영속 자산(보유·성장) | 매판 임시(단판) |
| 플랫폼 | 모바일 | Steam |
| 수익 모델 | 라이브 서비스(과금 추정) | 단품 판매 추정 |
| 서버 부담 | 계정·자산·저장 무결성 무거움 | 매칭·전투 판정 위주 가벼움 |
| 지속 콘텐츠 | 캐릭터·이벤트 계속 공급 필요 | 밸런스 패치 위주 |
| **1인 개발 적합성** | **낮음**(라이브 대응) | **높음**(단품·운영 최소) |
| 우리 합성 시스템(A0②) | 무한 조합 뽑기 | 1000개 사전 대량생성 |
| 기존 전투 MVP(4v4 턴제) | 재사용 | 재사용(밴픽·드래프트가 앞단에 추가) |

## Director 평가 (2026-07-10)
**방향성 2가 1인 개발 적합성에서 명확히 우세.** 근거:
- **라이브 서비스 부담 제거**: 매일 콘텐츠·이벤트·과금·고객대응·자산 분쟁이 없다. Steam 단품은 "만들어 팔고 패치"라 1인에 현실적.
- **서버 경량화**: 유저 영속 자산(계정·인벤토리·저장 무결성)을 관리 안 함 → 서버는 매칭+전투 판정만. [[알파_개발계획]] §2.56에서 확정한 "서버=경량 판정"과 더 잘 맞음.
- **우리가 방금 검증한 강점과 직결**: A0② 합성 머티리얼로 1000개 사전 생성 가능(parts.csv 387콤보 조합이면 쉬움). 기존 4v4 턴제 전투 MVP도 그대로 재사용.
- **트레이드오프**: 밸런스·리텐션·매칭에 방향1과 다른 종류의 난제(아래 열린 질문).

## 방향성 2 — 확정된 것 (오너 2026-07-10)
- ✅ **밸런스 모델**: 스킬 원자 조합. 포지션별 직업스킬≈4 + 공용·평타≈12 → 스크립트로 1000 조합 생성+중복 재배치. 밸런스 대상 = 원자 ~28개+조합 상호작용.
- ✅ **포지션**: 캐릭터 ID에 탱/딜/힐 배정(≈333씩). 직업스킬은 포지션이 규정.
- ✅ **스킬 공유**: 평타·공용은 **전 포지션 공유**(탱이 딜 평타 가능 = 조합 다양성↑).
- ✅ **외형↔포지션**: 느슨한 매핑(기본 준수+의도적 예외 허용). parts.csv에 포지션 태그.
- ✅ **골드**: 판 간 이월, 재굴림 전용.
- ✅ **리텐션**: Steam 리더보드(승률·승수). 랭크/MMR은 출시 후 결정(우선 제외).

## 방향성 2 — 다음 열린 질문 (남은 것)
1. **조합 여유 확인(가벼움)**: 포지션별 333개를 중복 없이 만들려면 (직업스킬×공용×평타) ≥ 333 필요. "공용·평타 12개"가 각각 12면 4×12×12=576≥333 여유 충분, 합쳐서 12면 부족할 수 있음 — 실제 스크립트 짤 때 확정.
2. **조합 상호작용 밸런스**: 원자 개별 밸런싱해도 특정 콤보가 우연히 OP/쓰레기 — 밴픽 재미의 원천이자 관리 포인트. **결정론 전투 공식(§전투완성)으로 1000 조합 자동 시뮬**하면 극단 조합 사전 검출(balance TTK 시뮬 확장).
3. **Steam PvP 매칭 유저풀**: 인디 PvP는 동접 부족으로 매칭 실패 흔함 → **봇/AI 폴백** 사실상 필수(방향1 vs-AI 재활용). 리더보드 우선이라 초기 vs-AI/비동기 대전도 후보.
4. **포지션 구성 규칙**: 6개(탱2딜2힐2)→5선택→1밴→4:4. 포지션 강제(최소 탱1힐1)가 있나, 밴으로 구성 붕괴 허용하나. 4:4 포지션 역할 정의도.
5. **캐릭터 ID 스키마**: [도메인 캐릭터][포지션 1자리][일련 3자리] 식 세그먼트 ID(방향1 규약 재활용) — 포지션이 ID에 인코딩되면 뽑기·밴픽 로직이 ID만으로 포지션 판별.

## 방향성 2 — 씬·화면 구조 (오너 정의 2026-07-11, 유니티→UE 매핑)

오너 흐름(유니티 기준): 게임시작 → 로비 → 매칭 → 전투 → 결과 → 로비(재활용). UE 번역 원칙 = [[알파_개발계획]] §2.6② 규약 계승: **화면 ≠ 레벨 — 레벨은 3D 무대가 다를 때만, 화면 전환은 위젯 교체**(레벨 로드는 무겁고 네트워크 연결이 끊김).

| 유니티 | UE | 우리 사용 |
|---|---|---|
| Scene / `LoadScene` | Level / `OpenLevel` | 무대 바뀔 때만(로비무대↔전투무대) |
| UI 프리팹 Instantiate | WBP 위젯 `CreateWidget`→`AddToViewport` | 화면·오버레이 전부 |
| `DontDestroyOnLoad`·static 전달 | **GameInstance**(레벨 전환 생존) | 드래프트 결과·전투 결과·누적 골드 운반 |

### 게임 flow 확정판 (오너 2026-07-11 — 레벨 2개 + 위젯 7종)
```
① 게임 진입   WBP_Title: 로고+2D 일러스트+Press Start (★로그인 없음 확정 2026-07-11 — Steam 자동 인증·접속상태만)
② 로비        [map_frontend] 3D 길드 하우스 — 영웅들 어슬렁(★랜덤 조합 캐릭터 스폰=정체성 쇼케이스)
③ Start       WBP_Lobby에서 클릭 → 로비 화면 그대로 waiting(WBP_Matching 오버레이: 스피너·취소)
④ 매칭 확정   ★WBP_Transition: FF6 전투돌입풍 와이프 → [map_battle] 레벨 전환(연출이 로드 시간 은폐)
⑤ 뽑기        [map_battle](판타지 세계풍 월드) 6개 랜덤 지급 — ★리롤: 공개 후 5초 내 1회 한정(골드 소모, 반복 불가)
⑥ 픽          6개 중 5개 픽 (30초)    ┐ 양측 동시 진행
⑦ 밴          상대 5개 중 1개 밴 (30초) ┘ (WBP_Draft 한 위젯의 3단계 상태: 뽑기·리롤→픽→밴)
⑧ 전투        4:4 (WBP_BattleHUD — 기존 턴제전투MVP 재사용)
⑨ 결과        WBP_Result 오버레이 → 분기:
   ├ 재시작: 레벨 전환 없이 전투 레벨에서 매칭 대기 → ④로 (알파=진짜 무전환 / PvP=새 상대 세션으로 내부 travel, UX 동일)
   │   └ 재매칭 실패 시: 로비로 이동(전투 레벨 무한 대기 없음)
   └ 로비로: OpenLevel(map_frontend) — 로비 재활용
```
- **매칭 대기 = 로비 화면(오너 확정)**: 별도 매칭 무대 없음(이전 "무대B" 구상 폐기 — "다른 배경"의 정체는 전투 레벨이었음). FF6 와이프는 로비→전투 **레벨 전환**을 덮는 위치로 확정 — 원전(필드→전투 돌입) 맥락과 일치+로드 은폐 실용 효과.
- **드래프트 위치 근거**: 밴은 상대와 주고받는 절차 → 같은 세션에 모인 뒤(전투 레벨) 수행.
- **GameInstance 운반**: 매칭 정보·드래프트 결과·승패·골드. SaveGame: 누적 골드.
- 에셋 메모: 길드 하우스(실내)·판타지 월드 전투 무대 콘셉트 — 무대 제작 시 판단(기존 map_battle_octopath 재활용/확장 여부 포함).

### flow 세부 규칙 확정 (오너 2026-07-11)
| 항목 | 확정 규칙 |
|---|---|
| 로그인 | **없음** — Steam 자동 인증. 진입 화면=로고+2D 일러스트+Press Start |
| 픽·밴 타이머 | **각 30초** (양측 동시 진행) |
| 리롤 | 6개 공개 후 **5초 내, 1회 한정**(골드 소모, 반복 불가) — 스냅 판단 설계 |
| 매칭 실패 | 로비 waiting = **그 화면에서 계속 대기**(취소 버튼으로 탈출). 전투 레벨 재매칭 실패 = **로비로 이동** |
| 이탈 | **상대편 승리 확정**(부전승) |

- 남은 세부(설계 시): 픽·밴 30초 초과 시 자동 처리 방식(랜덤 가정) / 전투 레벨 재매칭 "실패" 판정 시간(N초) / **골드 수지 밸런스** — 리롤이 판당 최대 1회·5초라 골드 소모처가 얇음 → 승리 골드 수입 대비 리롤 비용을 balance 설계 때 조정(인플레이션 시 추가 싱크 검토).

## 기존 자산 재활용 매핑 (방향2 확정 시)
방향2는 우리가 이미 만든 것의 재조합 — 헛수고 아님:
| 방향2 요소 | 재활용 기존 자산 |
|---|---|
| 캐릭터 1000개 외형 | A0② 합성 머티리얼 + parts.csv 조합 스크립트 |
| 스킬 원자 30개 | §2.56 데이터 드리븐 효과 시스템 + skills.csv 스키마 |
| 4:4 전투 | 턴제전투MVP(이미 완성) + 스탯·공식 v1 |
| 데이터 관리 | CSV top-down·세그먼트ID·다국어 규약 그대로 |
| 조합 밸런스 검증 | balance-designer TTK 시뮬 방식 확장 |
| 서버(경량 판정) | §2.56 서버 권위 범위 — 자산관리 없어 더 단순 |

## 포지션 구성 규칙 — Director 권고 (2026-07-11, 오너 결정 대기)
오너 고민: 도파민 터지는 랜덤성 ↔ 불합리 경험 걱정. 분석:
- **극단 랜덤은 이미 구조가 막고 있음**: 지급이 탱2/딜2/힐2 고정이라 "딜 6개" 참사는 불가능. 6→5픽→1밴 후 최종 4는 **2/1/1 또는 2/2/0** 두 형태뿐(수학적으로). 랜덤은 "어떤 스킬 조합이 오나"에만 있고 포지션 분포는 안전.
- **권고 ①: 최종 구성 강제(최소 1탱1힐 등)는 두지 않는다** — 강제하면 밴이 솔로 포지션을 못 건드려 전략성이 죽고, "힐 포기 4딜 러시" 같은 도파민 픽도 사라짐. 5픽에서 어느 포지션을 솔로로 남기느냐(=밴에 노출하느냐)가 심리전의 핵심.
- **권고 ②: 불합리 체감은 파워 평탄화 3중 보정으로** — 랜덤은 "제시"에, 공정은 "파워 총량"에 두는 원칙(TFT류 정석):
  1. **등급 축 없음 유지** — 방향2는 전 캐릭터 동일 기초 스탯(차이=스킬 조합뿐). "쟤는 3성 나왔는데" 류의 선천 격차가 원천 부재. ★이후에도 등급 도입 금지 권고(넣는 순간 불합리 폭발)
  2. **1000조합 자동 시뮬**로 극단 OP/폐급 콤보 사전 제거(기확정 계획)
  3. **리롤 1회**(기확정) — 최악 로스터 보험
- **권고 ③: 2/2/0(힐 전멸)도 허용하되 이길 수 있게** — no-heal 구성이 "패배 확정"이 아니라 "더 빠른 레이스"가 되도록 밸런스(v1에서 힐을 아웃힐 불가로 설계한 기조와 일치). 불합리가 아니라 다른 게임플랜이 되게.
- 선택지(알파 이후 테스트 후 도입 검토): **보호 지정** — 픽한 5개 중 1개 밴 면역 지정(심리전 추가, 밴의 일방성 완화). 규칙 복잡도가 늘어 알파엔 미포함 권고.

## 다음
~~오너가 1 vs 2 최종 결정~~ → **방향성 2 공식 확정(2026-07-11).** 방향성 1 문서는 `docs/방향성1_백업/`에 보관(방향성1_로드맵·방향성1_알파개발계획). 다음 작업: [[로드맵_버전계획]]·[[알파_개발계획]]을 방향성 2 기준으로 재작성(재활용 매핑표 기반) + 포지션 권고 오너 승인.
